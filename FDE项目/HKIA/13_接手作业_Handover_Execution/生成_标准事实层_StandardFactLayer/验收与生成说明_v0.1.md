# 验收与生成说明 · 标准事实层 Standard Fact Layer v0.1

> 对应 B2「2023Q1–2026Q1 同期序列正式接入」。
> 目标：把 72 条市场核心事实 & 4914 条公司事实固化为可查询标准事实表。

## 1. 生成说明（如何重建）
```bash
cd 生成_标准事实层_StandardFactLayer
python3 scripts/build_standard_fact_layer.py
```
- 依赖：`pandas`、`openpyxl`、`sqlite3`。
- 输出：`data/standard_fact_layer_2023_2026Q1.db` + `qa/standard_fact_layer_qa_report.md`。
- 幂等：重跑会重建 DB（删除旧库后重建），QA 报告覆盖写入。

## 2. 验收对照（B2 完成条件）
| B2 要求 | 达成 | 证据 |
|---|---|---|
| 固化为可查询标准事实表 | ✅ | SQLite 三表，`query_examples.py` 演示 |
| 保留：期间 / 指标ID | ✅ | market_facts、company_facts 均含 period / metric_id |
| 保留：原始值 / 单位 | ✅ | `value`（HKD_thousand/count）保留原值，不折算 |
| 保留：流量/存量 | ✅ | `period_basis` = flow_during_period / stock_at_period_end |
| 保留：可比等级 | ✅ | `comparability` 字段，个人 6 指标标 bridge |
| 保留：来源 locator | ✅ | market: source_sheet/source_range/source_file；company +source_cell |
| 缺值纪律 | ✅ | reported_missing 2471 保 NULL，不补零 |
| 公司事实覆盖 | ✅ | 4914 行，18 指标 × 4 期间 |

## 3. 质量验收（QA）
- **覆盖**：市场去重指标 18 / 公司去重指标 18 ✓
- **缺失纪律**：reported_numeric 2443 / reported_missing 2471 ✓
- **reconcile**：2026Q1 全 18 指标，市场总额 vs 公司数值和 **差异全为 0** ✓
  （例：NB_IND_TOTAL_SINGLE_PREMIUM 市场 90,464,756 = 公司和 90,464,756）

## 4. 边界与诚实披露
- 本层是**已落库资产的再固化**，未改动任何接手前原始文档 / DB 本体。
- 单位以官方原值（HKD_thousand / count）存储；亿港元等衍生口径需消费方自行折算。
  ⚠️ 历史 `report_data` 的 `value_yi` 字段存在 ×10 偏差，本层不走该字段，直接用官方 `value`。
- 2026Q1 Chubb（整付 +44.97倍）、canada_mypace 等转移事件经 `business_lineage`/`bridge_class` 承接，单值不虚增。
- 可比跨 2025 断点仅限个人 6 个核心指标（`comparable_with_schema_bridge`）。

## 5. 下一步
- [ ] 公司排名仅用于通过 identity/outlier gate 的指标。
- [ ] 验证后回流 `07_项目立项启动/`（依项目治理约定）。
- [ ] 承接 B1（2025 provisional 标签接入）时复用本层的 schema_metrics 字典。
