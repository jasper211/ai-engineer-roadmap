# ICD-T019 · 业务分析输出与生产化查询消费

> 执行与审计：Codex  
> 状态：ACCEPTED  
> 日期：2026-09-08

## 目标与门禁

基于最新成功快照生成可消费的 JSON 与 Markdown 业务分析，覆盖分红披露完整度、数值分布、RBC 资本缓冲和证据 run_id。分析必须只读数据库，保留产品不可直接横比、历史结果不代表未来、RBC 按法律主体而非集团等解释门禁；不得把官网占位值当零或以汇总统计替代底层证据。

## 执行与审计回执

- 新增 `tools/icd_analysis.py` 与 CLI `--analyze`，从 SQLite `mode=ro` 生成 JSON/Markdown；默认输出到受控 `reports/` 目录。
- 分红分析覆盖 9 家、13,039 条，输出报告年度、产品数、观测数、数值/占位数量、指标构成、数值率与分位分布，并携带证据 run_id。
- RBC 分析覆盖 8 个法律主体，输出比率、资本基础/PCA 原文、来源 URL、SHA-256 与 run_id；按法律主体而非集团解释。
- 报告固定写明四项解释门禁，禁止把中位数当跨产品排名、把占位原文当零或把历史履行率当未来保证。
- T019 定向测试验证数据库哈希前后不变、统计恒等式、证据字段、JSON/Markdown 与 CLI；T002–T019 共 16 套测试脚本全量通过。
- 效率指标：Codex 单闭环完成；返工 0；人工 Gate 0；DeepSeek 调用 0。
