# ICD-T008 · 真实验证证据（AIA Company Limited 2024 RBC 官方索引发现 → PDF 解析入库）

> 执行：vscode-deepseek  
> 日期：2026-09-03 / 2026-09-04（UTC）  
> 状态：SUBMITTED（待 Codex 审计）  
> 网络验证与 fixture 测试分开记录；本文件只记录真实网络证据。

## 一、真实网络证据链（两段）

### 段 1：官方索引（source_id=12）

- 命令：`SSL_CERT_FILE=/etc/ssl/cert.pem python3 04_定义Agent_Define_Agent/agents/agent.py --fetch 12`
- 结果：`OK`，HTTP 200
- final_url：`https://www.aia.com/en/investor-relations/overview/regulatory-disclosures`
- 字节：243320；SHA-256：`0a581e8a5f26a17e774592fb2c8c650ac4a83ec8d78aa1d5cb74649063e90972`
- 快照：`raw_data/AIA/12/0a581e8a5f26a17e774592fb2c8c650ac4a83ec8d78aa1d5cb74649063e90972.html`
- fetch_run.run_id = 7

索引页「Disclosure Material」以 tab 结构列出三家持牌实体的 2024 英文 Disclosure Statement PDF：

- `AIA Co Disclosure Statement 2024_Eng.pdf`（AIA Company Limited，偿付能力比率 304%）
- `AIAI Disclosure Statement 2024_Eng.pdf`（AIA International Limited，212%）
- `AIAE Disclosure Statement 2024_Eng.pdf`（AIA Everest Life Company Limited，457%）

### 段 2：发现 → 最终 PDF（source_id=22）

- 命令：`... --discover 12`
- 结果：`OK`，candidate_count=3，filename_hint=`AIA Co Disclosure Statement 2024_Eng.pdf`
- 发现的 PDF URL：`https://www.aia.com/content/dam/group-wise/en/docs/investor-relations/2025/AIA%20Co%20Disclosure%20Statement%202024_Eng.pdf`
- 命令：`... --fetch 22` → `OK`，HTTP 200，`%PDF-1.6`
- 字节：2678448；SHA-256：`8061102051f14ae91729d12eac138e0f6e573a42ee183c8f3a406958075b696b`
- 快照：`raw_data/AIACO/22/8061102051f14ae91729d12eac138e0f6e573a42ee183c8f3a406958075b696b.pdf`
- fetch_run.run_id = 8
- 文字层：12 页，14186 字符（非扫描件，pdfplumber 直接提取，无 OCR）

## 二、法律主体确认（任务书功能要求 3）

- PDF 第 3 页「1 Company profile」：`(a) Authorised insurer's name` → `AIA Company Limited (the "Company")`。
- 解析器提取（剥离定义性括号后）：`legal_entity_name_raw = "AIA Company Limited"`。
- 该主体 **≠** 注册表 `AIA`（`AIA International Limited`，寿险）。按 T007 已确认主体隔离政策，建立独立 `insurer_code=AIACO`（AIA **Co**mpany），`name_en=AIA Company Limited`。
- 佐证（本次附带核实，非任务目标）：AIAI PDF 法律主体 = `AIA International Limited`（212%）、AIAE PDF = `AIA Everest Life Company Limited`（457%），三者确为不同持牌实体。

## 三、解析入库结果（rbc_statement）

`--parse 22` → `OK`，run_id=8，report_year=2024，records_written=1，parse_status=OK。

DB 直查 `rbc_statement` 单行：

```
('AIACO', 8, 2024, 'AIA Company Limited', 3.04, '304%',
 70993766000.0, '70,993,766', 23371785000.0, '23,371,785',
 'HKD', 'in HKD thousands', 'thousands')
```

三处 PDF 原文 → DB 逐字核对：

1. `Ratio of capital base to prescribed capital amount 304%` → `'304%'` / `3.04`
2. `Capital base 70,993,766`（Unit: in HKD thousands）→ `70,993,766,000.0`（原文 `'70,993,766'`）
3. `Prescribed capital amount 23,371,785` → `23,371,785,000.0`（原文 `'23,371,785'`）

`risk_breakdown_json`：prescribed_capital_components=30 行（市场/利率/信用利差/权益/物业/货币/寿险/一般保险/对手方违约/操作风险 + 多元化收益与调整项）、capital_base_components=5 行（Unlimited/Limited Tier 1、Tier 2、Capital base），无损保留原文与标度（`unit: in HKD thousands`）。

## 四、数据库一致性 / 幂等 / 证据回查

- `PRAGMA integrity_check` = `ok`；`PRAGMA foreign_key_check` = `[]`。
- 自然键 `(insurer_code, report_year, run_id)` 全表重复 = 0；`AIACO` rbc 行 = 1（重复 `--parse 22` 幂等仍 1 行）。
- 迁移：`--init-db` 将默认库 `PRAGMA user_version` 4→5，`icd.db.pre-v5.bak` 备份生成；`data_source[12]` format `pdf`→`html`；新增 AIACO 主体与 AIACO rbc PDF 源（12 险企 / 22 源）。
- rbc_statement(AIACO).run_id=8 → fetch_run.final_url = 最终 PDF URL、http_status=200、content_hash、snapshot_path 均可回查；索引段 run_id=7 独立保留为发现证据。

## 五、命令与退出码

| 命令 | 退出码 | 关键结果 |
|---|---|---|
| `--init-db` | 0 | user_version=5，12 险企 / 22 源，迁移 actions 记录 |
| `--fetch 12` | 0 | HTTP 200 / 243320 B / hash `0a581e8a…` |
| `--discover 12` | 0 | 3 候选 → 消歧定位 AIA Co PDF |
| `--fetch 22` | 0 | HTTP 200 / 2678448 B / `%PDF-1.6` / hash `80611020…` |
| `--parse 22` | 0 | records=1 / 304%→3.04 / AIACO |

## 六、未决说明（供 Codex 复核，非阻断）

1. **并行执行观察**：本工作区在本次执行期间观察到已存在一套 T008 实现（`rbc_parser.py`/`rbc_index_discovery.py`/fixtures/迁移/`--discover` 等，时间戳早于本执行），判断为并行/先前执行产物。执行方采纳该实现为唯一主线、撤销自身重复核心模块，并在此基础上补齐：`fetch_recorder.get_source` 返回 `parser_hint`（否则 `--discover` 无法从注册表 parser_hint 提取消歧文件名，真实索引 3 候选会永久歧义）、新增 T008-13 CLI `--discover` 确定性测试、真实网络验证、README/settings 与回执。请 Codex 按文件路径与内容差异审计，忽略非 ICD 路径变更。
2. **http_fetcher UA 变更**：www.aia.com（Akamai 边缘）对非浏览器 UA 挂起连接，T008 将固定 UA 改为浏览器 UA（无登录、无 Cookie、无凭证），仅影响该 HTTP 抓取层；T002-T008 全量回归通过。
3. **AIAI 212% / AIAE 457%**：注册表 parser_hint 中作为索引歧义上下文标注的这两个比率，本次用 curl 独立核实成立（AIAI=212%、AIAE=457%），但二者非本任务采集目标，仅作消歧上下文，未入库。
