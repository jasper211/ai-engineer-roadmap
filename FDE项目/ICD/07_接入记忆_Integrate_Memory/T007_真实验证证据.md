# ICD-T007 · Prudential RBC 真实验证证据（受控真实 PDF / DB 验证产物）

> 执行方：vscode-deepseek · 生成日期：2026-09-03 · 状态：SUBMITTED 回执配套证据
> 说明：本文件是 T007「真实数据抽查」的确定性证据，与 fixture 测试分开记录。
> 可再生文本素材见同目录 `T007_真实PDF可再生素材_提取文本.txt`（由受控快照用 pdfplumber 提取，非 OCR）。

## 一、受控真实 PDF 快照

- **source_id**: 13（PRU · rbc · pdf）
- **final_url**: https://www.prudential.com.hk/content/dam/prudential-phkl/pdf/en/regulatory-information/PGHK-RBC-public-disclosure-statement-2024.pdf
- **HTTP 状态**: 200（HTTP/2）
- **Content-Type**: application/pdf（`x-content-type-options: nosniff`）
- **字节数**: 242184
- **文件签名**: `%PDF-1.7`（前 8 字节 `25 50 44 46 2d 31 2e 37`）；文件尾含 `%%EOF`
- **SHA-256**: `b61630e9b275146bb4ea16a1f60ae189aa2e19daae11ea8a13751d66f97d0d51`
- **快照路径**: `raw_data/PRU/13/b61630e9b275146bb4ea16a1f60ae189aa2e19daae11ea8a13751d66f97d0d51.pdf`
- **文字层验证**: 4 页，共 8142 字符，非扫描件（无需 OCR），PDF 文字层完好。

## 二、关键结构发现（供审计，非阻断）

- 该 PDF 主体公司为 **「Prudential General Insurance Hong Kong Limited」**（保诚一般保险（香港）有限公司，一般保险），
  与注册表 `insurer_code=PRU` 登记的 **「Prudential Hong Kong Limited」**（保诚保险有限公司，寿险）是同一集团下的**两个不同持牌实体**。
- 注册表 parser_hint 预期「结构同友邦（寿险）」并按 290% 登记，但真实 PDF 是一般保险实体的披露声明。
- 本任务书禁止修改 `source_registry.json`，故仅在证据中记录该「主体身份差异」，未改注册表，未阻断解析——
  偿付能力比率（290%）、报告年度（2024）、币种（HKD）及金额均为该 PDF 明确披露，且核心口径可由现有 RBC Schema 无损表达。

## 三、解析结果（从快照独立提取，未写死 290%）

| 字段 | 原文（PDF 逐字） | 标准化入库值 |
|---|---|---|
| report_year | "Disclosure Statement **at 31 December 2024**" | `2024` |
| solvency_ratio_raw | "Ratio of capital base to prescribed capital amount **290%**" | `290%`（原文） |
| solvency_ratio | 同上 | `2.90`（=290/100） |
| currency | "**(Unit: in HKD thousands)**" | `HKD` |
| capital_base | "Capital base **581,167**"（千港元） | `581167000.0`（HKD） |
| prescribed_capital_amount | "Prescribed capital amount **200,745**"（千港元） | `200745000.0`（HKD） |

### 三处逐字抽查（PDF 文本 → DB 字段）

1. PDF 第 4 页 `Ratio of capital base to prescribed capital amount 290%`
   → DB `solvency_ratio_raw='290%'`、`solvency_ratio=2.9` ✅
2. PDF 第 3 页 `Capital base 581,167`（表头 "Unit: in HKD thousands"）
   → DB `capital_base=581167000.0` ✅
3. PDF 第 3 页 `Prescribed capital amount 200,745`（表头 "Unit: in HKD thousands"）
   → DB `prescribed_capital_amount=200745000.0` ✅

（另抽查：`As at 31 December 2024` → `report_year=2024`；`Unit: in HKD thousands` → `currency='HKD'`。）

## 四、口径说明（金额标度，重要）

- 披露金额以 **「in HKD thousands」（千港元）** 给出；`rbc_statement.capital_base / prescribed_capital_amount`
  语义为「币种金额」，故按 PDF 明示标度 ×1000 折算为绝对 HKD 入库（581,167 千 → 581,167,000）。
- 该折算**非猜数/推算缺失数据**，而是 PDF 明示单位换算；原文数值与标度原样保留在
  `risk_breakdown_json`（含 `unit: "in HKD thousands"`、`capital_base_raw: "581,167"`、
  `prescribed_capital_amount_raw: "200,745"` 及全部 22 条 PCA 子风险行、5 条资本基础组成行）。
- 若项目方偏好「保留披露原值 + 新增标度列」而非「折算绝对币值」，属 Schema 口径决策，留待 Codex/Jasper 裁定。

## 五、数据库验证产物（默认库 icd.db）

- `rbc_statement`：`(rbc_id=1, insurer_code='PRU', run_id=6, report_year=2024, solvency_ratio=2.9, solvency_ratio_raw='290%', capital_base=581167000.0, prescribed_capital_amount=200745000.0, currency='HKD')`
- `parse_result`：`(run_id=6, parse_status='OK', records_produced=1, error_code=NULL)`
- `fetch_run` 证据链（run_id=6）：`source_id=13 → final_url（上述 PDF URL）→ http_status=200 → content_hash=b61630e9… → content_length=242184 → snapshot_path=raw_data/PRU/13/b61630e9….pdf → fetch_status='OK'`
- `PRAGMA integrity_check` → `ok`；`PRAGMA foreign_key_check` → 空（无违例）；`rbc_statement` 行数 = 1。
- **幂等**：重复 `--parse 13` 后 `rbc_statement` 仍 1 行，`parse_result` 仍 1 行（run_id=6）。

## 六、命令与退出码（真实网络 + 回归）

```bash
# 1) T003 抓取（真实网络）
SSL_CERT_FILE=/etc/ssl/cert.pem python3 04_定义Agent_Define_Agent/agents/agent.py --fetch 13
# EXIT=0 · result=OK · http_status=200 · content_hash=b61630e9… · content_length=242184

# 2) PDF 签名复核
xxd <snapshot> | head -1    # 2550 4446 2d31 2e37  (%PDF-1.7)
shasum -a 256 <snapshot>    # b61630e9b275146bb4ea16a1f60ae189aa2e19daae11ea8a13751d66f97d0d51

# 3) 解析（真实网络 → 默认库）
python3 04_定义Agent_Define_Agent/agents/agent.py --parse 13
# EXIT=0 · result=OK · run_id=6 · report_year=2024 · records_written=1 · parse_status=OK

# 4) 语法编译
python3 -m py_compile 05_集成工具_Integrate_Tools/tools/*.py 07_接入记忆_Integrate_Memory/memory/*.py \
  06_开发技能_Develop_Skills/skills/*.py 04_定义Agent_Define_Agent/agents/*.py 09_测试与调试_Test_and_Debug/tests/*.py
# EXIT=0

# 5) 全量回归（fixture，不联网，tempfile）
python3 09_测试与调试_Test_and_Debug/tests/test_integration.py   # EXIT=0（T002+T003）
python3 09_测试与调试_Test_and_Debug/tests/test_t004_parse.py    # EXIT=0
python3 09_测试与调试_Test_and_Debug/tests/test_t005_parse.py    # EXIT=0
python3 09_测试与调试_Test_and_Debug/tests/test_t006_parse.py    # EXIT=0
python3 09_测试与调试_Test_and_Debug/tests/test_t007_parse.py    # EXIT=0（15 组，T007）
```

## 七、依赖

- PDF 文本/表格提取库：**pdfplumber 0.11.10**（基于 pdfminer.six 20260107，维护活跃）。
- 依赖声明：`requirements.txt`（`pdfplumber>=0.11.0,<0.12`），非静默依赖全局环境。
- 解析器不联网、不执行 PDF 内嵌代码（pdfplumber/pdfminer 只解析，不运行 JS/动作）。
