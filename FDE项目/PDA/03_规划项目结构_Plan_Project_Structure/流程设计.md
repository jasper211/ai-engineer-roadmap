# PDA · 流程设计（SOP 第2步）

> 版本: v0.7.0
> 状态: 按 Jasper "按照你的发现和逻辑出来" 的授权直接推进，不再逐条等待确认
> 日期: 2026-09-17
> 前置文档: [需求定义.md](../01_初始化项目_Initialize_Project/需求定义.md)（第十一节"真实底表核实发现"是本流程设计的直接依据）、[S8衍生字段_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S8衍生字段_反推标准_v0.1.md)（L3-PDA-05的直接依据）、[S1_总览仪表盘_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S1_总览仪表盘_反推标准_v0.1.md)（L3-PDA-06的直接依据）、[S2_业务端视角_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S2_业务端视角_反推标准_v0.1.md)（L3-PDA-07的直接依据）、[S3_执行管理端_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S3_执行管理端_反推标准_v0.1.md)（L3-PDA-08的直接依据）、[S4_产品端视角_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S4_产品端视角_反推标准_v0.1.md)（L3-PDA-09的直接依据）、[S5_财务端视角_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S5_财务端视角_反推标准_v0.1.md)（L3-PDA-10的直接依据）

## v0.7.0变更摘要

反推还原 S5_财务端视角 全部8个板块（A/B-保费/B-件数/C-APE/C-件数/D/E/F/G/H），新增 **L3-PDA-10**。S5是目前口径最简单的一张表（基础过滤统一是`status=生效 AND issue_year=2026`，没有未批核/待签维度）。三处复用：D/E节分档边界直接复用report_enricher的PREMIUM_BUCKET/APE_BUCKET常量；G/H节"保司"简称复用S4的CARRIER_SHORT_NAME映射；"牌照"简称是S5独有的简单规则（issuing_entity前4个字截断，不是查表）。另发现G/H节TOP20排名遇到金额并列时按sign_date升序排（不是随机顺序），用真实数据里的并列案例验证过。S2的S/T板块+S3的E/F/J-O子板块+S6-S7、S9共4张表留待下一批。

## v0.6.0变更摘要

反推还原 S4_产品端视角 全部5个板块（A-E，是目前唯一覆盖率100%的一张表），新增 **L3-PDA-09**。发现两条S4专属规则：①A节排除状态范围（4种，含拒保）跟B-E节（3种，不含拒保）不同，不能混用；②产品TOP20的联合主键需要把SQ_rate标准化(数值0.025和字符串"2.5%"是同一个值)再分组，且SQ_rate缺失的约80%记录整体不参与这个排名（不是归成一个"缺失"分组）。另确认carrier_code→保险公司简称的报表专属映射，模式上跟S1-H节的"牌照"映射一致。S2的S/T板块+S3的E/F/J-O子板块+S5-S7、S9共5张表留待下一批。

## v0.5.0变更摘要

反推还原 S3_执行管理端 核心6个子板块（A/B/C/D/G/H，20个子板块中的6个），新增 **L3-PDA-08**。最重要发现：S3的"周"定义=`dt.strftime('%YW%U')`（周日起始的%U惯例周，不是ISO周），用连续3周真实数字排除了ISO周、"从年初简单按7天分段"两种猜测。另发现真实脏数据（一条保单批核日早于签单日，TAT=-2天），报表在"签批时效分档"里会排除这类负数记录，不计入任何分档，但合计行仍是全量口径——是需要单独处理的边界情况，不是清洗bug。S3的E/F（未批核待签透视表）+J-O（同行/银行周度趋势）留待后续（结构较复杂或高置信度推算不再逐一验证）。

## v0.4.0变更摘要

反推还原 S2_业务端视角 核心12个子板块（A/B/C-H/I/J/K/O，20个子板块中的12个），新增 **L3-PDA-07**。用A/B节的目标金额把v0.3.0遗留的BRK/SLBRK映射方向问题解决了（双重金额验证，置信度从"低"升到"高"）。发现S2专属规则："同行经代"分组=segment_code IN('同行经代','MGA业务')，跟S1"业务大类"的MGA→经代业务规则是两条独立规则；"流失类"=取消投保/退保/搁置受保(不含拒保)，跟S1"流失"口径一致；KEY ACCOUNT按segment拆分MGA业务时会生成"账户名(MGA)"独立行。S2的S/T板块（partner_code维度）+ S3-S7/S9共6张表留待下一批。

## v0.3.0变更摘要

目标APE数据源已找到并核实（服务器mga_platform数据库public.fact_target表，只读，见[目标APE数据源核实.md](../01_初始化项目_Initialize_Project/目标APE数据源_fact_target_核实.md)），解除了S1的阻塞。反推还原 S1_总览仪表盘 A-H 全部8个板块，用真实报表逐项核验全部通过，新增 **L3-PDA-06**。Jasper 还提供了报表→PPT阶段的参考代码（`raw_data/业绩报表PPT/`），但确认这是下游阶段、架构风格（硬编码补丁链）不适合直接复用，仅作为后续核对数字的参考材料。S2-S7、S9共6张专题视角表+PPT生成留待下一批。

## v0.2.0变更摘要

Jasper 说明真实工作场景是"raw表 → Python+模型结构要求 → 9-sheet专题报表(业绩分析报表) → Python → 周报PPT"，比阶段一深得多。现有生成代码 Jasper 暂时无法提供，改为从产出物反推标准（S8衍生字段_反推标准_v0.1.md），新增 **L3-PDA-05**。S1-S9其余7张专题视角表+PPT生成留待下一批（涉及外部"目标APE"数据源，在服务器数据库、目前只读未接入）。

---

## 一、能力线说明

demo 只有一条线性流程：**底表读取 → 清洗标准化 → 多维聚合 → 看板生成**。跑一次生成一份 HTML 即可，暂不做调度/增量（阶段二前提未确认，见需求定义 4.2）。

## 二、L3 端到端流程

```
┌───────────┐    ┌───────────┐    ┌───────────┐    ┌───────────┐    ┌───────────┐
│ L3-PDA-01 │ →  │ L3-PDA-02 │ →  │ L3-PDA-03 │ →  │ L3-PDA-04 │    │ L3-PDA-05 │
│ 底表读取   │    │ 清洗标准化 │    │ 多维聚合   │    │ 看板生成   │    │ 报表衍生字段│
│ +完整性校验│    │ (含真问题修复)│  │ (7类矩阵) │    │ (HTML)    │    │(S8的13个字段)│
└───────────┘    └───────────┘    └───────────┘    └───────────┘    └───────────┘
```

L3-PDA-05 是独立分支，接在 L3-PDA-02 之后、不依赖 L3-03/04——它服务的是"复刻9-sheet专题报表"这条后续能力线，跟"生成看板"这条能力线目前是并行、不交叉的两条线（呼应 SOP 第2步"不是所有Agent都只有一条线性流程"的提醒）。

## 三、每个 L3 的输入输出

| L3 编码 | 名称 | 输入 | 输出 | 对应 skill |
|---------|------|------|------|-----------|
| L3-PDA-01 | 底表读取+完整性校验 | `raw_data/*.xlsx` | 原始 DataFrame（剔除表头残留行）+ 列完整性报告（30 列是否齐全，缺列直接报错不静默） | `skills/data_loader.py` |
| L3-PDA-02 | 清洗标准化 | L3-01 产出的原始 DataFrame | 清洗后 DataFrame，新增 `policy_status_tier`（生效/在途/终止）、`future_dated`（bool）字段，日期字段类型统一为 datetime | `skills/cleaner.py` |
| L3-PDA-03 | 多维聚合 | L3-02 清洗后 DataFrame | 聚合结果 dict（entities/months/top_carriers/by_entity_*/cycle_avg/fin_by_entity，结构对齐阶段一原型的 `DATA` 对象，保证看板 JS 不用改） | `skills/aggregator.py` |
| L3-PDA-04 | 看板生成 | L3-03 聚合结果 | 可交互 HTML 文件 | `skills/dashboard_generator.py` |
| L3-PDA-05 | 报表衍生字段 | L3-02 清洗后 DataFrame | 附加13个衍生字段的 DataFrame（对应`业绩分析报表_0724.xlsx`的S8_明细数据底表），存CSV | `skills/report_enricher.py` |
| L3-PDA-06 | S1总览仪表盘 | L3-05 附加"业务大类"的 DataFrame + fact_target快照 | S1_总览仪表盘 A-H 八个板块结果，各存一份CSV | `skills/s1_dashboard.py` + `tools/fact_target_sync.py` |
| L3-PDA-07 | S2业务端视角 | L3-02 清洗后 DataFrame（本节直接用segment_code，不需要"业务大类"） + fact_target快照 | S2_业务端视角 核心12个子板块结果，各存一份CSV | `skills/s2_business_view.py` |
| L3-PDA-08 | S3执行管理端 | L3-02 清洗后 DataFrame（同样直接用segment_code） | S3_执行管理端 核心6个子板块结果，各存一份CSV | `skills/s3_execution_view.py`（复用s2的SEGMENT_GROUPS/LAPSE_STATUSES常量） |
| L3-PDA-09 | S4产品端视角 | L3-02 清洗后 DataFrame | S4_产品端视角 全部5个板块结果，各存一份CSV | `skills/s4_product_view.py` |
| L3-PDA-10 | S5财务端视角 | L3-02 清洗后 DataFrame | S5_财务端视角 全部8个板块结果，各存一份CSV | `skills/s5_finance_view.py`（复用report_enricher的分档常量+s4的CARRIER_SHORT_NAME） |

## 四、L3-PDA-02 清洗规则明细（直接对应需求定义第七、十一节，逐条可测试）

| 规则 | 依据 |
|------|------|
| 剔除 `policy_id=='订单编号'` 的表头残留行 | 需求定义 7 |
| `res_date`/`sign_date`/`submit_date`/`issue_date`：单元格为 `int`/`float` 时按 Excel 序列号（`datetime(1899,12,30)+timedelta(days=值)`）换算；为 `datetime`/`date` 时直接用 | 需求定义 11.2【真问题1】 |
| `future_dated` = `sign_date` 晚于底表文件名日期（从文件名正则提取 `YYYYMMDD`，取不到则退化用文件 mtime） | 需求定义 11.3【真问题2】——不再硬编码 2026-07-24，換成从文件名动态解析，避免底表更新后阈值过期 |
| `policy_status_tier` 映射：`生效`→生效；`待批核/pending/排期/已签单/尚欠保费`→在途；`取消投保/退保/搁置受保/拒保`→终止（沿用阶段一原型footer已经写死的口径，不是本次新发明） | 需求定义 七 |
| `premium`/`ape` 缺失按 0 处理 | 需求定义 七 |
| `sum_assured` 的全角/半角空格值统一转 `NaN`（不当有效值参与后续任何聚合） | 需求定义 11.4 |
| `currency_code` 缺失归为 `"未填"` | 需求定义 七 |
| `sum_assured`/`SQ_rate`/`referral_code`/`premium_term` 本版本**不参与任何聚合**，清洗时保留原样透传，不清洗不假装处理——docstring 里显式注明"已知未处理" | 需求定义 11.4 |

## 五、L3-PDA-03 多维聚合口径（对齐阶段一原型 `DATA` 结构，需求定义第八节 7 类矩阵）

聚合全部以**清洗后的 `premium`（已折算港币）** 为金额口径，`count` 为记录数：

| 聚合键 | 维度 | 对应第八节矩阵项 |
|---|---|---|
| `by_entity_all` | issuing_entity | 1 业绩趋势的基数 |
| `by_entity_month` | issuing_entity × 签单月份(`sign_date` 取 `YYYY-MM`) | 1 业绩趋势 |
| `by_entity_bcat` / `by_entity_mkt` | issuing_entity × business_category / market_segment | 2 业务结构 |
| `by_entity_prod` / `by_entity_carrier` | issuing_entity × product_category / carrier_code（carrier 按保费 top6 + 其他分桶，沿用阶段一原型口径） | 3 产品与保司结构 |
| `by_entity_cust` | issuing_entity × customer_type | 4 客户结构 |
| `by_entity_status` | issuing_entity × `policy_status_tier` | 5 状态结构 |
| `cycle_avg` | issuing_entity → 平均(`issue_date - sign_date`天数)，仅统计两个日期都非空的记录 | 6 运营效率（用修正后的日期计算，消除【真问题1】的污染） |
| `fin_by_entity` | issuing_entity → [`Is_Premium_Financing==1`计数, 总数] | 7 风险结构·融资单占比 |
| `by_entity_ccy` | issuing_entity × currency_code | 7 风险结构·非本币占比 |

## 六、L3-PDA-05 衍生字段规则明细（对应 S8衍生字段_反推标准_v0.1.md 一节，12/13已100%核验）

见 [S8衍生字段_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S8衍生字段_反推标准_v0.1.md) 一节完整表格——保单阶段/业务大类/融资标签/年期分类/保费分档/APE分档/签批时效(天)/签单年/签单年月/批核年/批核年月/预约年月 共12个字段已用真实`业绩分析报表_0724.xlsx`的S8 sheet逐行核验100%匹配；"首年折扣(标准化)"来源字段已确认=SQ_rate，标准化分档规则待Jasper提供，本版本原样透传。

## 七、L3-PDA-06 S1_总览仪表盘规则明细（对应 S1_总览仪表盘_反推标准_v0.1.md，A-H全部100%核验）

见 [S1_总览仪表盘_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S1_总览仪表盘_反推标准_v0.1.md) 完整表格。关键结论（可能适用于后续S2-S9）：
- "流失"口径明确排除"拒保"（承保前被拒不算流失）
- 凡按业务类型分组，必须用 `report_enricher` 的"业务大类"衍生字段（含MGA→经代业务重分类），不能用原始 `business_category`
- 目标APE的"全业务/永明业务"口径要排除 `segment_code IN ('NGP','TA')`（实证得出，原因待Jasper确认，见目标APE数据源核实.md）
- H节"牌照"列（JF/UNIWIN/DW Bank/DW-Non-Bank/EG）是报表专属命名映射，不在原始底表任何字段里，需要单独维护映射表

## 八、L3-PDA-07 S2_业务端视角规则明细（对应 S2_业务端视角_反推标准_v0.1.md，12/20子板块已核验或高置信度推算）

见 [S2_业务端视角_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S2_业务端视角_反推标准_v0.1.md) 完整表格。关键结论（S2专属，跟S1不完全一样）：
- "同行经代"分组 = `segment_code IN ('同行经代','MGA业务')`——MGA在segment层面折进同行经代，不是永明经代，也不同于S1"业务大类"用的MGA→经代业务规则，三条规则各自独立
- "流失类" = 取消投保/退保/搁置受保（不含拒保），跟S1"流失"口径一致，多个独立案例交叉验证过
- KEY ACCOUNT排名/同行业绩分析里，同一账户若同时有MGA和非MGA业务，会拆成"账户名"和"账户名(MGA)"两个独立行
- fact_target的BRK/SLBRK映射方向本次金额级验证清楚：BRK=同行经代，SLBRK=永明经代

## 九、L3-PDA-08 S3_执行管理端规则明细（对应 S3_执行管理端_反推标准_v0.1.md，6/20子板块已核验）

见 [S3_执行管理端_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S3_执行管理端_反推标准_v0.1.md) 完整表格。关键结论：
- "周"定义 = `dt.strftime('%YW%U')`（周日起始%U惯例周），是S3最重要的新发现，跟月度趋势的日期口径不是简单类比
- 漏斗四阶段(预约/签单/递交/批核)的过滤规则各不相同，跟S1/S2月度趋势的规则一脉相承（只是从月改周）
- 签批时效分档要排除TAT<0的异常记录（真实脏数据，不是清洗bug），但分档合计行仍是全量口径

## 十、L3-PDA-09 S4_产品端视角规则明细（对应 S4_产品端视角_反推标准_v0.1.md，5/5板块全部核验）

见 [S4_产品端视角_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S4_产品端视角_反推标准_v0.1.md) 完整表格。关键结论：
- A节排除状态范围（4种，含拒保）跟B-E节（3种，不含拒保）不同，是S4内部两套口径并存，不能混用
- carrier_code → 保险公司简称有一套报表专属映射（含6家写死的零业务参考名单），模式上跟S1-H节"牌照"映射一致
- 产品TOP20（B/C节）的联合主键需要把SQ_rate标准化成统一百分比字符串再分组，SQ_rate缺失（约80%）的记录整体不参与这个排名

## 十一、L3-PDA-10 S5_财务端视角规则明细（对应 S5_财务端视角_反推标准_v0.1.md，核心5个板块100%核验）

见 [S5_财务端视角_反推标准_v0.1.md](../01_初始化项目_Initialize_Project/S5_财务端视角_反推标准_v0.1.md) 完整表格。关键结论：
- 基础过滤口径统一简单：`status=生效 AND issue_year=2026`，没有S1-S4常见的"未批核/待签"维度
- D/E节分档边界直接复用report_enricher的常量，不用重新定义
- G/H节"牌照"简称是issuing_entity前4字截断（跟S1-H/S4-A的查表映射模式不同，是S5独有的简单规则）
- TOP20排名遇到金额并列时按sign_date升序排序（tie-breaker），不是随机顺序

## 十二、下一批预告（不在本次范围内，先记录）

- S2的S/T板块（partner_code维度）+ S3的E/F（未批核待签透视表）/J-O（同行/银行周度趋势）+ S6-S7、S9 共 4 张专题视角表
- PPT生成：Jasper提供的`raw_data/业绩报表PPT/`是报表→PPT这一段的参考代码（35+张原生图表 + "So What"叙述洞察文字 + 达标节奏预测逻辑），架构是硬编码补丁链，不适合直接复用，只作为核对数字的参考材料
- 前端呈现层：Jasper的诉求是最终以前端系统呈现、可手动选择，完整度/准确度要跟现有PPT一致——技术路线上可以复用现有看板的单文件HTML+Chart.js方案，但内容深度要补齐到9-sheet+35图表+叙述洞察的水平

## 十三、输出物

- [x] 流程图 + 每个 L3 的输入输出表（本文档）
- [x] 清洗规则明细表（可直接转成测试断言）
- [x] L3-PDA-05 衍生字段规则明细
- [x] L3-PDA-06 S1总览仪表盘规则明细
- [x] L3-PDA-07 S2业务端视角规则明细
- [x] L3-PDA-08 S3执行管理端规则明细
- [x] L3-PDA-09 S4产品端视角规则明细
- [x] L3-PDA-10 S5财务端视角规则明细
- [ ] 进入 SOP 第3步：配置编写

---

> 定稿时间：2026-07-31
