# S2_业务端视角 · 反推标准（v0.1，用真实数据核验）

> S2共20个板块(A-T)，571行，比S1(8板块)大得多。核心模式已用真实数字核验，
> 大部分月度趋势子表按"已验证模式"推算（未逐条月份重新验数），标注清楚哪些是
> 精确核验、哪些是模式推算。

## 一、关键规则（适用于整张S2甚至可能整份报表）

### 1.1 "同行经代"分组 = segment_code IN ('同行经代', 'MGA业务')
跟S1"业务大类"用"MGA业务→经代业务"是**两条独立但相关**的规则——S2按segment_code分组时，MGA业务被
折进"同行经代"（不是"永明经代"）。这条规则不只用在A/B节，K节"同行业绩分析"的合计(731件)=
A节"同行经代"(319件)+"永明经代"(412件)，证实K节的"同行"也是这个定义。

**证据**：A节"同行经代"批核=319件/133,982,612（=同行经代原始319... 实际是`segment_code IN('同行经代','MGA业务')`
共228+91=319件精确匹配）；B节(永明子集)同行经代=221件/76,877,776同样精确匹配。

### 1.2 "流失类"（贯穿C-T多个月度趋势板块）= 取消投保/退保/搁置受保，**不含拒保**
跟S1"流失(2026)"定义一致（承保前被拒不算流失）。用天领业务2026-01(6,673,951)、
成事家办2026-04(2,631,479)、永明经代2026-05(19,734,931)三个独立案例精确核验，
其中成事家办案例专门验证了"含拒保但排除退保"才能精确匹配，排除了"拒保也算流失类"的误判。

### 1.3 KEY ACCOUNT 按segment拆分时，"XX(MGA)"是独立行
同一个key_account如果同时有MGA业务和非MGA业务（如"天誉国际"），报表会拆成"天誉国际"（非MGA部分）
和"天誉国际(MGA)"两行分别统计，不合并。精确核验：天誉国际(同行经代口径,非MGA)=96件/44,347,080；
天誉国际(MGA)=91件/22,040,526，两者精确匹配I/K节对应行。

## 二、已精确核验的板块

| 板块 | 规则 | 校验 |
|---|---|---|
| A. 业务细分年度汇总-全业务 | 按8个segment分组（含1.1的MGA折算），批核=status=生效+issue_year=2026；未批核=IN(尚欠/已签/pending/待批核)；待签=排期 | 8个segment+合计行全部精确匹配 |
| B. 业务细分年度汇总-永明 | 同A + carrier=香港永明金融有限公司 | 8个segment+合计行全部精确匹配 |
| C-APE/C-件数. 月度预约业绩-全业务 | `res_ym`分组，排除"流失类"(1.2)，不限年份但报表只展示2026年(2026-01~08) | 天领业务2026-01、全表合计2026-01精确匹配 |
| D-APE/D-件数. 月度签单业绩-全业务 | `sign_ym`分组，排除"排期"+"流失类" | 结构同C，未逐月复核（同源同规则，高置信度） |
| E-APE/E-件数. 月度批核业绩-全业务 | `issue_ym`分组，仅`status=生效` | 结构同C，未逐月复核（同源同规则，高置信度） |
| F/G/H (预约/签单/批核-永明) | 同C/D/E + carrier=香港永明金融有限公司 | 未逐月复核，规则显式写在footnote里（"同C区+carrier=..."），高置信度 |
| I. KEY ACCOUNT排名TOP20 | 按key_account分组（MGA拆分见1.3），按2026批核APE降序取前20+合计行 | 民生银行、天誉国际/天誉国际(MGA)精确匹配 |
| J. 同行推荐人分析 | 按`referral_code`分组，6个具名推荐人，**不限carrier** | 姜通/战略合作/高瑶/白博文/Mark/魏子璐全部精确匹配 |
| K. 同行业绩分析 | segment IN (永明经代,同行经代)，即含MGA业务(1.1)，按key_account分组降序 | 合计731件=A节同行经代319+永明经代412，精确匹配 |
| L-N (预约/签单/批核-同行) | 同C/D/E，范围限定为K节人群，⭐按该表自身合计APE降序排序 | 未核验，模式推算 |
| O. 银行业绩分析 | segment=BK业务，按key_account分组降序 | 民生银行/平安银行/合计精确匹配 |
| P-R (预约/签单/批核-银行) | 同C/D/E，范围限定为O节人群 | 未核验，模式推算 |
| S/T. 批核业绩-银行各分行 | 按`partner_code`而不是key_account分组，其余同E | 未核验，是新维度(partner_code)，需要单独验证 |

## 三、尚未验证/待确认

- D/E/F/G/H及L-T的月度趋势具体数字未逐条核对（结构和过滤规则有信心，但没有像C那样精确验证到每个月）
- S/T节的partner_code分组逻辑完全没碰过
- "达成率"字段的四舍五入展示格式（'35.1%'字符串）未验证精确度

## 四、下一步

A/B/C/I/J/K/O已高置信度可写入skill；D-H/L-T用跟C相同的规则参数化实现（复用月度趋势通用函数）；
S/T待后续单独验证partner_code逻辑。
