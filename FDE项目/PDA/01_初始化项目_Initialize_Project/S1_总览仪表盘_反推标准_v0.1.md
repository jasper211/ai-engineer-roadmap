# S1_总览仪表盘 · 反推标准（v0.1，用真实数据逐项核验）

> 用 `raw_data/业绩数据底表-20260724.xlsx`（清洗后，用 PDA Cleaner）+ `report_enricher`产出的"业务大类"字段
> 对照 `raw_data/业绩分析报表_0724.xlsx` 的 `S1_总览仪表盘` sheet 逐项核对。方法与S8一致：写公式→跑真实数据→比对
> 报表里的真实数字，不是凭空定规则。

## 一、已100%验证的规则

### A. 目标达成率
- 已达成APE(全业务) = 批核(2026)总APE = `status=生效 AND issue_year=2026` 求和 → 618,939,268.33 ✅
- 已达成APE(永明) = 同上 + `carrier_code=香港永明金融有限公司` → 522,699,342.4 ✅
- 目标APE来自 `fact_target`（见[目标APE数据源核实.md](目标APE数据源_fact_target_核实.md)）
- 达成率 = 已达成APE / 目标APE（简单除法）

### B. 顶部KPI汇总（5行全部核验，含件均APE/融资占比派生列）
| 指标 | 规则 | 校验 |
|---|---|---|
| 批核(2025) | `status=生效 AND issue_year=2025` | 1645件/616,841,098.59 ✅ |
| 批核(2026) | `status=生效 AND issue_year=2026` | 1205件/618,939,268.33 ✅ |
| 未批核(跨年) | `status IN (尚欠保费,已签单,pending,待批核)`，不分年 | 260件/123,714,140.47 ✅ |
| 待签(跨年) | `status=排期`，不分年 | 55件/20,033,148.4 ✅ |
| 流失(2026) | `status IN (取消投保,退保,搁置受保) AND sign_year=2026`——**明确排除"拒保"**（拒保=承保前被拒，不算"流失"） | 97件/34,167,370.61 ✅ |

### C/D/E. 月度业绩走势（按预约/签单/批核时间，各13个月：2025-01~2026-07 已见数据，报表标注到2026-08）
| 维度 | 日期字段 | 状态过滤 | 校验 |
|---|---|---|---|
| C 按预约 | `res_date`取年月 | 报表footnote写"排除取消预约"，但**当前数据没有"取消预约"这个status值，等同不过滤** | 2025-01: 69件/22,560,612.856 ✅ |
| D 按签单 | `sign_date`取年月 | 排除 `status=排期`（"取消预约"同上不存在，等同不过滤） | 2025-01: 70件/25,095,612.856 ✅ |
| E 按批核 | `issue_date`取年月 | 仅 `status=生效` | 2025-01: 120件/41,402,627.944 ✅ |

环比/同比增长% = 简单的月度APE环比/同比百分比变化，未单独验证但结构显然（跟件数/APE同源）。

### F. 保单状态分布-2026（10行，仅9行有真实规则，1行恒为0）
| 行 | 规则 | 校验 |
|---|---|---|
| A.批核(2026) | `status=生效 AND issue_year=2026` | 1205件 ✅ |
| B-F（排期/已签单/待批核/pending/尚欠保费） | `status=X`，**不分年份，全量** | 全部✅精确匹配 |
| G.失效 | **恒为0**——当前数据没有"失效"这个status值，这一行是占位，不对应"拒保"或任何真实数据 | 0件 ✅（原样） |
| H/I/J（退保/取消投保/搁置受保） | `status=X AND sign_year=2026` | 全部✅精确匹配 |
| 合计 | 上述9行(不含G)求和 | 1617件 ✅——**"拒保"这个status(2条记录)完全没被计入这张表的任何一行**，是报表本身的已知缺口，不是我这边的bug |

### G. 业务类型维度-2026（⚠️关键：必须用"业务大类"衍生字段，不是原始business_category）
| 业务类型 | 规则 | 校验 |
|---|---|---|
| 代理人业务/经代业务/KA业务 | 按 `report_enricher`产出的"业务大类"字段分组（MGA业务→经代业务重分类），批核/未批核/待签用B节同款规则 | 三行全部✅精确匹配（经代业务原始字段分组会差91件/2200万，必须用业务大类） |

**这条结论可能适用于S1-S9所有涉及business_category分组的表**——凡是按"业务类型"分组的，都要用"业务大类"（含MGA重分类），不能直接用raw的business_category。后续每张表都要验证这一点，不能想当然套用。

### H. 永明业绩汇报数据（全部验证通过）

限定 `carrier_code=香港永明金融有限公司` 全表范围。"牌照"这一列不是原始字段，是这张表专属的命名映射：

| 牌照代码 | 对应真实值 |
|---|---|
| JF | issuing_entity=九富保险服务有限公司 |
| UNIWIN | issuing_entity=众和恒富理财集团有限公司 |
| DW Bank | issuing_entity=怡泰财富管理有限公司 且 market_segment=银行网点 |
| DW-Non-Bank | issuing_entity=怡泰财富管理有限公司 且 market_segment≠银行网点 |
| EG | key_account=EGA（当前0条记录） |

| 列 | 规则 | 校验 |
|---|---|---|
| 月度列(如2026-01) | `status=生效 AND issue_ym=该月`，**取APE不是保费**（JF/UNIWIN两行premium恰好等于APE导致一度误判，靠怡泰行的premium≠APE才分辨清楚） | JF 2026-01: 45,864,039.84 ✅；DW Bank 2026-01: 18,259,800 ✅；DW-Non-Bank 2026-01: 401,809.2 ✅ |
| 未批核 | `status IN (尚欠保费,已签单,pending,待批核)`，不分年，同实体范围 | JF未批核: 23,077,485.2 ✅ |
| 本月已递交 | `submit_date`年月 = 报表导出月(2026-07)，不限status | JF本月已递交: 14,526,341.81 ✅ |
| Sub Total | = JF+UNIWIN+DW-Non-Bank+EG（**不含DW Bank**） | 2026-01: 67,920,745.32 ✅ |

## 二、下一步

**S1整张表（A-H共8个板块）已100%验证完毕**，可以写成skill。下一步转向 S2-S7、S9。
