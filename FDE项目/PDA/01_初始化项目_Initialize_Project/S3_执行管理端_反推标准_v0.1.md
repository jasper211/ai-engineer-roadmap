# S3_执行管理端 · 反推标准（v0.1，用真实数据核验）

> S3共417行，主要是周度趋势（跟S1/S2的月度趋势对应），外加签批时效分析。核心板块(A/B/C/D/G/H)
> 已用真实数字核验，E/F(未批核待签分布透视表)和J-O(同行/银行周度趋势)本版本未实现。

## 一、关键规则

### 1.1 "周"的精确定义：pandas `dt.strftime('%YW%U')`（周日起始，US惯例）
这是S3最重要的新发现——报表里"2026W01"这种周标签，不是ISO 8601周（周一起始），
而是**周日起始的%U惯例周**。用天领业务预约W01(16件/6,517,000)、W02(36件)、W03(35件)
三个连续周精确核验，ISO周和"从年初简单按7天分段"两种猜测都对不上，只有`%U`精确匹配。

### 1.2 A-APE漏斗四个阶段的过滤规则各不相同
| 阶段 | 日期字段 | 过滤 |
|---|---|---|
| 预约 | res_date | 排除流失类(取消投保/退保/搁置受保) |
| 签单 | sign_date | 排除流失类+排期 |
| 递交 | submit_date | 排除流失类 |
| 批核 | issue_date | 仅status=生效 |

跟S1/S2的月度趋势规则完全一致，只是从月改成周。

### 1.3 TAT(签批时效)分档要排除负数异常值
发现一条真实脏数据：policy PI108585，sign_date(2026-06-30)晚于issue_date(2026-06-28)，
TAT=-2天。H节"≤7天"分档**不把这条算进任何分档**（126件而不是127件），但H节"合计"行
仍然是全量1205件（直接复用G节总数，不是6个分档相加）。这是真实数据异常，不是清洗bug——
底表允许批核日早于签单日的情况存在，报表选择把它排除在时效分档之外而不是归类到"≤7天"。

## 二、已精确核验的板块

| 板块 | 规则 | 校验 |
|---|---|---|
| A-APE/A-件数. 阶段周追踪漏斗 | 4阶段×周(见1.2)，只看2026年 | 预约/签单/递交/批核 W01精确匹配 |
| B-APE/B-件数. 周度预约业绩 | `res_yw`，排除流失类（footnote列了5个状态但当前数据里"失效""取消预约"不存在，等同只排3个） | 结构同A-APE预约行 |
| C-APE/C-件数. 周度签单业绩 | `sign_yw`，排除流失类+排期 | 结构同A-APE签单行 |
| D-APE/D-件数. 周度批核业绩 | `issue_yw`，仅status=生效 | 结构同A-APE批核行 |
| G. 签批时效分析-2026批核 | 按segment分组（同S2的MGA折算规则），TAT=issue_date-sign_date，含平均/中位/P90/最大/SLA达标率(≤60天占比) | 天领业务全部7个指标精确匹配 |
| H. 签批时效分档-2026批核 | TAT分6档(≤7/8-14/15-30/31-60/61-90/>90天)，**排除TAT<0的异常记录**，合计行=全量1205件(不是6档相加) | 6个分档+合计精确匹配 |

## 三、未实现（本版本不做，留待下一批）

- E-APE/E-件数/F-APE/F-件数：未批核与待签分布透视表（行=KEY ACCOUNT，列=有数据的sign_ym，值=APE/件数，按is_pf拆常规/融资），结构复杂且是钻取型展示，优先级低于核心KPI
- J-O：同行/银行的周度预约/签单/批核趋势——结构跟B/C/D完全一致，只是限定人群（同行=S2的PEER_SEGMENTS，银行=BK业务），高置信度但未逐条验证

## 四、下一步

A/B/C/D/G/H可直接写入skill。E/F/J-O视优先级决定是否补齐。
