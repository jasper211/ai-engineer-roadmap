# VNW · 05 血缘覆盖脉络：剩余86张主表分析

> **本文档回答"7族18张之外，剩下86张的血缘到底连好没"。** 基于 data_lineage.json 三类边（view/FK/pipeline_sibling）+ business_data_bridge 语义层交叉核对，只读原文件。

## 一、结论（一句话）

> 86张主表里：**47张已连入血缘**（血缘主干），**23张有真实业务数据但血缘图中孤立**（不是僵尸，是证据没覆盖），**16张0行空表**。23张孤立表里，**11张可被业务语义层直接接管**（无需造假补血缘边），其余为快照/映射/配置维度类，补版本指针或待 batch 覆盖。

## 二、主线：血缘已连入的 47 张

三大业务主线当前血缘是通的（视图依赖+FK+流水线）：

```
客户/保单主线：fact_sales_activity → fact_policy → v_policy_current_state / v_fact_sales_activity
佣金费率主线：fact_commission_rate → agg_source_commission_wide → agg_market_commission_tier_rate
人员活动主线：dim_person / bridge_person_* → v_person_activity → v_fact_policy_person
```

三类边合计 129 条去重连接（view 41 + FK 21 + pipeline 76 原始计数）。

## 三、问题：23张"孤立但有数据"表 —— 分四类处理

### A. 可被业务语义层直接接管（11张）✅ 优先级最高
> 血缘无覆盖，但 `business_data_bridge` 已确认到具体 L4。**直接从 bridge 接入"业务语义连接"即可，不造血缘边。**

| 表 | 行数 | 已确认 L4 |
|----|-----|----------|
| fin_sandbox.fact_receivable | 47142 | L4-COM-10 |
| fin_sandbox.match_receipt_receivable | 1154 | L4-COM-09/11 |
| comm_sandbox.config_table_type_product_scope | 251 | L4-COM-02 |
| public.dim_employee | 209 | HRA/HRM 6个L4 |
| public.partner_tier_rules | 179 | L4-COM-12 |
| comm_sandbox.config_table_type_tier_pricing_rules | 109 | L4-COM-02 |
| public.fact_target | 62 | COM-04/HRA-02/HRM-01 |
| fin_sandbox.config_carrier_settlement_schedule_rules | 36 | L4-COM-18 |
| public.config_product_commission_formula | 32 | L4-COM-01 |
| comm_sandbox.config_table_type_def | 20 | L4-COM-01 |
| comm_sandbox.market_table_header_state | 20 | L4-COM-01/18 |

### B. 快照/版本类（1张）—— 补版本指针即可
| 表 | 行数 | 说明 |
|----|-----|------|
| public.sales_renewal_due_snapshot | 29640 | 续保到期快照，判为僵尸是误判，标注为主表的快照版本 |

### C. 映射类（2张）—— 排查引用后基本可接管
| 表 | 行数 | 说明 |
|----|-----|------|
| comm_sandbox.map_commission_plan_scheme | 349 | 方案映射，对应 L4-COM-01 语义 |
| public.map_name_entity_type | 67 | 名称-实体映射，工具表性质 |

### D. 真断点（9张）—— 配置/维度类，待 batch 覆盖（如实标"语义层未排到"，不作僵尸）
| 表 | 行数 | 类型 |
|----|-----|------|
| public.dim_date | 1826 | 日期维度 |
| public.dim_product_feature_value | 1014 | 产品特征维度 |
| public.config_product_exclusion_range | 228 | 配置 |
| public.product_risk_override | 92 | 规则 |
| public.dim_binder_agreement | 32 | 维度/转介协议 |
| comm_sandbox.config_product_pricing_group | 31 | 配置 |
| public.config_product_feature_type | 25 | 配置字典 |
| public.config_commission_table_type | 23 | 配置 |
| public.config_identity_source_map | 22 | 配置 |

## 四、关键判断

- **绝大部分"孤立但有数据"的表不是僵尸**：它们要么已被语义层认领到 L4（11张A），要么是版本/快照（B）、映射（C），只剩 9 张配置/维度类（D）是真待 batch 排。
- **血缘图的"孤儿/僵尸"标签，主要来自三类客观证据覆盖不到第四类"业务语义连接"**——这是修正的核心，不是补造假边，而是把 bridge 的 evidence_type 接进来。
- 唯一需优化的是 D 类 9 张：目前被标 `suspected_zombie`，应改为"语义层未排到"，避免误读为"与业务无关"。

## 五、血缘覆盖现状小结（86张）

| 类别 | 张数 |
|------|-----|
| 已连入血缘（主干，有数据） | 47 |
| 孤立但可语义层接管（A类） | 11 |
| 快照/版本（B类） | 1 |
| 映射（C类） | 2 |
| 真断点-配置维度（D类） | 9 |
| 0行空表（未启用/未populate） | 16 |

## 六、当前状态
- [x] 05 血缘覆盖脉络分析（本文档）
- [ ] 待反馈修正方案（把 B/C/D 类从僵尸解放，A 接 bridge）
- [ ] 06 待回写方案
