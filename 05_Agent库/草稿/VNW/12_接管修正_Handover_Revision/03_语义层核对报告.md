# VNW · 03 语义层核对报告：血缘缺口 × business_data_bridge 交叉核对

> **本报告回答 Mark "你先核对"的要求。核对方法：把 02盘点里"无血缘但有数据"的33张表，与 `business_data_bridge.py` 已人工核实的 `L4_BUSINESS_TABLE_MAP` 做交集，逐张判定"语义层是否已确认到 L4"。全部基于只读原项目数据，不改任何原文件。**

## 一、核对结论（一句话）

> **104 张表里，33 张"血缘无覆盖但有真实业务数据"。其中 14 张业务语义层（bridge）早已确认到具体 L4，19 张语义层也完全没覆盖。** 这证明两件事：
> 1. 血缘图"孤儿/疑似僵尸"的误判是真实且规模不小的（14 张被语义层认领却血缘判成无关联）；
> 2. 血缘图的缺口，恰好可以靠已存在的 `business_data_bridge` 语义层补上一部分（14张），剩下19张是语义层本身也没排到/没覆盖的（其中多为归档/版本/主数据，语义层未来补齐）。

## 二、逐张核对明细（33张）

### A. 语义层已确认到 L4（14 张）—— 血缘误判最清晰的一批

> 这 14 张血缘图 `has_lineage=false`（部分还标 `suspected_zombie`），但 `business_data_bridge` 早已把它们匹配到具体 L4 + evidence_type。**第四类"业务语义连接"可直接从 bridge 取，无需造血缘边。**

| 表 | 行数 | 已确认 L4 | evidence | 血缘图标记 |
|----|----|----------|----------|-----------|
| fin_sandbox.fact_receivable（应收主表） | 47142 | L4-COM-10 | output | 无血缘，none |
| comm_sandbox.match_receipt_receivable | 1154 | L4-COM-09/11 | output | none |
| public.sync_history | 305 | L4-COM-15/16 | audit | none |
| comm_sandbox.config_table_type_product_scope | 251 | L4-COM-02 | rule | none |
| public.dim_employee | 209 | HRA/HARM 6个L4 | output | none |
| public.partner_tier_rules | 179 | L4-COM-12 | rule | none |
| comm_sandbox.dim_comm_scheme | 173 | L4-COM-01 | rule | none |
| comm_sandbox.config_table_type_tier_pricing_rules | 109 | L4-COM-02 | rule | none |
| public.fact_target | 62 | COM-04/HRA-02/HRM-01 | output | none |
| comm_sandbox.config_partner_routing | 41 | L4-COM-12 | rule | none |
| fin_sandbox.config_carrier_settlement_schedule_rules | 36 | L4-COM-18 | rule | none |
| public.config_product_commission_formula | 32 | L4-COM-01 | rule | none |
| comm_sandbox.config_table_type_def | 20 | L4-COM-01 | rule | none |
| comm_sandbox.market_table_header_state | 20 | L4-COM-01/18 | audit/workflow | none |

> ⚠ 特别注意：`fact_receivable`（4.7万行）、`dim_employee` 这些"核心业务/主数据表"，血缘图因无SQL/FK而判 `has_lineage=false`，但 bridge 早已确认到 L4。**这就是你质疑的"血缘没做完"最直接证据——不是没血缘，是血缘的证据类型没覆盖。

### B. 语义层也未覆盖（19 张）—— 真实待补缺口

> 这 19 张 bridge 也没匹配到 L4。分两类：
> ① **归档/版本/快照类**（数据庞大，是主表的历史，语义应作为"版本指针"，bridge 未显式登记）；
> ② **维度/配置/主数据类**（多为主数据被代码字段引用、非FK，bridge 尚未排到）。

| 表 | 行数 | 归类 | 血缘图标记 |
|----|----|------|-----------|
| comm_sandbox.fact_commission_tier_rate_history_long | 337610 | 归档/版本 | ⚠suspected |
| public.fact_insurance_plan_lines_history | 41050 | 归档/版本 | ⚠suspected |
| public.sales_renewal_due_snapshot | 29640 | 快照 | ⚠suspected |
| public.dim_date | 1826 | 维度 | ⚠suspected |
| public.dim_product_feature_value | 1014 | 维度 | ⚠suspected |
| public.fact_insurance_plan_header_history | 416 | 归档/版本 | ⚠suspected |
| comm_sandbox.map_commission_plan_scheme | 349 | 映射 | ⚠suspected |
| public.config_product_exclusion_range | 228 | 配置 | ⚠suspected |
| public.product_risk_override | 92 | 配置/规则 | ⚠suspected |
| public.config_partner_routing | 67 | 配置 | ⚠suspected |
| public.map_name_entity_type | 67 | 映射 | ⚠suspected |
| public.dim_binder_agreement | 32 | 维度 | ⚠suspected |
| comm_sandbox.config_product_pricing_group | 31 | 配置 | ⚠suspected |
| public.agg_sales_base_etl_scope | 30 | ETL参数 | ⚠suspected |
| public.config_product_feature_type | 25 | 配置字典 | ⚠suspected |
| public.config_commission_table_type | 23 | 配置 | ⚠suspected |
| public.config_identity_source_map | 22 | 配置 | ⚠suspected |
| public.config_quarter | 4 | 配置 | ⚠suspected |
| fin_sandbox.fin_sync_history | 1 | 日志 | ⚠suspected |

## 三、核对对修正方案的含义

| 层 | 数量 | 修正归属 |
|----|------|---------|
| 语义层已确认（A） | 14 | **直接接入 bridge 的 evidence_type 作"业务语义连接"**，不造血缘边，前端并排展示 |
| 语义层未覆盖（B） | 19 | 其中归档/版本/快照类（5张）补"版本指针"连接；维度/配置类（其余）由 bridge 未来 batch 推进覆盖，或如实标"语义层未排到"——不作为僵尸 |
| 0行未启用 | 15 | 维持 `never_activated`，不动 |

## 四、当前状态

- [x] 03 语义层核对报告（本文档，只读交叉核对）
- [ ] 04 待回写方案（Mark 验证本报告后产出）
- [ ] 05 验证记录

> ⚠ 一项需 Mark 确认的细节：`config_quarter`(comm_sandbox版) 在 bridge 里被 L4-FBA-01 引用为 rule 证据，但本核对查的是 `public.config_quarter`（另一张同结构表，未在 bridge 里）——**同名两表不同 schema，语义层覆盖的是 comm_sandbox 版，public 版仍待补**。这与 `config_partner_routing` 两 schema 重复的现象一致，是下一轮 bridge 该处理的重复项。
