# L3 模型复核队列

> 日期：2026-07-31  
> 原则：结构校验通过不等于业务确认；模型草稿不得标记为人工复核。

## 0. 本轮执行结果

| L3 | 执行结果 | 当前可复核性 |
|---|---|---|
| HRA | 技术质检通过，7个L4、13个任务、2项决策 | 可进入业务复核 |
| IRI | 技术质检通过，7个L4、12个任务、1项决策 | 可进入业务复核 |
| HRM | 任务模块修复为9个L4全覆盖、19个任务；L4-HRM-05完成定向刷新 | 可带“控制链待补”进入业务复核 |
| IBRD | 任务模块修复完成，5个缺口L4定向刷新，已证实解决的旧缺口提示已清理 | 可带“控制链待补”进入业务复核 |
| SPE | 首次统一模型运行完成，3个L4、7个任务、1项决策 | 可带三项明确缺口进入业务复核 |

五份结果仍统一为`MODEL_DRAFT`，没有自动升级为`REVIEWED`。

## 1. 当前可信基线

| L3 | 状态 | 使用方式 | 已知限制 |
|---|---|---|---|
| L3-COM | REVIEWED | 统一模板金标准 | 早期复核包缺当前输入快照Hash |
| L3-BAM | REVIEWED | 跨业务域人工复核参考 | 仍声明蓝图RACI、业务价值量化和银行准入标准缺口 |

## 2. 可提交 Jasper 业务复核

### L3-HRA

- 7个L4、13个任务、2项负责人决策；
- L4分析和任务覆盖完整；
- 优先级仍全部为`unclassified`，没有伪造逐L4业务价值坐标；
- 当前为`MODEL_DRAFT`。

复核重点：

1. 13个任务是否对应真实日常工作，有无漏项、错拆或过度合并；
2. 人效数据ETL、指标对标、成本分析、流失预警等任务的AI分工是否合理；
3. 数据质量、合规、报告发布和干预决策的人工控制边界；
4. “人力诊断报告自动化”和“人效数据基础自动化”是否适合作为首批试点；
5. 是否需要补充真实频率、人工耗时、验收指标或现状基线。

### L3-IRI

- 7个L4、12个任务、1项负责人决策；
- L4分析和任务覆盖完整；
- Gate E为`CONDITIONAL`，应保留证据缺口提示；
- 优先级仍全部为`unclassified`；
- 当前为`MODEL_DRAFT`。

复核重点：

1. 权限清单、资源对接、联调、权限配置、测试和通知任务是否符合真实执行顺序；
2. 权限谈判、异常协调、签字、回滚等人工责任是否正确；
3. 权限配置和生效测试是否具备只读或影子模式试点条件；
4. “自动化权限配置与测试流程”是否应为首批试点；
5. 蓝图岗位描述是否需要进一步归并为正式岗位族和部门。

## 3. 已完成返工、带缺口提交业务复核

### L3-HRM

- 9个L4、27个任务；
- L4-HRM-05仍存在字段不完整；
- 4条控制链因缺少L4定位被拒绝；
- 当前只有1项负责人决策，集中在资金与法律控制门。

已完成：

1. 任务重新拆分为19项并覆盖全部9个L4；
2. L4-HRM-05完成定向刷新；
3. 原L4分析、任务修复和定向刷新均保留备份与审计记录；
4. 控制链缺少L4定位的提示继续保留，不用模型补造HR规则、金额阈值或审批人。

### L3-IBRD

- 8个L4、12个任务；
- 5个L4存在明确字段或输入不足；
- 5条控制链因缺少L4定位被拒绝；
- 当前只有1项负责人决策。

已完成：

1. 任务模块修复为8个L4全覆盖、12个任务、2项负责人决策；
2. 对01/02/03/06/08五个L4完成定向刷新；
3. 已补字段均通过当前事实包证据校验；
4. 控制链缺少L4定位的真实缺口继续保留。

### L3-SPE

- 3个L4、3条事实抽取任务；
- `PENDING_MODEL`，没有正式模型运行记录；
- 逐L4分析、任务AI分工、优先级和负责人决策均未生成。

已完成：

1. 使用当前统一提示词和事实快照完成首次运行；
2. 生成3个L4、7个任务和1项负责人决策；
3. 输出保持`MODEL_DRAFT`并记录输入快照Hash；
4. 保留Skill证据待佐证、系统触发阈值不足和控制链定位不足三类缺口。

## 4. 外部模型调用边界

HRM、IBRD修复和SPE首跑会把各自冻结的单L3事实包发送至VNW当前配置的外部模型
服务。执行前需要数据所有者明确授权。未授权时只保留本地`PREPARED`运行包，不发送、
不发布、不覆盖现有分析。
