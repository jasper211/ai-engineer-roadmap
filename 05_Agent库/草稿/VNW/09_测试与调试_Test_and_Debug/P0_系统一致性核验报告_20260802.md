# VNW P0 系统一致性核验报告

> 执行日期：2026-08-02（America/Chicago）  
> 范围：本地正式 React 前端、全量 L3 快照、已有分析包、自动测试和页面抽检。  
> 边界：本轮未调用大模型生成新结论，未更新公开版，未回写数据库或知识库。

## 1. 执行结果

1. 以只读方式连接 PostgreSQL，成功重建 67 个 L3 模型快照并同步到正式前端。
2. 自动化测试 43/43 通过；新增“源头新增 L4 时旧分析安全降级”回归测试。
3. React/TypeScript/Vite 正式构建成功。仅有现有 JS chunk 超过 500 kB 的性能提示，不阻断 P0。
4. 实际页面抽检 COM、HRA、HRM、IBRD、SPE，无浏览器错误或警告。
5. 修正分析状态文案：`REVIEWED` 显示“人工复核基线”，不再与 `MODEL_DRAFT` 共用“模型分析草稿”的视觉语言。

## 2. 全量快照现状

| 指标 | 数量 |
|---|---:|
| L3 总数 | 67 |
| 已人工复核基线 | 2 |
| 模型草稿 | 7 |
| 待运行统一模型 | 58 |
| 可生成完整模型 | 4 |
| 可生成带缺口模型 | 56 |
| 输入未达准入 | 7 |

生产状态：`ANALYSIS_CURRENT=4`、`ANALYSIS_INPUT_CHANGED=3`、
`BLOCKED_INPUT=7`、`READY_TO_RUN=51`、`REVIEWED_BASELINE=2`。

## 3. 五个样本核验

| L3 | L4 | 任务 | 页面结果 | 当前处理 |
|---|---:|---:|---|---|
| COM | 18 | 49 | A/E/C/B/D/F 统一结构正常 | 保留人工复核基线 |
| HRA | 7 | 13 | 统一结构正常 | 进入业务复核 |
| HRM | 9 | 19 | 统一结构正常；明示控制链缺口 | 进入业务复核，不隐藏缺口 |
| IBRD | 9 | 0（当前页） | 正确进入待补页，不展示旧8-L4分析 | 先补 L4-IBRD-09 的 D1–D6，再重跑分析 |
| SPE | 3 | 7 | 统一结构正常；明示 Skill、阈值和控制链缺口 | 进入业务复核 |

## 4. IBRD 源头变化

权威库当前新增/恢复了 `L4-IBRD-09 合作复盘与决策（待注册）`：

- 交付物：《合作复盘报告》+《合作决策记录》（继续/调整/终止）；
- AI 介入类型：Human；
- D1–D6：六项均为空；
- 旧 IBRD 分析包只覆盖 L4-IBRD-01–08。

因此 Gate M 为 `BLOCKED`，这是数据变化后的正确结果。旧8-L4分析包仍保留，
但不再作为当前页面结论。快照中记录 `stale_analysis.status=ANALYSIS_INPUT_CHANGED`
及具体缺失编码，便于后续精准补数。

## 5. P0 结论与后续入口

P0 的“重建—测试—构建—页面抽检”已完成。下一步进入 P1 业务复核：

1. 先复核 HRA、HRM、IRI、SPE；
2. IBRD 不进入草稿复核，先由数据负责人补齐 L4-IBRD-09 的 D1–D6 和任务依据；
3. 待当前草稿反馈稳定后，再运行下一小批。根据本次最新快照，系统推荐已更新为 `L3-CDS`、`L3-SPD`、`L3-SDS`；`L3-CDA-SRA` 因蓝图未可解析退出下一批。
