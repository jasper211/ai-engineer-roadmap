# VNW 流程分析框架总蓝图 v0.3

> 状态：草稿，讨论中
> 变更记录：v0.2 借鉴了 HKIA 项目的方法论（命题分级、Fact/Evidence/Claim、"危险错觉"等），内容是对的，但写得太绕，看不懂。v0.3 换回朴素写法，按最初那份"战略/流程模型/分析模型/数据底座/Agent编排/输出治理"六层清单的结构写，把之前做出来的真实内容（域数据、根因分析、Agent现状对照）保留，去掉专业术语外壳。
> 需要查证据细节的，看 [01_PhaseA_HR域命题链示范_v0.1.md](01_PhaseA_HR域命题链示范_v0.1.md)（这份保留原样，是给要核对数据出处时查的底稿，平时不用看）。

---

## 1. 战略与规划层

### 1.1 愿景定义

**北极星指标：任务级"人力-Token置换经济性"**

针对每一类具体任务，量化两件事：
1. 这个任务能不能被AI替代——VNW现在已经有雏形（Skill封装可行性A/B/C/F + Human/Aug/Hybrid/Auto分级），这块能做；
2. 替代之后成本划不划算——现在花多少人力做这件事，AI做要花多少Token，两者一对比。

目的是让公司知道：哪个部门/哪条流程该加人，哪个该转投AI。

**现状**：第1点能做，第2点做不了——VNW现在完全没有"人力成本基线"（这件事实际花多少人时/多少钱）和"Token成本"（AI做同样的事要花多少Token）这两类数据，需要新建数据采集能力，不是现有数据能拼出来的。

**AI原生成熟度目标：阶段一——先把数字化可视化做扎实**

接下来一年不追求Agent自主决策，先把74个L3全部看清楚、结构化完整。这跟VNW现在做的事（一个个流程建模）方向一致，是做深做透，不是换方向。

### 1.2 现状诊断 Baseline

- 内部现状：能做——哪些流程卡住、卡在哪、共性原因是什么，这块已经做出真实结论（见3.1）。
- 与行业标杆的Gap分析：做不了——没有任何外部对标数据，VNW只有内部数据。

### 1.3 路线图

**阶段一的完成标准（已定）**：每个L3都要按照完整的分析框架做完深度分析（不是只跑通Gate M/E/A这类准入判断），并且除了前端展示，还要能产出一份正式的分析报告。

**这个标准比"全部74个L3跑完流水线"要求更高，缺口比原来想的大，拆成两块**：

1. **"每L3深度分析"这块**：现在做过的是抽样案例（HR域3个L3）和全域聚合（根因分析），不是"74个L3每个都做深"。要达标，得把3.1/3.2这类分析方法，逐个应用到74个L3上——3.1（根因诊断）方法上是通的，可以推广；3.2（AI原生度评估）现在连HR域这个案例都做不出来（缺"实际怎么干活"的信息），74个全做更做不到，这是这条路线图里最大的卡点。
2. **"能出报告"这块**：VNW现在完全没有报告产出能力，只有前端页面。这是一个新的输出物，需要新建（见6.1）。

**具体路线图（哪个月做什么）暂不排**——3.2缺数据、报告产出没建，这两个缺口不解决，路线图排了也是空话。建议先确认这两个缺口怎么补（3.2需要各部门提供真实工作方式信息；报告产出需要先定报告要回答什么问题、给谁看），有了解法再排时间表。

---

## 2. 流程模型层

### 2.1 流程全景图（价值链拆解——已有真实数据，之前没用上）

VNW已有的单L3链路：`L2能力 → L3目标 → L4交付物 → 具体能力/AI介入方式 → 价值节点`，目前只在"单个L3详情页"里用，没有"跨L3看全貌"的版本。

**价值链拆解**：VNW数据库里其实已经有真实的价值流（客户/合作伙伴旅程）分类，之前没注意到，比域划分更贴近"价值链"这个概念。5条真实价值流，49个L3有映射（每个L3还标了处于旅程第几阶段S1-S8）：

| 价值流 | L3数量 | 说明 |
|---|---|---|
| VS-2 机构合作伙伴旅程 | 19 | 最大的一条 |
| VS-1 保司资源投放旅程 | 9 | |
| VS-5 权益服务企业客户旅程 | 8 | |
| VS-3 KA上架旅程 | 7 | |
| VS-4 理财师发展旅程 | 6 | |

**25个L3没有价值流映射**，包括几个做得最深的旗舰流程（L3-COM佣金全链路、L3-HRA/HRM/SPE人力三兄弟）——这个覆盖缺口本身是个真实发现，说明"价值流"这条线的建设进度跟"域"这条线不同步，域覆盖率75%（56/74），价值流覆盖率只有66%（49/74），且两边缺的不是同一批L3。

### 2.2 流程域划分（已完成，真实数据）

VNW权威数据库里有19个真实业务域，74个L3流程分别挂在下面：

| 域 | L3数量 | 代表交付物 |
|---|---|---|
| EQ | 11 | 同行经代服务方案、初版权益规划方案 |
| INS | 7 | 保司合作可行性报告、已签合同+备案号 |
| SMPS | 7 | 市场洞察分析报告、价值主张文档 |
| FA | 6 | 理财师经营分析报告、招募计划与执行记录 |
| PARTNER | 6 | 合作伙伴尽调记录、联合运营需求诊断包 |
| KA | 5 | KA培训完成记录、KA评估定级记录 |
| PAY | 4 | 佣金制作产出包、理财师应派计算清单 |
| HR | 3 | 绩效方案、人效分析报告 |
| TREASURY | 2 | 银行账户全生命周期资产包、现金流管理报告包 |
| BSRV/CPM/CRR/IBRD/JOPD/NG/RCM/SSVA/URD/USV | 各1 | 结算核对台账/优化闭环执行包/续约DD重审记录/合作复盘决策记录/联合运营交付包/合同终稿/信用评估报告/服务对账单/合规风险评估报告/服务验证包 |

（原来这里有一版我自己猜的"5个业务集群"，已删除——2.1 的真实价值流数据是更权威的业务语义来源，不需要再猜。域和价值流是两个不同视角：域偏组织归属，价值流偏客户/合作伙伴旅程，两个都保留，不用互相替代。）

已知数据问题：3处域归属冲突（L3-IBRD/URD/CFM，同一个流程在数据库里被记到两个不同的域），至少18个流程查不到域——这两类先如实显示"待确认"，不瞎猜。

### 2.3 流程指标字典

- 已有：自动化率、Gate通过率这类，能从VNW数据库字段拼出来；
- 没有：周期时间、等待时间、吞吐量这些效率指标——VNW不记录耗时数据，这块是空的。

---

## 3. 分析模型层

### 3.1 流程诊断模型（已完成，真实数据）

对74个L3做了一次全量的"为什么卡住"的原因统计，结果：

| 排名 | 真实阻断原因 | 影响L3数 | 占比 |
|---|---|---|---|
| 1 | 存在熔断或无价值节点 | 44 | 59.5% |
| 2 | 价值节点-L4映射=0 | 19 | 25.7% |
| 3 | Gate M/E 汇总阻断标志 | 14 | 18.9% |
| 5 | D1-D6完整度不足 | 10 | 13.5% |
| 6 | 缺少可解析流程蓝图 | 4 | 5.4% |

结论：**卡住的头号原因是"具体工作环节没理清楚"（价值节点覆盖不足），不是常被提及的"缺规则"或"缺SOP"**。这条结论比较可信——两个独立的方法（早先的定性判断+这次的数据统计）互相印证，但还没有排除"是不是某几个域数据本来就少所以显得比例高"这种可能性，所以算"有依据的判断"，不算"板上钉钉"。

拿HR域（人力资源三条流程）做了个更细的案例，查到具体是哪个价值节点卡住的：人力分析与决策支持流程2个价值节点全卡，人员全生命周期管理流程7个里卡了2个，人员绩效管理流程零卡点已经可以往下推进（细节见 `01_PhaseA_HR域命题链示范_v0.1.md`）。

### 3.2 AI原生度评估模型——草案，且缺数据

打分标准草案（参照"数字化组织 vs AI原生组织"五个维度）：

| 维度 | 纯数字化 | 系统辅助 | Agent辅助决策 | AI原生 |
|---|---|---|---|---|
| 流程设计 | 纯人工执行 | 系统提供工具，人决策 | Agent做部分执行，人判断 | Agent是设计起点 |
| 决策模式 | 人凭经验 | 人看报表决策 | Agent给草稿，人确认 | Agent自主决策，人管例外 |
| 流程形态 | 无文档化 | 固化成文档，人工修订 | 有预设分支 | 能按实时反馈动态调整 |
| 组织架构 | 纯部门制 | 部门制+少量跨部门SOP | 部分能力域打破部门边界 | 按能力域+Agent集群运作 |
| 分析频率 | 无定期分析 | 周期性报告 | 部分事件触发 | 全面事件驱动 |

**这套标准现在用不了**——拿HR域试了一下，五个维度VNW的数据全部答不出来，因为VNW记录的是"这项工作能不能被拆解建模"，不是"这项工作现在实际怎么运作的"，这是两件不同的事。要用这套标准，得先有人（大概率是各部门自己）说清楚"现在具体怎么干活的"，VNW数据补不了这个空。

### 3.3 组织变革模型

完全空白，VNW没有任何HR/组织层面的数据，先记录缺口。

---

## 4. 数据底座层

| 内容 | 状态 |
|---|---|
| 流程当前状态数据 | 有（T1-T30这套表） |
| 流程历史/趋势数据 | 没有——只有"现在"，看不出变化，之前的也补不回来 |
| 业务域数据 | 有，但18个流程缺域标签、3处冲突 |
| 岗位/角色数据 | 部分有，关联不回具体价值节点 |
| 自动化难度评估 | 有，但衡量的是"值不值得封装"，不是"AI原生阶段" |
| Agent自己的运行记录 | 几乎没有 |

---

## 5. Agent编排层（VNW自己怎么跑起来的，不是分析内容本身）

| 环节 | 现状 |
|---|---|
| 战略规划 | 全靠人（Jasper）手写 |
| 流程建模 | 自动，但是从已有文档整理出来的，不是从系统操作记录里挖出来的 |
| 分析执行 | 自动，真的调用AI模型生成分析 |
| 数据处理 | 自动，但是固定脚本，靠人手动触发 |
| 整体调度 | 基本靠人工敲命令，只有一个"每30分钟检查一下有没有新数据"是自动的，检查完之后要不要用还是人决定 |
| 输出与治理执行 | 展示模板固定，不是AI自动写的；有没有通过检查的判断规则写死在代码里，没有专门审计的角色 |

---

## 6. 输出与治理层

- 输出物：VNW前端的流程详情页算一种模板，正式的"分析报告"模板没有；
- 治理规则：有没有通过检查的判定规则、数据来源可信度分级，这些都有；"不同人对同一个指标口径有分歧该怎么裁定"，这个机制没有，之前提过——如果3.2的AI原生度评估模型真要建成正式标准，这条治理规则必须同步补上，不然会跟"域"这个字段一样没人真正维护。

---

## 待办清单

- [ ] 3处域归属冲突（L3-IBRD/URD/CFM）需要人工判定
- [ ] 至少18个流程缺域标签，需要确认谁负责补
- [ ] 北极星指标要落地，需要新建"人力成本基线"和"Token成本"两类数据采集，现在完全没有
- [ ] AI原生度评估要能用，需要各部门提供"现在具体怎么干活"的信息，VNW数据补不了
- [ ] 3.1根因结论要从"有依据"升级到"板上钉钉"，需要排除"部分域数据量本来就小"这个可能性
- [x] 2.2业务集群归纳——已用VNW真实价值流数据替代猜测（见2.1）
- [ ] 25个L3缺价值流映射（含L3-COM/HRA/HRM/SPE等旗舰流程），需要确认谁负责补，跟域缺失是两条独立的缺口线
- [ ] 3.2 AI原生度评估需要各部门提供真实工作方式信息，VNW数据补不了
- [ ] 6.1 报告产出能力需要新建（阶段一完成标准要求"除前端展示外能出报告"，现在完全没有）
- [ ] 路线图（1.3）等以上几条推进后再排
