# 104 张表标签 · L1 描述层 · L2 诊断层 · CSV 字段说明

> 任务：VNW 延伸任务。基于现有业务数据分析，输出 104 张表的各标签罗列，以及 L1 描述层、L2 诊断层（L2 只关联现有 L3 流程内容，没有的为空）。
> 数据源：`VNW/10_部署与运行_Deploy_and_Run/frontend/public/data/table_analysis.json`（104 张表，只读，未改动原文件）。
> 补充：`表注释` 列取自 `description` 字段，已与 `db_catalog.json→description` **全量核对一致（104/104）**。
> 接手准则遵守情况：原始 JSON 全只读；本 CSV 产出在接手区 `13_延伸任务_业务分析CSV/`；27 处空缺如实置空，不编造。

---

## 输出文件

`104张表标签_L1描述_L2诊断.csv`（utf-8-sig，Excel 可直接打开；105 行 = 表头 + 104 张表）

## 列字典

| 列 | 含义 | 数据来源 | 空缺规则 |
|---|---|---|---|
| `schema` | 表所属 schema | `table_analysis.json → schema` | — |
| `table` | 表名 | → `table` | — |
| `table_type` | **标签①**表类型（维度/配置/事实/视图/桥接/映射/汇总/历史/系统/调整/核对/疑似/快照/规则/其他/事件/状态） | → `table_type` | — |
| `role` | **标签②**角色（业务数据 / 系统数据） | → `role` | — |
| `business_label` | **标签③**业务标签（单表语义名） | → `business_label` | — |
| `表注释` | **表字段级注释**（如 `HR员工档案表：姓名、入职/转正日期、合同地区、雇佣类型、学历；与process_analytics.dim_org不是一回事，这是实际HR档案`） | → `description`（与 `db_catalog.json→description` 全量一致） | 有注释则填，无则留空 |
| `row_count` | 当前行数 | → `row_count` | — |
| `L1_描述层` | **L1 描述层**：事实陈述（含行数口径）+ 是否有数据 | → `layer1.fact_statement` | — |
| `L2_关联L3流程` | **L2 诊断层·关联的 L3 流程**（`L3码: L3名`，多值分号分隔） | → `layer2.related_l3_l4[]` 取 l3_code+l3_name | **无关联则留空** |
| `L2_关联L4` | L2 诊断层·关联的 L4（`L4码: L4名`） | → `related_l3_l4[]` 取 l4_code+l4_name | 随 L3 空 |
| `L2_数据健康` | L2 诊断·数据健康度（如 未开始录入/小规模在跑） | → `layer2.data_health` | — |
| `L2_岗位` | L2 诊断·关联岗位（多值分号） | → `layer2.positions[]` | 空则留空 |
| `L2_状态` | L2 诊断·定位状态（已定位关联 / 未定位关联 / 已核实与业务无关） | → `layer2.status` | — |

---

## 统计口径（已实测核验）

- 总表数：**104**
- **77 张** 已定位 L3 关联 —— L2.L3 列有内容
  - 覆盖 8 个「现有 L3」：L3-COM(55) / L3-RSJD(13) / L3-FBA(12) / L3-RPD(5) / L3-CPM(5) / L3-HRA(4) / L3-HRM(4) / L3-KAGA(3)
- **27 张** L3 为空（留空，不编造），含两种情况：
  - 24 张 status="未定位关联"（只对 8/74 个 L3 做过匹配，其余未纳入本轮范围）
  - 3 张 status="已核实与业务无关(系统表)"（auth_users/auth_sessions/auth_audit_log）

---

## 边界与遗留

- 本次只输出 L1+L2；L3 归因、L4 预测、L5 决策层**未包含**（如需可另扩）。
- 27 张未定位关联：**不代表表真的无关**，只是现有业务数据匹配仅覆盖 8/74 个 L3。是否补匹配，取决于业务是否需要扩到更多 L3——待人工/业务确认。
- 本 CSV 是数据视图，不是管理层拍板结论；涵盖"未定位/待确认"的一切结论均按接手准则如实标注，不替代人工裁定。
