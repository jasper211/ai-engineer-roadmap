# VNW v0.2.0 执行记录

## 本批范围

按 Agent 搭建 SOP v1.2 建立 01–11 骨架，并完成“检测→提取→状态提交”最小闭环。Phase1 提取器通过适配层复用，避免首批重写 700 余行已验证业务逻辑。

## 关键决策

- 用文件内容 SHA-256，替代旧原型的 mtime+size MD5 伪指纹。
- 只在提取成功且产物存在后提交状态，避免失败被误记为已处理。
- 所有产物与状态写 VNW 专属工作区，不写源项目。
- 常驻监听、规则合并、规则空白和 SOP 生成延后，遵循按风险递增迁移。

## 验证

集成测试覆盖首次处理、未变化跳过、内容变化重开闭环、失败不推进状态、CLI 状态入口。

### 2026-07-18 真实数据验证

- 旧标准化 `D1_价值节点清单_标准化_数据表版_v2.0.xlsx`：PAY 9 节点，生成 710 行基线；再次运行正确返回 `unchanged`。
- 当前权威 `D1_价值节点清单_V3.44.xlsx`：首次运行暴露 Sheet 改名、标题行偏移和“是否熔断”从布尔值改为“是/否”三类兼容问题；修复后 PAY 9 节点，生成 616 行基线，4 个熔断节点与源表一致；再次运行正确返回 `unchanged`。
- 真实验证继续暴露同名产物覆盖和页脚数据源硬编码问题；现已改为按 SHA-256 前 12 位分目录保存，并动态写入真实源文件名。
- Phase1 原脚本保持不变；迁移副本位于 `skills/signal_extractor_legacy.py`，VNW 已不再依赖 Phase1 执行目录。

### 2026-07-25 前端设计优化

- 对正式前端 `规则前端设计/Agent_价值节点-规则分析前端/app_v2` 完成信息架构重整，保留既有数据接口和页面路由。
- 桌面端由 9 项拥挤顶栏改为“总览 / 分析与执行 / 数据治理”三组侧栏导航，移动端改为抽屉导航。
- Dashboard 从数据表数量陈列改为治理决策顺序：风险提醒 → 核心治理指标 → 业务域推进矩阵 → 数据审计队列 → 规则治理/SOP生产/Agent移交快捷入口。
- 首页新增真实 T18/T19/T21/T23/T25 数据口径：熔断节点、SOP资产、数据审计和 VNW→AIT 移交状态；删除未接功能的搜索和通知按钮。
- 更新页面标题、描述、焦点态、背景层次和通用 panel 视觉组件；正式目录生产构建通过。

### 2026-07-25 前端目录归并

- 将原独立“规则前端设计”中的唯一有效版本 `app_v2` 迁入 `10_部署与运行_Deploy_and_Run/frontend/`。
- 将当前有效数据底座迁入 `07_接入记忆_Integrate_Memory/data_foundation/`；未迁移旧归档、已完成任务包、构建产物、依赖缓存和同步冲突文件。
- 只保留规则识别操作系统框架 v1.2；v1.0/v1.1 作为旧版本不再保留。
- `sync_data_foundation.py` 已改用相对 VNW 根目录定位数据底座和前端 JSON，不再依赖“规则前端设计”旧路径。
- 合并后的前端以迁移目录完成 TypeScript + Vite 生产构建，VNW 原有 5 项集成测试继续通过。

## 2026-07-25 数据底座同步脚本（`skills/sync_data_foundation.py`）

- 新增一键可重复执行的同步脚本，一次运行从 02 层 8 域信号基线 + 03 层规则空白地图 + 04 层规则/GAP 清单 + `L4流程_Skill封装可行性评估` 重新生成前端展示数据底座里的 T1/T2/T6/T11/T12/T14/T20/T21/T24 共 9 张表，同时写入 `规则分析（Jasper）/前端展示数据底座` 和 `规则前端设计/数据底座` 两处物理副本并保持一致。
- 幂等设计：全量重写而非增量追加，源文件不变时连续两次运行输出字节级一致（已用 md5 校验通过）；T5/T7 不重建，只校验与 04 层是否一致，不一致会在运行日志里报警而不是静默跳过。
- 已知限制：T24（交付物四标签风险分析）只解析 03 层规则空白地图里 PAY 域"交付物N："逐条编号的格式；EQ/HR/INS/PARTNER/TREASURY/FA 用的是"交付物群（合并分析）"格式，本版本不解析，运行日志和 T21 会如实报告这个覆盖缺口。
- 定位：这是 VNW 目前唯一一段真正在处理业务数据的代码（此前 S01-S03 只做 PAY 域信号提取），后续价值节点清单/信号基线/规则清单有更新时，直接重跑此脚本即可，不需要再靠对话临时写脚本核对。

### 2026-07-25 补充：T18 扩展到全域8个域

`03_访谈准备与执行/熔断节点补建清单` 此前只有 PAY 域被解析进 T18（22 条任务，4 个节点）。核实后发现 PAY 是唯一用 7 列表格（行动/类型/执行主体/期望产出/交付物上传要求/预估周期）的域，其余 EQ/FA/HR/INS/KA/PARTNER/TREASURY 7 个域共用一套 5 列格式（列名有"负责人"/"负责人岗位"/"当前状态"等出入，语义一致）。脚本已用同义词表把两种格式都接入 T18，各域自动取最新版本号（PAY 现为 v1.2、FA/HR 为 v1.1、其余域 v1.0），重跑后 T18 从 22 条任务/4 节点扩展到 121 条任务/28 节点，孤儿引用校验通过。

### 2026-07-25 补充：T1 移除熔断字段，新增 T25 作为唯一权威熔断判定

核实 T1 的 gate_status（源自 02 层 Step2 的"熔断状态"列）时发现 KA 域内部矛盾：03 层 `KA_熔断节点补建清单` 明确写"KA域6个节点全部处于熔断状态"，但 02 层信号基线的 Step2 表把 KAEM-02/KASC-01 标成"非熔断"。Jasper 拍板：T1 不承载熔断判断，03 层（规则空白地图 + 熔断节点补建清单）才是 VNW 的过程核心产物，熔断信息应以此为唯一来源。已执行：①T1 移除 gate_status/gate_1/gate_2/gate_3/verdict 五个字段；②新增 T25_熔断状态清单，判定逻辑为"节点出现在熔断节点补建清单（按定义只收录熔断节点）→ 熔断，否则 → 非熔断"，不依赖规则空白地图头部的"包含节点/熔断节点"声明（核实过该声明跨域写法不统一，FA 用半角冒号、PARTNER 把"熔断节点"叫成"待补入节点"且域内数字自相矛盾）。重跑后 KA 域 6 个节点全部正确判定为熔断，矛盾解除。T12（Gate评级明细）保留，因为它的定位是记录 Gate①②③ 的评级细节，不是"是否熔断"的二元结论，跟 T25 不冲突。

### 2026-07-25 补充：数据底座清点收尾（T19重建、T22/T23归属、旧版本归档）

- **T19 重建**：发现之前只认 `SOP_VN-*.md` 命名，`05_SOP` 实际 41 份文件里漏了 8 份非 VN 编码的 SOP（TOI/TOB-EVD-编码 4 份、L3-编码 4 份）。改为通用扫描 `SOP_*.md`，新 T19（v2.0）覆盖全部 41 份，按编码体系分类（VN价值节点 33 / TOI-TOB证据 4 / L3流程 4），VN 类关联 T7 做 Gap 对齐（gap_total/gap_open/gap_resolved），非 VN 类如实留空不编造。
- **05_SOP 里 6 个节点补进 T1**：Jasper 拍板"以 05 内容为准"，VN-EQ-11、VN-FBA-02/03/05、VN-HR-01/02 这 6 个节点只在 SOP 里出现、不在 02 层 8 域批次内，已从对应 SOP 的"节点信息"表提取基础字段补进 T1（78 节点），来源标注为"05_SOP直接来源"。（另外核实此前误判的第 7 个"孤儿节点" VN-EQ-01-02 其实是文件名把"SOP 同时覆盖 EQ-01 和 EQ-02"误读成单一节点编码，EQ-01/02 本来就在 T1 里。）
- **T22/T23 补入同步脚本**：这两张表之前是用旁支脚本一次性建的，从未被纳入 `sync_data_foundation.py`，源文件更新后不会跟着刷新。T23（VNW→AIT移交追踪）内容本身是机械的（节点集+固定状态+L3-COM试点标记），已改为每次跟着 T1 节点集重新生成。T22（SOP规则人机协同分类）是人工判断产物（4条 VN-PAY-01 试点分类示例），无法机械推导，明确保留为人工维护表，不纳入自动重建，在 T21 里加了一条说明记录，避免被误认为是自动同步的。
- **旧版本归档**：清理了两处数据底座里的旧版本文件（T1 v1.8/v1.8_pre_node_change/v1.9、T2 v2.0、T6 v2.1/v3.0、T11/T12 v2.0、T14 v2.0、T18/T19/T20/T23 各自的 v1.0），移入 `_归档/` 下按日期命名的子文件夹，主目录现在每张表只保留当前最新版本。

### 2026-07-25 补充：T5/T7从"只校验"改为直接重建，并入A类

Jasper 拍板：T5/T7 应基于04层规则清单/Gap清单重新生成替代旧文件，归入A类（自动同步）。执行时发现旧T7有个不能弄丢的坑——`sop_field_affected` 字段（211条里45条有真实值）是04层原始文件里没有、后续单独补充上去的，如果直接从04层重建会把这45条丢掉。已处理为：重建时按 `gap_id` 从旧T7读回这个字段的值再合并进新T7，`resolution_date`（旧文件一直全部为空）如实留空。重建后T5=115条、T7=211条，与之前完全一致，sop_field_affected 45条全部保留，幂等性验证通过（含B文件夹归档移除后的二次验证）。T5/T7的物理文件和`TABLE_SUBDIR`映射都已从B类改到A类，旧B文件夹下的副本归档到 `_归档/T5T7旧B类_20260725/`，B文件夹本身已删除（因为改完后B类已无内容）。

**当前已知、未在这次处理的项**：T3（访谈线索）/T4（行动项）/T9（价值流归属）/T10（访谈批次与岗位映射）/T13（节点复评追踪）/T16（访谈记录跟踪）/T17（报告跟踪，从建立起一直是0行）——这几张不在 `sync_data_foundation.py` 管理范围内，只对 T9/T13 做了孤儿引用校验（结果为0），没有重建内容，因为它们的字段（owner/phase/blocker 等）看起来是人工维护的过程追踪数据，不是能从 01-04 源文件机械推导的内容，是否要挪进自动化范围需要 Jasper 确认。另外数据底座里还有一个 `L3蓝图覆盖清单_v1.0.csv`（67行，L3编码→流程蓝图文件映射），不在任何 T 编号体系内，这次完全没有评估过，也需要 Jasper 确认要不要处理。

### 2026-07-25 补充：T20 并入候选Agent归属，新增 T26

`Agent与Skill体系/候选Agent目录_数据表_v3.xlsx` 之前没有被同步过。核实过它的"L4明细"sheet 和 T20 原来源（`L4流程_Skill封装可行性评估_确认最终版_v2.xlsx`）对同样 368 条 L4 的 automation_tier 判断完全一致（零差异），不是重复数据源，只是多了"这条 L4 归属哪个候选Agent"这一列。已把这列按 l4_code 关联并入 T20（升级到 v2.0，368 条全部关联成功，无空值），另外把"Agent汇总"sheet（30 个候选Agent 各自的 L4 覆盖数/Tier分布/定位类型）建成新表 T26。

### 2026-07-25 补充：审计范围明确排除 EA 权威清单 V3.44

Jasper 拍板：数据表同步只按他定义的输入文件走（02层信号基线/03层规则空白地图+熔断节点补建清单/04层规则GAP清单/L4评估表/候选Agent数据表），EA 项目的权威清单 V3.44 不在这次的输入范围内，不再拿它跟 T1 做比对。已从 `build_t21()` 移除 V3.44 相关的比对逻辑和 `AUTHORITATIVE_LIST` 路径常量。T21 记录数从 27 条降到 3 条（原来 23 条都是 V3.44 对比产生的），剩下 3 条是真正跟当前输入范围有关的缺口：INS/PARTNER 域规则空白地图未解析出交付物清单、T24 只覆盖 PAY 格式。

### 2026-07-25 补充：前端接入新数据表（T18/T19/T20/T21/T23/T24/T25/T26）

之前 `app_v2` 只认识 15 张旧表，读的是 6 月 28 日的静态 JSON 快照，不知道这次新建/重建的 8 张表存在。做了三件事，一次做完：

- **数据管道**：`sync_data_foundation.py` 末尾新增 JSON 导出阶段，复用脚本内存中各表已有的 `(fields, rows)`，覆盖 A 类 16 张 + C 类 7 张 + T22，写出到 `app_v2/public/data/*.json`（纯数组格式）。`data.ts` 的 `TABLE_FILES`/`TABLE_DISPLAY_NAMES` 各加 8 条，描述文字不再写死行数。同步过程中顺带修正了 `node_name` 字段遗留的 `[🔴熔断]` 之类方括号标注未清理干净的问题。
- **专属视图**：`NodeDetail.tsx` 新增熔断状态(T25+T18)/SOP追踪(T19)/AIT移交(T23)/四标签风险(T24) 4 个 tab；新建 `AgentReadiness.tsx`（T26 候选Agent卡片 + 展开查看 T20 关联的 L4 明细）和 `DataHealth.tsx`（T21 按严重度分组的审计面板），各自挂进导航栏和路由；`StatusBadge.tsx` 补充熔断/Auto-Aug-Hybrid-Human 等新状态词的颜色映射。
- **验证中发现并修复的真实缺陷**（不在原计划内，但会拦掉"页面无报错"这条验收标准，所以顺手修了）：`Nodes.tsx` 存在真实的 React Hooks 调用顺序违规（`useMemo` 写在提前 return 之后，导致数据加载完成后整页黑屏崩溃），已修复为把所有 hooks 移到提前 return 之前；`Domains.tsx` 里 T1 改表后遗留的 `gate_status`/`domain_code`/`signal_base` 字段引用全部改成现有字段；`Home.tsx`/`Tables.tsx`/`Layout.tsx` 里写死的"15张数据表"文案清理掉。

验证：`npm run dev` 后逐页检查 `/tables`（全部 23+1 张表可浏览搜索导出）、`/nodes`→`NodeDetail`（新 4 个 tab 有真实数据，VN-EQ-01 熔断状态/VN-PAY-01 AIT移交均核对正确）、`/agent-readiness`（30 个候选Agent卡片+展开 L4 明细正常）、`/data-health`（T21 按严重度分组正常，当前 0 高优/0 中优/4 低优）均渲染无误；浏览器 console 无残留报错；同步脚本连续两次运行 JSON 输出 md5 完全一致（幂等性确认）。未执行 `vercel --prod`，线上发布留给 Jasper 另行确认。

### 2026-07-25 补充：前端交由Codex二次设计后合并，新增"内部/业务"双视图拆分

Jasper 把整个前端项目文件夹从 `规则前端设计/Agent_价值节点-规则分析前端/app_v2` 移动合并进 `VNW/10_部署与运行_Deploy_and_Run/frontend`，交给 Codex 做了一版UI重设计（新导航结构"治理驾驶舱/闭环工作流/业务域进度/价值节点/Gap与行动/访谈与SOP/Agent就绪度/数据表中心/数据健康"共9个入口）。旧路径 `规则前端设计/` 已被移进 `~/.Trash`（Jasper自行操作），`node_modules` 一度是指向 Trash 内旧目录的悬空软链接——已改成本地重新 `npm install`，避免 Trash 被清空后前端直接跑不起来。`.claude/launch.json` 的 `vnw-frontend` 预览配置已同步改到新路径。

复盘发现两个问题，均已处理：

1. **C类(人工维护)表在前端里被当作现状展示，但从未纳入本次数据底座重建**：`InterviewSOP.tsx`(访谈与SOP页)整页 + `NodeDetail.tsx`部分tab 用了 T3/T4/T9/T10/T13/T16/T17，这些表这次会话完全没被 `sync_data_foundation.py` 碰过，而 T1 这次重建了好几轮。抽查发现真实错配：`interview_recordings.json`里 REC-018 引用的 `VN-LCS-AGMT-01/02`等节点编码在当前78节点T1里不存在。处理方式是在这两处加了醒目的黄色提示条，明确标注"未纳入本次同步范围,可能与当前节点集有出入",不重写这些表(没有可靠的自动化重建路径,涉及人工判断)。
2. **前端信息架构过重,不适合给业务负责人看**：现有9入口+"26张治理表"字样的界面是数据运维视角,不是决策沟通视角。Jasper 拍板：先做两套视图跑通结构逻辑，AIT怎么接入前端的问题往后放。

新增"业务视图"(`/brief`、`/brief/node/:id`，独立 `BriefLayout.tsx`，无侧边栏)：每个节点=背景一句话(T1精选字段)+自动化/Skill就绪度结论(T25熔断状态+T18补建任务进度+T24四标签风险,PAY域外节点暂无风险数据)+验证与下一步(T23 AIT移交状态)。诚实处理了一个数据缺口：T20/T26的L4级自动化Tier判断目前只能做到L4活动颗粒度，没有可靠字段能关联回具体价值节点，页面里用提示框直接说明这个缺口，不编造关联。原有9入口界面保留作为"内部数据视图"(`Layout.tsx`+`Navbar.tsx`不变)，两者互相加了跳转入口。

验证：`/brief`列表78节点正确显示熔断/AIT试点标记；VN-EQ-01(熔断)和VN-PAY-01(非熔断+四标签风险+AIT试点中)两个详情页手动核对字段全部正确；内部视图`/node/:id`确认未被回归破坏,新增的C类数据滞后提示正确显示；console无报错。

### 2026-07-26 补充：T20接入业务视图,新增全域自动化全景页

Jasper 要求把T20(L4自动化Tier评估)的数据在业务视图里体现。因为T1(节点用EQ/PAY等8个域编码)和T20(L4活动用"权益/保司/经代业务"等13个业务板块标签)是两套独立生成的分类体系,重新核实过后确认没有可靠的字段能做节点级或域级精确关联(文本重合度几乎为0)，所以没有在`BriefNodeDetail.tsx`里硬造关联，而是新增独立页面`BriefAutomation.tsx`(`/brief/automation`)：全域368条L4活动的Tier分布(Auto50/Aug120/Hybrid167/Human31)+30个候选Agent卡片,可展开查看各自覆盖的L4明细。页面顶部保留一段说明,讲清楚这是L4颗粒度的参考,不是节点级结论。`BriefLayout.tsx`顶部加了入口链接,`BriefNodeDetail.tsx`原来"已知数据缺口"提示改为跳转链接到这个新页面。验证：Tier分布条形图数字正确,候选Agent卡片展开显示真实L4明细,console无报错。

### 2026-07-26 补充：新增SOP版前端(第三套界面),核出并修复sop_ref重复的老bug

Codex对前端做了一版重设计(改名"VNW·AI化机会台",`/`变成机会台默认入口,治理驾驶舱挪到`/internal`),Jasper看过后提出一个新方向：不以VNW数据表为基准,而是直接以`05_SOP`里每份SOP文档自己的结构(章节/步骤/条目)做一套展示,目的是逐条跟负责人核对现状,并让负责人看清SOP里哪些条目和自动化/Skill搭建疑似相关。

核实41份SOP文件的实际写法后发现三种结构,不是一套schema能覆盖：
- **详细版**(6份:VN-PAY-01/02/03/05/07+VN-IBRD-01)：第X章→交付物N→步骤N,步骤内是"子步骤"表格,穿插✅/🔲/⚠️批注(常带Gap编号)
- **精简版**(25份:VN-FBA/FLM/FOB/FOR/FPG/FTR/HR/HRD/XX等)：没有章节表格,整篇是✅/🔲/⚠️开头的条目,按###字母分组
- **流程版**(10份:4个L3-\*/3个TOB-EVD-\*/TOI-EVD-001/VN-EQ-01-02/VN-EQ-11)：##N.编号章节+表格,完全没有✅/🔲/⚠️批注体系,也没有Gap汇总表

`sync_data_foundation.py`新增`build_sop_structure()`,用两个通用抽取原语(按标题栈定位章节+提取紧邻表格/提取批注行)解析全部41份文件,产出两张新表：T27(SOP结构化工作项,1135条)、T28(SOP内Gap清单,148条,流程版10份文件如实0行,不编造)。automation_hint是关键词命中的启发式标记(系统/脚本/自动/AI/接口等15个词),不是权威判断,前端每处都标注"疑似相关,待确认"，避免误导。

新建`/sop`(全部41份按VN价值节点/TOI-TOB证据/L3流程分组浏览)+`/sop/:sopRef`(按文档原始顺序还原章节结构,自动化相关条目高亮,可一键过滤只看疑似相关项,底部附该SOP的Gap清单)。VN价值节点类的SOP详情页加了跳转链接到机会台对应节点页。

**顺带修的真实bug**：验证时SopHome出现React重复key报错("VN-XX"),查出是`SOP_REF_RE`正则一直有问题——`SOP_VN-XX-例会机制_...`和`SOP_VN-XX-季度激励_...`这两个文件名,因为"例会机制"/"季度激励"是中文不在正则的`[A-Za-z0-9]`范围内,两份文件都被截断成同一个ref"VN-XX"。这不是这次新代码引入的,是T19从建表起就带着的数据缺陷(两行sop_ref冲突),只是之前没有页面用sop_ref做过唯一性用途,没暴露出来。已改正则加一个可选的"-中文描述段"尾缀,重跑后两份文件ref正确区分为"VN-XX-例会机制"/"VN-XX-季度激励",T19/T27/T28全部同步修正,验证过其余39份文件ref不受影响。

验证：三种cluster各挑一份(VN-PAY-01详细版/VN-HR-02精简版/L3-CDS-SDS流程版)逐条核对渲染内容和原文一致,自动化hint关键词提示正确显示,Gap表(含"流程版没有Gap汇总表"的诚实提示)正确渲染,console无报错。

### 2026-07-26 补充：取消SOP版前端及其过程产物

Jasper看过SOP版前端原型后判断不行,要求取消这套界面和它的过程产物。已执行：
- 删除`SopHome.tsx`/`SopDetail.tsx`两个页面文件,`App.tsx`里对应的import和两条路由,`BriefLayout.tsx`导航栏的"SOP视图"入口链接
- `data.ts`里`TABLE_FILES`/`TABLE_DISPLAY_NAMES`撤销`sop_items`/`sop_gaps`两条注册;`StatusBadge.tsx`撤销专为SOP条目加的"已确认"/"待补充"颜色映射(确认没有别处在用)
- `sync_data_foundation.py`删除`build_sop_structure()`整段(含`_iter_headed_blocks`/`_parse_md_table`/`_automation_hint`等全部辅助函数和正则),`TABLE_SUBDIR`撤销T27/T28登记,`main()`撤销`[5c/8]`阶段、对应`write_both`调用和JSON导出key
- 删除两处数据底座下的T27/T28 CSV物理文件,前端`public/data/sop_items.json`和`sop_gaps.json`

**保留的部分**：`SOP_REF_RE`正则的修复没有回退——它修的是T19一直带着的真实bug(`VN-XX-例会机制`和`VN-XX-季度激励`两份文件的ref冲突),跟SOP版前端是否存在无关,回退它会让T19重新出现数据错误,所以保留。

重跑同步脚本验证：JSON导出从26张表回落到24张(不再有sop_items/sop_gaps),脚本运行无报错;浏览器验证`/sop`路径不再匹配任何路由,机会台首页和`/brief/automation`不受影响,console无残留报错。

### 2026-07-26 重大架构调整：接入公司数据仓库(PostgreSQL)为权威数据源

Jasper提供了公司数据仓库(PostgreSQL,`process_analytics` schema)的访问权限,并明确拍板权威层级：**数据仓库是公司标准化的权威数据源,跟01-05层文件材料(项目/OB知识库背后的原始素材)冲突时以数据仓库为准;数据仓库没有的字段/表,才用文件材料补充**。这是本session最大的一次架构转向,`sync_data_foundation.py`从"纯文件解析管道"变成"数据仓库优先+文件兜底"的管道。

**探索过程**：先只读连接查了`public` schema(62张表,是佣金/保单/合作伙伴/产品的业务运营数据仓库,`fact_commission_rate`等表名跟VN-PAY-01 SOP里描述的系统完全对得上),后来Jasper指出真正的流程数据在`process_analytics` schema——这里有`dim_process`(366条L4,含automation_tier"agentifiability"+6维打分体系agent_d1-d6,目前d1-d6基本是空的)/`dim_vn`(93个价值节点,含3个Gate+overall_judgment+is_fused)/`bridge_vn_l4`(节点↔L4桥接)/`bridge_l3_l2`(L3↔L2业务能力桥接,66条)/`dim_agent`(361行,1L4=1个规划中的原子Agent)/`dim_org`(8个岗位族)/`dim_kpi`/`dim_vs`/`fact_card`(执行遥测表,目前0行,框架建好还没真实数据)。

**验证过的关键一致性**：KA域6个节点的`is_fused`在数据仓库里全部为true,跟本session早前T25基于03层熔断节点补建清单判定的结果完全一致——说明数据仓库口径没有引入新矛盾,可以放心切换权威源。

**发现的关键不一致(未强行调和,如实以数据仓库为准)**：L3-IRI这7条L4的automation_tier判断,数据仓库和之前用的xlsx文件(`L4流程_Skill封装可行性评估`)有5条对不上(数据仓库整体更"乐观",更多判Auto)。按拍板的权威层级,直接采用数据仓库判断,不再跟xlsx对齐。

**执行的改动**：
- 新增`db_config_local.py`(已加入`.gitignore`,不进版本库)存数据库连接参数,`db_query()`统一查询入口(含date/Decimal/UUID的JSON序列化处理)
- T1：新增`merge_t1_with_dim_vn()`,把dim_vn的l3_code/l2_code/l2_name/l1_code合并进T1(冲突覆盖priority),数据仓库比文件材料多出的22个节点作为新行补入(domain从vn_id前缀推导,其余文件字段留空,如实标注来源),T1从78行增至100行
- T12(Gate评级)+T25(熔断状态)：改为`build_t12_t25_from_dim_vn()`直接查`dim_vn`,废弃原来解析03层熔断节点补建清单全文扫描的`build_t25_fused_status()`
- T20：改为直接查`dim_process`,不再解析xlsx;不强行关联`dim_agent`(核实过按l3_code+l4_name匹配只有155/361=43%,关联太弱,不编造)
- T26：改为直接查`dim_agent`,**颗粒度从"30个聚合候选Agent"变成"361个原子级规划中Agent(1L4=1个)"**——这是两种不同的Agent组织哲学,不是同一份数据的新旧版本,前端`AgentReadiness.tsx`/`BriefAutomation.tsx`两个页面因此重写(改为按l3_primary/l3_code分组展示,不再是"展开候选Agent看L4明细"的交互)
- 新增T29(`bridge_l3_l2`原样落表)、T30(`dim_org`原样落表)
- JSON导出新增`l3_l2_bridge`/`org_roles`两个key,`data.ts`同步注册

**验证**：语法检查通过;实际运行`node_index=100`节点/`gate_ratings=93`/`fused_status=93`/`l4_tier=366`/`candidate_agents=361`/`l3_l2_bridge=66`/`org_roles=8`;幂等性验证通过(连续两次运行JSON md5一致);浏览器验证`/nodes`(100/100节点正常)、`/agent-readiness`(重写后的按L3分组视图正常展开)、`/brief/automation`(同样重写,Tier分布+按L3分组正常)均无console报错。

**未完成/下一批**：`dim_kpi`/`bridge_kpi_l3`(KPI↔L3权重贡献)、`dim_vs`/`bridge_l3_vs_stage`(价值流阶段)、`fact_card`(执行遥测,目前空表)这几张数据仓库表这次没有接入,留给下一批;`dim_process`的agent_d1-d6这套6维自动化打分体系目前基本是空值,尚未真正打分。

### 2026-07-28：流程模型系统 V1 第一开发批次

按 Jasper 拍板的七项开发基线落地了第一段可执行链路：

- 新增统一证据对象，强制记录权威/补充/派生/共识四类证据及源系统、源对象、源键、源字段；派生结论必须带计算规则。
- 新增 PostgreSQL 只读访问边界，拒绝 INSERT/UPDATE/DELETE/DDL；V1 不实现反向写回。
- 新增 OB 补充材料资格检查，状态非“生效”、含“待复核”或“已被删除”的材料不能进入模型。
- 新增确定性 L3 快照和 manifest；同一事实输入反复构建得到相同 SHA-256。
- 新增 Gate M/E/A 构建器。只有蓝图覆盖索引时标为 `INDEX_ONLY`，不编造流程步骤、箭头和决策点。
- 默认四样本为 IRI、IBRD、IBEC、EO。真实库结果：IBEC=`PASS/PASS/BLOCKED`（D1-D6 0/3、无 VN-L4 映射）；EO=`PASS/PASS/PASS`（D1-D6 9/9、1 个价值节点）；IRI、IBRD 均因真实数据缺口停在 Gate A。
- 新增 8 个专项测试，连同原有 5 个集成测试共 13/13 通过。

本批次只完成模型后端和数据契约，不把临时验证快照伪装成正式前端结论。下一批将把快照目录、模型清单与待补清单接入现有 React 前端，并实现浏览器本地工作坊会话。

### 2026-07-28：流程模型系统 V1 第二开发批次

把上一批的模型后端正式接入现有 React 前端，不再维护独立 HTML：

- 快照构建器新增 `index.json`，按 Gate A 将 L3 分为 `MODEL_READY` 与 `NEEDS_DATA`，前端不再自己猜测分类。
- 新增 `/models`：并列但分入口展示“可查看的 L3 模型”和“数据不足待补”。当前真实结果为 EO 可进入，IRI/IBRD/IBEC 待补。
- 新增 `/models/:l3Code`：展示 Gate M/E/A 的逐项理由、L4 交付物地图、蓝图覆盖状态和字段证据数量。
- 蓝图只有覆盖索引时仍显示 `INDEX_ONLY`，页面明确不展示未解析的步骤、箭头和决策点。
- 新增工作坊任务卡片墙：初始位置读取数据库 Tier；桌面可拖动，窄屏可用下拉选择；改动明确标为“工作坊共识假设”，同时保留数据库原位置。
- 工作坊卡片位置和会议结论只使用浏览器 `localStorage`，按 L3 隔离；“恢复数据库位置”不会触碰源数据。

验证：前端 `npm run build` 通过；真实模型索引和四份快照均被打入 `dist/data/model_snapshots/`；Python 自动化测试维持 13/13 通过。构建仅有既存的 bundle 大小提示，不影响功能。

### 2026-07-28：流程模型系统 V1 第三开发批次

把真实 L3 流程蓝图正文接入模型，不再依赖手工 demo 中写死的流程：

- 新增 `blueprint_parser.py`，支持当前流程库里的两种主干结构：IRI 类 `步骤N：`代码块，以及 IBRD/IBEC 类 `[Step N]` 结构。
- 只解析蓝图显式出现的步骤编号、L4 编码、判断问题、分支目标和“返回/重新”关系；相邻顺序边注明派生规则。
- 每个步骤和判断点增加 `SUPPLEMENTAL` 证据，记录蓝图文件、版本、原文行号和文件 SHA-256。
- 真实解析结果：IRI 6步/2判断；IBRD 8步/2判断；IBEC 3步/2判断；EO 正文没有可用步骤结构，且当前数据库 9 个 L4 中有 8 个未被该蓝图覆盖，因此保持 `INDEX_ONLY`，不绘图。
- IBRD 蓝图正文还出现数据库当前集合之外的 `L4-IBRD-09` 和历史编码 `L4-IBPCR-01`，已进入 diagnostics，不并入数据库 L4 事实。
- React L3 详情新增“实际流程蓝图”面板：展示步骤、判断分支、回退说明、原文行号，并对 VN-L4 桥接命中的步骤做紫色标识。
- 工作坊本地草稿开始记录基础模型快照 hash；模型更新后旧草稿会显示过期警告，不自动合并到新事实。

验证：专项测试由 13 项增至 15 项，全部通过；四份真实模型索引完整；前端生产构建通过。

### 2026-07-28：流程模型系统 V1 第四开发批次

从四样本扩展到数据库当前全量 L3：

- 新增 `BulkPostgresL3Reader`，固定读取 `dim_process/dim_vn/bridge_vn_l4/bridge_l3_l2/bridge_kpi_l3/bridge_l3_vs_stage`，每类数据只查一次，再按 L3 内存分组。
- 新增 CLI `--build-all-model-snapshots`，L3 集合直接从 `dim_process` 当前有效记录发现，不手工维护名单。
- 修正蓝图索引编码大小写差异，例如数据库 `L3-IAC-AUTH` 能正确关联文件名中的 `L3-IAC-Auth`；版本统一显示为 `V1.0`。
- 全量真实结果：67 个 L3；49 个有蓝图索引，42 个可解析正文，7 个只有索引，18 个无蓝图；48 个最高到 Gate E、1 个到 Gate A、18 个未通过 Gate M。
- 唯一通过 Gate A 的仍是 `L3-EO`。系统没有因为需要“更多可展示样本”而放宽门槛。
- 索引新增 `highest_gate`、`gap_reasons` 和 `blueprint_structure_status`，待补清单可以直接说明缺蓝图、缺映射、D1-D6不完整或存在熔断。
- `/models` 升级为全量系统页：全部、可移交AIT、可评估待补、数据不足四类入口，增加统计卡和编码/名称搜索。

验证：自动化测试增至 16 项并全部通过；全量索引严格为 67 个 L3；React 生产构建通过。

### 2026-07-28：暂停全量扩展，切换单 L3 验证——L3-COM

Jasper要求暂停继续扩大系统范围，后续按 IRI 的方式逐个验通 L3，优先确保每个 demo 背后的 SSOT 可溯源。本批只处理 `L3-COM`：

- 数据库权威事实：18 个 L4、交付物 18/18、Tier 合法；D1-D6 为 0/18；`bridge_vn_l4` 中 COM 映射为 0；因此 Gate M/E 通过、Gate A 阻断。
- 蓝图补充事实：`流程蓝图_L3-COM_佣金全链路管理_V1.0.md` 明确给出 10 步主链、6 个 PAY 价值节点和 16 行 RACI。由于桥接表未同步，这些内容标为 `SUPPLEMENTAL`，不参与 Gate A。
- 蓝图还有 6 个历史归并编码（CBD/CMU/CVI），进入 diagnostics，不混入数据库当前 18 个 L4。
- 解析器新增 `[COM-xx]` 主链格式；每个步骤、蓝图节点和 RACI 行都记录文件、版本、原文行号、文件 hash 和稳定 evidence ID。
- 新增 `/demo/l3-com` 独立入口。流程图可按 6 个蓝图价值节点做黄色高亮，并明确“黄色是蓝图陈述，不是数据库桥接”；数据库桥接命中仍使用紫色。
- demo 顶部直接披露阻断原因，不把完整流程蓝图误写成“AIT已就绪”。

验证：专项测试增至 17 项；React 生产构建通过。

### 2026-07-28：COM交付形态纠正为独立HTML标准测试版

Jasper明确：COM demo 应与此前 IRI V2、IBRD 标准测试版采用相同的独立 HTML 和面板结构，而不是 React 系统详情页。已新增：

`03_规划项目结构_Plan_Project_Structure/L3流程模型_demo_L3-COM_标准测试版_20260728.html`

结构固定为：面板A流程叙事、B人工控制点、C任务卡片工作坊、D四象限机会矩阵、E交付物地图、F标准数据表，以及负责人决策结论。面板C/D支持拖动与重置。COM没有per-L4价值/可行性坐标，因此D面板18项初始统一位于“待补评估”，不编造差异位置。

验证：HTML内嵌JavaScript语法检查通过；18条L4与数据库逐项数量一致；Tier统计为Auto 13/Aug 3/Hybrid 2；D1-D6完整度0/18、VN-L4映射0均与真实快照一致。

### 2026-07-29：OB vault更新后校准COM demo

只读检索并核对了OB vault中与L3-COM直接相关的新增知识。采用生效/02_定稿材料，并对未声明confidence的内容显式标注，不采用待复核材料。HTML新增：

- Agent定位：G族JF-07佣金合规族下属专家型Orchestrator，18条L4=主链10+支链8；
- 政策校准子链COM-16→01→02由保司政策更新事件触发，非周期调度；
- 三级验证链：COM-11自动对账→COM-13人工强制签字→COM-17合规独立评估；
- 显式冲突：数据库COM-13 Tier=Auto，但OB生效高置信规则要求Human gate且不得自动放行；
- “值得建”与“当前可移交”拆开：OB判断投资价值通过，数据库Gate A因D1-D6 0/18和VN桥接缺失继续阻断；
- COM-16按保司拆Skill（MEDIUM）、COM-11生成器/评估器分离及COM-17独立评估作为工作坊补充卡。

面板F新增采用的OB证据清单与置信度，负责人结论改为优先裁定COM-13 Tier冲突和三级验证链。HTML/JS语法复验通过。

### 2026-07-29：OB知识库范围自检 + Gate A重新核算 + 新增引用源

Jasper要求划定"VNW需要OB知识库读到什么信息"的范围交给OBagent自检。核实过程中发现两个此前误判：

- **D1-D6打分方法论与全量覆盖此前判定有误**：`process_analytics.dim_process`里只有35/366条L4有D1-D6分数，之前据此认为"只有6个L3做过打分"。实际上EA项目`03_发布成果-交付物/权威数据/`和相邻目录里，D1-D6方法论定义（DICT数据字典DP-021~DP-027）和全量368条打分（`L4两阶段复核_全量368条_合并版_v1.0.csv`，68个L3全覆盖）都是完整的，只是没同步进数据库——是同步缺口，不是打分工作本身的缺口。
- **蓝图索引CSV(`L3蓝图覆盖清单_v1.0.csv`)确认过期**：67个L3里有18个不在索引里，但按实际文件核实，只有L3-EFB、L3-SRM两个是真的没有蓝图文件，其余16个文件本来就存在，只是索引没收录。

处理：Jasper裁定这批"项目组成员产出、不适合走OB白名单原子提炼"的材料应原样镜像可检索，已有`05_Agent库/草稿/OB/06_开发技能_Develop_Skills/skills/sync_vnw_reference_files.py`承担此职责（镜像到`OB知识库_vault/EA流程架构项目/_VNW引用原件/`）。本次新增5个引用源（VN价值节点全量`D1_价值节点清单_V3.44.xlsx`+映射表`D2`、KPI权威层`dim_kpi_v3.3`+明细`kpi_registry_154`+交叉表`crosswalk`），跑过同步确认全部成功镜像，`_VNW引用原件/README.md`同步更新。核实结论：VN规模跟`dim_vn`（93条）接近，不是隐藏缺口，但VN→L4绑定只在自由文本notes里，无法结构化补`bridge_vn_l4`；KPI是真实大缺口，明细表224行远超`bridge_kpi_l3`当前24行/19个L3。

**一次性核算（脚本在scratchpad，未接入正式pipeline）**：用CSV补齐D1-D6空值、按`_VNW引用原件`实际文件核实蓝图覆盖后重新跑Gate M/E/A判定，Gate A通过数从2个（L3-CPM、L3-EO）跳到30个，330条L4的D1-D6由CSV补齐。剩余37个L3卡在价值节点覆盖/熔断状态（A-003/A-004），与D1-D6无关，是价值节点体系覆盖天然稀疏（366条L4里只有136条有VN映射）。是否要把这两条修正正式接入`l3_model_builder.py`待Jasper确认。

其余待外部输入、非知识库检索缺口：L4级别责任岗位归属（68L3岗位族归属设计v6.1只到L3级人数汇总，Jasper后续找HR补充）、L3-EFB/SRM蓝图（确认真的没梳理，需要其他人输入）。`dim_process`与`dim_deliverable`的`agentifiability`字段冲突（368条里321条不一致）：Jasper裁定以`dim_process`为准；核实过VNW代码库当前没有任何地方读取`dim_deliverable`，无需改代码，仅记录裁决。

### 2026-07-29补充：两处修正正式接入l3_model_builder.py（Jasper确认"需要"）

`06_开发技能_Develop_Skills/skills/l3_model_builder.py`新增`load_blueprint_index_from_dir()`（按`流程蓝图_L3-*.md`文件名正则解析编码/名称/版本，同编码多版本取最大版本号，直接替代过期的CSV索引）和`load_d1d6_supplement()`（加载`L4两阶段复核_全量368条_合并版_v1.0.csv`，仅收录6个维度全部有值的行）。`L3ModelBuilder`新增`d1d6_supplement`参数：D1-D6字段DB有值则走原`authoritative()`记AUTHORITATIVE，DB为空且CSV有该L4完整6维时改记`EvidenceClass.SUPPLEMENTAL`（source_object标注为CSV文件名，与DB证据不混同）。`04_定义Agent_Define_Agent/agents/agent.py`默认改用目录扫描判定蓝图覆盖（`--blueprint-index`仍可显式传参走旧CSV路径，兼容排查），并从`_VNW引用原件/`加载D1-D6补充CSV。

验证：`python3 agent.py --build-all-model-snapshots`全量重跑成功，`index.json`结果与此前scratchpad一次性脚本完全一致——Gate M/E=65/67、Gate A=30/67（对照组组仅2个），蓝图覆盖65/67（仅L3-EFB/L3-SRM缺失），解析出流程结构50个。抽查L3-IRI快照：7条L4的D1-D6全部由CSV补齐（42条SUPPLEMENTAL证据，逐条可溯源到具体L4两阶段复核CSV行），该L3因此从BLOCKED变为Gate A PASS。`README.md`同步更新真实扫描统计和本次修正说明。

### 2026-07-29再补充：Jasper追问"非白名单文件谁来盯更新"，修了同步机制的真实缺口

Jasper指出：这批`_VNW引用原件/`里的EA项目源文件是其他同事在维护，不像白名单被他持续扫描，问更新怎么进知识库。核实`sync_vnw_reference_files.py`确认每日定时任务(`com.jasper.ob-daily-extract`，launchd，已verify真实在跑)会调用它，但发现两个真实缺口：①今天新加的5个"file"类型引用源(D1/D2/kpi_registry/kpi_crosswalk/L4两阶段复核)全部把版本号写死在文件名里，同事升级到新版本号会因为文件名变了而被当成"源缺失"或干脆检测不到；②即使侦测到新版本，旧版本文件会永久留在vault里，从未被清理。

处理：`sync_vnw_reference_files.py`新增`file_latest_version`类型和`_latest_by_prefix()`，按"文件名前缀+版本号紧贴扩展名"自动匹配同类文件里版本最大的一份；新增`_prune_stale()`，每轮同步后清掉manifest记录过、但本轮不再产生的旧版本镜像文件。核实过8份引用源里的版本号写法：D1价值节点清单/D2映射表/kpi_registry_154/kpi_crosswalk_154_to_43/L4两阶段复核这5份版本号紧贴扩展名，改成`file_latest_version`后自动化；DICT数据字典/68L3岗位族归属设计/dim_kpi_v3.3权威层这3份版本号后面还带日期或状态后缀（如"...v6.1_SUBMITTED.md"），自动匹配容易误判，保留"file"手动指定，出新版本仍需人工把新文件名加进`VNW_REFERENCE_SOURCES`——这是本机制唯一还需要人盯的触点，已在`_VNW引用原件/README.md`写清楚。

验证：改造后`--dry-run`确认75个文件全部"未变化"，无回归；在`/tmp`隔离目录模拟"kpi_registry_154从v2.1升级到v2.2"场景，验证自动识别新版本+自动清理vault里v2.1旧文件两个行为都生效，未影响真实EA项目文件或vault内容。
### 2026-07-30：01–11 工程说明书与现阶段对齐

对 VNW 01–11 结构做了一次“实现对文档”的差异审计。确认系统实现已经进入统一
L3 流程模型、统一分析包和 React 展示阶段，但多层 README 仍停留在早期最小闭环。
本次只更新工程说明，不修改代码、事实快照或分析结论：

- 根 README 升级到 v0.6.0 基线，增加 01–11 导航、完整生产链和真实阶段状态；
- 重写 01 需求定义，固定三阶段边界，明确第一阶段主体完成但尚未封版；
- 为 02、04、05、07、09 新增 README；
- 补充 Gate 准入后的分析运行检查、输入谱系和 Skill/Tier 双轴约束；
- 更新统一模板的面板级 SSOT/模型逻辑说明和面板 F 实际输入谱系标准；
- 更新 06 分析执行器运行说明，覆盖完整分析、修复和单批 L4 刷新；
- 修正 08 README 中“V0.1 不调用 LLM”的过期描述；
- 重写 10 部署运行链和 11 数据/分析/运行质量监控说明；
- `agent.yaml` 版本提升为 v0.6.0，并补充统一分析、修复和受控刷新能力。

第一阶段后续工作保持不变：为全部合格 L3 分批运行统一分析、执行证据抽检和跨域
回归，完成发布版本封版；第二阶段源头自动更新和第三阶段交互写回尚未启动。
### 2026-07-30：面板 C 增加可追溯任务执行链

针对“筛选单个 L4 后，任务直接分散到四个 AI 分工列，无法看出时序”的问题，
将面板 C 从单一分类墙升级为双层结构：

- 上层按来源证据展示所选 L4 的任务执行阶段；
- 下层保留 Human/Aug/Hybrid/Auto 拖动工作坊；
- 新增 `sequence_no/sequence_status/source_step_id/source_line/previous_task_ids/
  next_task_ids/relation_type` 任务关系字段；
- 能由蓝图证据定位的任务显示蓝图步骤和源文行号；
- 多任务只能定位到同一来源步骤时并列显示，明确“阶段内先后待确认”；
- 没有时序证据时进入“顺序待确认”，禁止根据任务 ID 或模型返回顺序猜测；
- 拖动只改变 AI 分工标签，不改变来源确认的执行关系。

统一提示词、分析归一化、类型契约和模板说明同步更新。新增两项时序真实性测试，
总计 41 项测试通过；React 生产构建通过。公开评审站点按 Jasper 要求保持冻结，
本次优化仅进入本地开发版。

后续经 Jasper 评审调整展示方案：不新增独立“任务执行链”模块，恢复面板 C 原四列
工作坊的紧凑结构；每张任务卡增加统一展示编号，例如
`TASK：L3-COM-12-01/02`。底层时序和证据关系字段继续保留，展示编号在任务拖动后
保持不变，用于负责人跨象限识别同一 L4 下的任务项。
### 2026-07-31：HRA/IRI质检、HRM/IBRD修复与SPE首跑

- HRA、IRI完成离线结构质检，L4、任务、四维分析和决策引用覆盖通过，继续保持
  `MODEL_DRAFT`等待业务复核；
- HRM完成任务/决策模块修复，任务由27项校准为19项并覆盖9个L4；L4-HRM-05
  完成定向刷新；
- IBRD完成任务/决策修复，并对01/02/03/06/08五个缺口L4做定向刷新；
- SPE首次运行统一模型，形成3个L4、7个任务和1项负责人决策，明确保留Skill证据
  待佐证、触发阈值不足和控制链定位不足；
- 首次IBRD修复因运行期间分析包变化被Hash锁拒绝，未覆盖新版本；重新基于当前版本
  冻结后通过，证明并发保护有效；
- 修复L4定向刷新后旧`missing_analysis`不自动清理的问题：只删除能够由刷新后非空
  字段直接证明已解决的提示，真实控制链缺口继续保留；
- 自动化测试由41项增至42项并全部通过，React生产构建通过。

由于当前执行环境的联网额度限制，全量数据库快照重建未执行，因此新分析包已经安全
发布到`analysis_packages/`，但尚未重新灌入前端`model_snapshots/`。公开评审版继续
保持冻结；恢复数据库读取能力后需执行`--build-all-model-snapshots`再做前端复验。
## 2026-08-02：P0 系统一致性恢复

- 以 PostgreSQL 只读连接重建 67 个 L3 正式快照，并同步 React 前端。
- 发现权威库中 L3-IBRD 已增至 9 个 L4，旧模型包只覆盖8个。构建器现会保留旧包的过期审计信息，回退到最新事实层的 `PENDING_MODEL`，不用旧分析填充新 L4。
- 错误输出增加 L3 和缺失 L4 编码，可直接定位 `L4-IBRD-09`。
- 回归测试增至 43 项，43/43 通过；正式前端构建通过；COM/HRA/HRM/IBRD/SPE 页面抽检无浏览器错误。
- 完整报告：`09_测试与调试_Test_and_Debug/P0_系统一致性核验报告_20260802.md`。

## 2026-08-02：P1 业务复核预审

- 基于P0最新快照重建复核队列，不将技术校验通过等同于业务确认。
- HRA检出2个疑似跨界任务：`TASK-012/013`由`HR-R004`及`VN-HR-04` SOP经`expanded`桥接进入`L4-HRA-02`。
- SPE检出4个明显不符绩效管理范围的合同归档任务，源头精确定位为`T5_规则清单_全域_v3.0.csv`第2行`HR-R003`，其`node_id=VN-HR-03`与“员工劳动合同归档”语义不一致。
- HRM、IRI可带明确缺口进入负责人复核；HRA需先确认跨界任务归属；SPE暂停复核；IBRD继续等待硬输入。
- 前端“负责人决策”增加模型建议草稿警示和证据ID，避免试点周期、范围等模型建议与SSOT事实使用同一视觉语言。
- 当前队列：`09_测试与调试_Test_and_Debug/L3模型复核队列_20260802.md`。

## 2026-08-03：阶段2 V1源头更新闭环

- 新增`--check-source-updates`：全量只读候选重建，对比当前发布快照，输出变化L3、变化域、受影响面板和后续动作，不更新前端。
- 新增`--apply-source-updates`：归档发布前快照，再安全更新事实层、manifest、索引和React前端；不调用模型、不回写源头。
- 单L3快照新增`analysis_freshness`；输入变化时页面显示红色过期警示，旧分析不再冒充当前结论。
- 首次真实基线扫描：67→67，变化L3=0，需重跑=0，阻断=0；阶段2前端状态已建立。
- 自动化测试44/44通过，React生产构建通过。

## 2026-08-03：阶段2.1半自动扫描

- 将`--check-source-updates`结果同步到前端`source_updates/pending.json`，首页可见最近扫描、影响L3、重跑和阻断数。
- 新增防并发锁的只读扫描脚本；扫描不应用事实、不调用模型、不回写源头。
- 已安装`com.jasper.vnw-source-check` LaunchAgent，每1800秒运行，登录后自动加载。
- 首次安装暴露launchd对中文工程路径传递异常，改用`~/.vnw-agent/`英文运行入口；同时修正Shell `exec`导致锁目录不清理的问题。
- 手动脚本、launchd真实后台执行、plist、44项测试和React构建均已验证。

## 2026-08-03：第一阶段合格 L3 首批统一分析

- 以各 L3 的冻结事实包为唯一输入，分别运行 CDS、SPD、SDS 三个单 L3 统一模型任务；三份分析包均通过程序校验并发布，未跨 L3 共用生成内容。
- 统一快照重建后，CDS 覆盖 2/2 个 L4、3 项任务；SPD 覆盖 2/2 个 L4、2 项任务；SDS 覆盖 3/3 个 L4、4 项任务。每个 L4 均具备 L4 分析和任务覆盖。
- 三份模型均为 `MODEL_DRAFT / UNIFIED_MODEL`，`analysis_freshness=CURRENT`，模型输入哈希与当前事实包一致。
- 三份均按门槛显示 `Gate M=PASS、Gate E=CONDITIONAL、Gate A=PASS`，模型成熟度为 `LIMITED_MODEL`；缺少规则、质量锚点、人机触点或控制链定位等内容继续进入 `missing_analysis`，未由模型补造。
- CDS 拒绝了 1 条结构不合格的负责人决策草稿和 2 条无法定位 L4 的控制链；SPD、SDS 同样拒绝无法定位 L4 的控制链，保留审计缺口。
- 自动化测试 44/44 通过，React 生产构建通过。该批次仅进入本地开发版，公开评审版继续冻结。

## 2026-08-03：第一阶段合格 L3 第二批统一分析

- 经 Jasper 明确授权，将 FOB、FPG、FBA 三个冻结事实包发送至已配置的 DeepSeek 服务；三份输出均通过统一校验并发布。
- FOB、FPG、FBA 均覆盖 4/4 个 L4，分别形成 8、7、7 项任务；L4 分析与任务均无漏项，模型运行输入哈希与当前事实快照一致，`analysis_freshness=CURRENT`。
- 三份模型统一标记为 `MODEL_DRAFT / UNIFIED_MODEL / LIMITED_MODEL`。Gate M 均为 PASS、Gate E 均为 CONDITIONAL；Gate A 因存在熔断或无价值节点均为 BLOCKED。
- Gate A 的 BLOCKED 不阻止流程模型生成，但明确禁止把模型草稿解释为可直接自动化落地；FOB/FPG 的字段与证据缺口、三份控制链定位拒绝项继续保留在 `missing_analysis`。
- 自动化测试 44/44 通过，React 生产构建通过。公开评审版继续冻结。

## 2026-08-03：大型 L3 分段生成机制与 CPM 闭环

- 新增隔离 staging 工作流：无完整分析包时从当前事实快照初始化证据骨架，按最多 6 个 L4 分批生成并逐段校验；中间结果不进入正式分析包或前端。
- 新增 `--prepare-segmented-analysis`、`--prepare-segmented-repair`、`--finalize-segmented-analysis` 三个命令。最终发布门要求全部 L4 为 `MODEL_DRAFT`、任务覆盖全部 L4、至少一条有效负责人决策、证据契约通过且事实 Hash 未变化。
- CPM 按 3+2 两批完成 5/5 个 L4 分析，再独立生成任务与负责人决策；正式结果包含 8 项任务和 1 条决策，`analysis_freshness=CURRENT`，运行方式记录为 `SEGMENTED_L4_THEN_MODULE_REPAIR`。
- 修正分段完成后仍继承证据骨架通用缺口的问题；只有对应模块确实完成后才清除通用待分析标记。CPM 最终 `missing_analysis=[]`。
- 自动化测试由 44 项增至 46 项并全部通过，React 生产构建通过。该机制已可用于后续 12–16 个 L4 的大型样本，但任务模块若在更大样本再次触发截断，仍需继续拆分任务批次。

## 2026-08-03：第一阶段合格 L3 第三批统一分析

- 经 Jasper 明确授权，运行 FLM、FOR、FTR 三个冻结事实包。FOR、FTR 首次通过；FLM 首次响应为损坏 JSON，被校验器阻止发布，基于同一输入重试后通过。
- FLM、FOR、FTR 均覆盖 4/4 个 L4，分别形成 6、6、10 项任务；L4分析与任务无漏项，输入哈希与当前事实快照一致，`analysis_freshness=CURRENT`。
- 三份均为 `MODEL_DRAFT / UNIFIED_MODEL / LIMITED_MODEL`，Gate M=PASS、Gate E=CONDITIONAL、Gate A=BLOCKED；A 门阻断原因仍为存在熔断或无价值节点，仅限制自动化落地，不阻止流程模型草稿生成。
- 证据成熟度差异被保留：FOR 无 `missing_analysis`；FTR 有 3 项缺口；FLM 有 18 项缺口，集中在岗位、质量标准、转人工条件、控制门和数据依据。因此 FLM 只能作为结构完整但证据待补的草稿。
- 自动化测试 44/44 通过，React 生产构建通过。公开评审版继续冻结。

## 2026-08-03：第一阶段合格 L3 第四批统一分析

- 经 Jasper 明确授权，运行 SOB、SPO、CFM 三个冻结事实包，三份输出均首次通过统一校验并发布。
- SOB、SPO 分别覆盖 3/3 个 L4，形成 3、4 项任务；复杂样本 CFM 覆盖 8/8 个 L4，形成 11 项任务。全部 L4 均有分析和任务，输入哈希与当前事实快照一致。
- 三份均为 `MODEL_DRAFT / UNIFIED_MODEL / LIMITED_MODEL`，Gate M=PASS、Gate E=CONDITIONAL、Gate A=BLOCKED；A 门阻断继续只限制自动化落地。
- SPO 无 `missing_analysis`；SOB 有 5 项缺口，主要为人机触点、控制门与控制链定位；CFM 有 5 项缺口，部分岗位/能力判断依赖推导，部分任务只能确认到蓝图步骤层级，未猜测更细时序。
- 自动化测试 44/44 通过，React 生产构建通过。公开评审版继续冻结。

## 2026-08-03：第一阶段合格 L3 第五批统一分析

- 经 Jasper 明确授权，运行 CRR、CPM、MED 三个冻结事实包。MED 首次通过；CRR 首次响应截断，基于同一输入重试后通过；CPM 连续两次响应截断，均被校验器阻止发布，因此保持 `PENDING_MODEL`。
- CRR 覆盖 6/6 个 L4，形成 14 项任务；MED 覆盖 5/5 个 L4，形成 15 项任务。两份均为 `MODEL_DRAFT / UNIFIED_MODEL / LIMITED_MODEL`，输入哈希一致且 `analysis_freshness=CURRENT`。
- CRR 蓝图 V1.2 的 6 个有效 L4、6 个步骤和 5 条顺序边全部解析；退役的 `L4-CRR-01` 被忽略，任务均可定位到蓝图步骤和源行号。缺少更细任务时序时继续标记待确认。
- CPM 当前只保留 `EVIDENCE_ONLY_BOOTSTRAP / PENDING_MODEL`，不把截断结果纳入系统。后续需采用分模块或分 L4 的生成方式，避免继续无效重试。
- 自动化测试 44/44 通过，React 生产构建通过。公开评审版继续冻结。
