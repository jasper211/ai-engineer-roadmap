{
  "_meta": {
    "note": "PTA 内置的空白兜底：当 --pipeline-check 指向的项目没有自己的 05_Agent库/草稿/_pipeline_health/checks.json 时用这份文件降级，避免报错。真正的检测项定义应该放在目标项目自己的 _pipeline_health/checks.json 里（参照本项目自己的那份），不建议直接往这份内置默认文件里加具体项目的检测逻辑。"
  },
  "checks": []
}
