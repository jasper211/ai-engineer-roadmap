# PTA v2.9.0 执行记录

## 本批范围

依据《Pipeline差距矩阵_检测机制设计_v1.0.md》规格（对应矩阵原件`[RPT]_AI协同建造者_Agent架构目标差距与实现路径_Jasper_v5.0.html`），新增`--pipeline-check`能力：跟`--daily-scan`平级、不是它的子功能，对矩阵里能查证的部分（文件存在性/测试exit code/字段读取/mtime）做确定性核对，不调用LLM、不做主观判断，只把"矩阵声明"和"实际状态"的差异摆出来，判断交给人。

## 产出清单

| 产出 | 路径 | 说明 |
|------|------|------|
| 技能 | `skills/pipeline_health.py` | 探针引擎：load_checks + 9种probe（file_exists/dir_exists_any/count_glob/grep_count/json_field/run_test_script/mtime_latest/mtime_staleness/doc_count_drift）+ 周报生成 |
| CLI分支 | `agents/agent.py` `--pipeline-check [--project-root] [--checks-path]` | 复用已有 `--dry-run`/`--notify` 全局参数，不新增重复语义的参数 |
| 检测项定义 | `05_Agent库/草稿/_pipeline_health/checks.json` | 覆盖矩阵7个阶段的auto检测项（17条），加载优先级同`pta_tasks.json`：显式路径 > project_root下 > 内置默认兜底 |
| 内置默认兜底 | `05_集成工具_Integrate_Tools/pipeline_checks_default.json` | 空列表，仅保证指向无关项目时不报错 |
| 定时任务 | `10_部署与运行_Deploy_and_Run/com.jasper.pta-pipeline-check.plist` | 每周五09:00独立触发，不改动daily-scan现有的每日plist，不需要DEEPSEEK_API_KEY |
| 文档同步 | `README.md`（新增章节+修正13→24测试数量）、`settings.json`（version/migration_note/skills/pipeline_check字段） | |

## 关键决策

- 检测项定义外置为JSON而不是硬编码进`pipeline_health.py`——遵循PTA一贯的"通用引擎+可插拔数据"原则（同`pta_tasks.json`）。
- `track_change`模式（无绝对期望值，只跟上次记录比）的检测项，首次运行只建立基线，不判定为drift——避免"没有历史数据可比"被误当成"发现问题"。
- `run_test_script`探针独立统计"声明测试数"（grep源码`def test_`数量）和"实际exit code"（真实subprocess跑一遍），两者都记录，不互相替代。

## 验证

首次`--dry-run`确认报告格式后，正式运行（非dry-run）一次，成功写入报告+建立基线。这次真实运行本身就验证了机制的价值，不只是走通了代码路径：

### 2026-07-21 真实运行发现的问题

1. **path前缀bug（本次开发中发现并修复）**：checks.json里`OB/VNW/AIT/PTA`开头的相对路径最初都缺了`05_Agent库/草稿/`前缀，导致多个检测（grep_count/json_field/run_test_script等）实际指向不存在的目录，`.exists()`守卫让它们静默返回"未发现"而不是报错——表面上看起来跑通了，实际什么都没真正检测到。这是一类需要警惕的bug模式：**路径写错时容易被"优雅降级"逻辑掩盖，看起来正常但没有真的在查证据**。修复后重新验证，所有探针都指向真实存在的路径。

2. **README文档漂移（工具首次真实运行即命中的目标场景）**：`doc_count_drift`探针捕获到`README.md`声明"测试覆盖13项"，与`test_integration.py`实测24个`def test_`函数不一致，drift_flag=true。本次同步修正README为24项。

3. **`/tmp/pta-daily-scan.log`缺失**：`mtime_staleness`探针发现本机没有这份日志文件，与矩阵声明的"daily_sensing每天10点真实生产运行"不符，标记为drift，需人工核实是环境问题（比如日志被清理/这台机器未安装该定时任务）还是任务真的停了。

4. **`test_integration.py`在本机实际exit_code=1，不是声明的24/24全过（新发现，未在本次修复范围内）**：`run_test_script`探针如实记录了这个结果。排查后确认根因大概率是`memory/workspace.py`的`WORKSPACE_ROOT`兜底默认值仍硬编码指向上一台机器的用户目录（`/Users/zhaoqitrenda.cn/...`），本机未设置`PTA_WORKSPACE_ROOT`环境变量时，测试清理阶段（`finally`块里`shutil.rmtree`前置的`mkdir`）因该路径不可写抛出`PermissionError`，导致整个测试进程非正常退出——这掩盖了测试内容本身的真实通过率，且看到的两处`❌`（Test 7"状态记忆未生成"、Test 13"未按预期报错"）是否也是同一根因的连锁反应还是独立的真实回归，需要另外确认。**本次未修复**：这是PTA自身的环境适配问题，跟"搭建--pipeline-check功能"是两件不同的事，本次刻意不顺手改，避免在验证新功能的同时又动了不相关的代码引入新变量。留给下一次任务单独处理。

5. **`run_test_script`的track_change比对，第一次连跑两次dry-run就误判出"drift"（本次开发中发现并修复）**：`stdout_tail`里带着`tempfile`每次运行都重新生成的随机临时目录名，就算exit_code/测试内容完全没变，整段文本也每次不同——直接拿整个结果dict做相等比较，等于每次都会误报drift，是纯噪音不是真漂移。修复：新增`_comparable_value()`，track_change的比对只看`exists/exit_code/declared_tests/passed`这几个真正有意义的字段，`stdout_tail`仍完整保留在`value`里供人查看，只是不参与"变没变"的判断。

6. **`json_field`的`text_hash`用了Python内置`hash()`，同一份migration_note内容换个进程再跑，算出来的"哈希"每次都不一样（本次开发中发现并修复）**：Python内置`hash()`对字符串按进程随机加盐（`PYTHONHASHSEED`，是CPython的一个安全特性，不是bug），本意是防止哈希碰撞攻击，但用来做"内容有没有变"的指纹就完全不适用——同一段文字，这次进程算出的值和下次进程算出的值必然不同，导致track_change永远误判成"有变化"。修复：改用`hashlib.sha256`，任何时候、任何进程，同样内容算出来的哈希都一样。修复后重新跑一次建立正确的基线，再跑一次验证幂等（无变化时不再误报），确认修复生效。

7. **（非bug，真实观察）vault概念笔记原子数量在两次相隔几分钟的运行之间有小幅波动（2041→2050）**：这不是检测逻辑的问题，是Jasper在另一个项目/会话里正跑着OB的批量提炼任务（~143次LLM调用，产出新原子），vault文件数本来就在实时增长——这条track_change检测项如实反映了这个真实的、动态变化的现状，这正是这层检测机制设计的初衷（老实说出"变了"，不猜"变得对不对"）。

## 后续更新（2026-07-21 同日追加）

- **daily_sensing诊断**：确认这台机器从未安装过`com.jasper.pta-daily-scan.plist`（`launchctl list`/`~/Library/LaunchAgents/`均无记录），EA工作区最后一份报告停在07-15，纯粹是换机器后没重装，不是代码问题。
- **`WORKSPACE_ROOT`硬编码bug已修复**：`memory/workspace.py`兜底默认值从硬编码的`/Users/zhaoqitrenda.cn/...`改为`Path.home()`动态推导。
- **多项目巡检已上线**：新增`skills/daily_sensing.py`的`extra_exclude_dirs`支持（与默认排除取并集）、`agent.py`的`--exclude-dirs`、`multi_project_daily_scan.py`调度器、`daily_scan_projects.json`清单（EA/Rw/Jasper工作文档，后者排除OB知识库_vault等无关目录）、`com.jasper.pta-multi-daily-scan.plist`。
- **两个plist已装进`~/Library/LaunchAgents/`并`launchctl load`成功**（`com.jasper.pta-multi-daily-scan`/`com.jasper.pta-pipeline-check`，`launchctl list`确认已注册）——**唯一剩余人工步骤**：`~/Library/LaunchAgents/com.jasper.pta-multi-daily-scan.plist`里的`DEEPSEEK_API_KEY`仍是占位符，需要Jasper本人填入真实key（按惯例不由AI代填）。
- **Test 7/Test 13确认是`WORKSPACE_ROOT`同一根因的连锁反应，不是独立回归**：修复后重新跑`test_integration.py`，真实`24/24通过，exit_code=0`——这是这台机器上第一次真实验证的完整绿灯。

## PTA↔OB接口第一个真实试点（v2.10.0，同日追加）

新增`tools/ob_bridge.py`——subprocess调用OB自己的`agent.py --retrieve`，不直接import OB的Python包（两边都有同名的agents/skills/tools包，同进程import会静默污染sys.modules，跨进程调用规避这个风险）。`skills/document_task_discovery.py`新增`use_ob_context`开关（默认关闭），开启后每个文件分析前用文件名去查OB取背景注入提示词。选它做试点是因为它单文件孤立分析、最容易把历史记录误判成新任务，缺背景的代价最直接。

**真实验证**：直接调OB的`--retrieve "价值节点"`确认检索链路真实可用（3条命中，带信任标注，不是空转）；用假API key跑通完整`--discover --use-ob-context` CLI链路，确认OB背景检索步骤正确执行、不崩溃，失败准确停在预期的DeepSeek 401认证错误——说明OB接入这一步本身是通的。**留待后续**：用真实DEEPSEEK_API_KEY专门验证背景内容是否真的改善了任务抽取的噪音问题，这次只验证了接口链路，没有验证效果。

## 三项目真实小范围测试（v2.10.2，同日追加）

真实key + `deepseek-v4-pro`跑通EA/Rw/Jasper工作文档三个项目，过程中发现并修复两个真实问题：

1. **新增`--seed-baseline`**：Rw(1719文件)/Jasper工作文档(440文件)从未建过基线，直接跑会把全部现存文件当「今天的变化」打包成一次巨大LLM调用。新增只写快照不调LLM的种子基线能力。
2. **`daily_scan_projects.json`补排除`项目工作区`**：真实复现自我递归噪音——daily-scan自己写的报告/状态文件被当成「项目变化」反过来分析。`PTA_OWN_ARTIFACTS`此前只排除`pta_tasks.json`一个文件名，没覆盖整个工作区目录。
3. **`llm_client.py`超时60秒→180秒**：`deepseek-v4-pro`的思考模式比`deepseek-chat`慢，真实跑EA项目6天累积的416处变更时触发了60秒HTTP超时。

**最终结果**：Rw/Jasper工作文档零变化零成本；EA真实产出9条建议任务（含P0级治理审计红线事项），企业微信通知+完整简报文件真实发送成功。全链路（多项目调度→模型调用→建议任务生成→企业微信推送）端到端验证通过。

## daily_sensing「执行→回写」闭环补齐（v2.11.0，同日追加）

Jasper指出一个真实设计缺口：任务生成后如果没执行，第二天文件没再变化，该怎么体现价值——查代码确认`status`字段只在建议任务首次生成时写成pending，之后再没有代码路径改过它，导致任务不管有没有被真正执行，都会天天原样重复出现（不是"消失"，是"永远不会被标记完成"）。

**补的闭环**：`workspace.py`新增`mark_suggested_task_status()`；`run_instruction()`执行RPT-xxx成功后自动回写done；新增`--dismiss TASK_ID`人工关闭不需要执行的任务；`SuggestedTask`加`first_suggested`支持算搁置天数；同名任务被重新建议时自动从done/dismissed重开为pending（视为"新"而非"搁置中"）；已完成/关闭的任务展示一次后不再重复出现。

**顺带按报告优化要求重构了输出**：提示词新增`domain`短标签（按域分组展示变化，不再是碎片化逐文件流水账）；建议任务按🆕新增/⏳搁置中（按天数降序）分桶；新增`format_text_plain()`通俗版，企业微信正文改发通俗版、详细版作为附件。

**真实验证**：mock掉LLM调用（避免真实费用），完整跑通"新建议→dismiss→展示一次已关闭→不再重复→同名任务reopen"全流程；`test_integration.py`全量24/24仍然通过，无回归。

## 文件回执自动识别（v2.12.0，同日追加）

Jasper说明了前端设想：任务分两种关闭方式——需要文件回执的应该自动识别，不需要的才走人工勾选/CLI。补上前一种。

**做法**：`daily_sensing_system.md`提示词加第5层职责，把"当前搁置中的建议任务"（task_id+名称+关注文件）喂给模型，模型核对今天的变化是否满足要求，命中的在`resolved_pending_tasks`里报告+附证据，从严判断（宁可漏报不错报）。`scan()`只信任真实存在于搁置列表里的task_id（防止模型编造），命中立刻标done，当次简报就展示"已完成（文件回执自动识别）"+识别依据。

**顺带修的数据丢失**：fingerprint此前从未持久化`related_files`，导致"沿用上次巡检"路径重建的任务永远丢失关联文件信息——这也是回执匹配能拿到"这条任务关心哪些文件"的前提，一并修复。

**真实验证**（mock LLM）：模拟"第1天生成需要KPI表格的任务→第2天真的补了对应文件→模型正确识别→自动标done并展示识别依据→第3天不再重复"全流程，行为符合预期；`test_integration.py`全量24/24仍然通过，无回归。

## PTA第一个前端：任务看板（v2.13.0，同日追加）

按已批准的实施计划（`/Users/a112233/.claude/plans/splendid-beaming-lagoon.md`）落地`12_任务看板_Task_Dashboard/`，补上"没有文件回执的任务，在界面上勾选关闭/继续跟踪"这条此前只能靠CLI（`--dismiss`）操作的路径。

**架构决策**：后端用纯标准库`http.server`（不引入FastAPI/Flask，延续`llm_client.py`/`wecom_notify.py`一贯的零第三方HTTP依赖习惯）；前端是独立的Vite+React+TS+Tailwind项目，跟VNW自己的`app_v2`是兄弟关系（借用主题token和StatusBadge/PriorityBadge写法，不合并路由/不共用recharts等库）。

**新增纯函数**（复用优先，不重造轮子）：`skills/daily_sensing.py`的`list_tasks_from_state()`（直接读持久化状态分三桶：new/aging/resolved_recent，不跑LLM不做diff）；`skills/pipeline_health.py`的`summarize_latest_report()`（只读最新周检测报告数drift，不重新跑检测）。

**API**（`api/server.py`+`views.py`）：`GET /api/projects`、`GET /api/tasks?project=`、`POST /api/tasks/{id}/status`、`GET /api/pipeline-status`——全部对真实3个watched项目curl验证通过，包含一次真实写回（dismiss了`RPT-20260715-03`，验证真的从aging搬到resolved_recent）。

**前端**：`TaskBoard`（旗舰视图，🆕新增/⏳搁置中/✅最近完成三栏+勾选关闭）、`PtaStatus`（daily-scan/pipeline-check运行状态，VNW/AIT/方法论/OB四块明确留白说明"暂不接入本阶段"）。`npm run build`零TypeScript错误；单进程生产模式（build+API服务器静态兜底）验证通过。

**测试**：新增Test 25-27（分桶+时间窗单测、报告摘要单测、真实起停HTTP服务器的API冒烟测试），全量27/27通过。

**诚实记录的限制**：Chrome MCP工具本次会话不可用，前端**没有做真实浏览器渲染/点击交互的可视化验证**——只验证了TypeScript编译、Vite模块图解析、API层集成。建议实际使用前先手动在浏览器里过一遍任务看板的勾选流程，确认UI真的按预期工作。

## 调度时间调整 + launchd局限性澄清（同日追加）

Jasper指出电脑关机/睡眠/未登录时launchd的LaunchAgent不会运行也不会补跑（这是macOS的机制限制，不是bug）。据此把两个定时任务都调到11:00：`com.jasper.pta-multi-daily-scan.plist`从"每天10:00"改成"周一至周五11:00（周六日不跑）"（`StartCalendarInterval`用5个`Weekday`条目的数组实现工作日限定）；`com.jasper.pta-pipeline-check.plist`从"周五9:00"改成"周五11:00"，跟daily-scan新时间对齐。仓库模板+`~/Library/LaunchAgents/`安装副本（含真实API key的那份）都已同步改，`launchctl unload/load`重新加载生效。

## Skill调用日志 + Agent执行监控器（v2.14.0，任务看板"A类"模块第一批）

Jasper对任务看板的"A类"模块给了go-ahead，同时提出一个新需求：需要一个"目前搭建的全部Agent执行的监控器"——哪些自动跑、哪些人工跑、哪些"死的"，以及Agent背后各个skill的调用频率，明确说这是"为了未来优化skill要做的"。范围经AskUserQuestion确认：四态分类（自动/人工/未搭建/死的），skill日志范围只接PTA自己（不接VNW/AIT/OB）。

**Skill调用日志**：`memory/workspace.py`新增`log_skill_call(skill_name, project_root)`（纯追加写`skill_usage_log.json`，读写失败静默忽略不影响主流程）+`load_skill_usage_summary()`（按skill分组统计次数+最后调用时间，按次数降序）。埋点覆盖`agent.py`的`main()`里全部8个`cmd_*`分发分支，以及`run_instruction()`内部5个阶段（intent_parsing/execution_planning/progress_tracking/archive_review/doc_sync，后者只在真正走`--sync`分支时才记）。这份日志从代码上线这天开始累计，历史调用量无法补，仪表盘展示时如实说明这一点，不能暗示是PTA诞生以来的完整数据。`.gitignore`新增对应条目，不进git。

**Agent状态检测**：新增`skills/agent_status.py`+配置`02_配置项目_Configure_Project/agent_registry.json`——五个主Agent（PTA/VNW/AIT/方法论转正Agent/OB）各自的`code_paths`（判断"有没有真实代码"）+`launchd_labels`（判断"有没有接自动化"），检测逻辑跑一次`launchctl list`拿真实退出码快照，逐Agent匹配。四态判定：**自动**=有job且全部健康；**人工**=有真实代码但无job；**未搭建**=代码和job都没有；**死的**=有job但至少一个最近一次退出码非0。

真实检测结果（如实反映，不做主观修正）：
- PTA=自动（`pta-multi-daily-scan`/`pta-pipeline-check`两个job最近都是exit 0）
- VNW=人工（`Agent_价值节点-规则分析前端/app_v2`有真实代码含构建产物，但没有任何launchd job）
- AIT=未搭建（无任何可执行代码，VNW之后的下一步规划还没开始）
- 方法论转正Agent=未搭建（同上，尚未启动）
- OB=死的（`ob-daily-extract`健康运行，但`ob-sync-agent`最近一次退出码是1——这是唯一需要人工介入排查的状态，OB本身归另一个项目管，这里只做只读展示不深入排查）

两者打包进新接口`GET /api/agent-monitor`（`views.py`新增`agent_monitor()`，`server.py`新增路由）。

**测试**：`test_integration.py`新增Test 28（对真实五Agent状态的结构性断言：五个agent_id齐全、状态都落在四态定义内、AIT/方法论转正Agent确为未搭建、OB两个job都被匹配到）、Test 29（`load_skill_usage_summary`聚合排序单测——用monkeypatch替换`ws.SKILL_USAGE_LOG_PATH`为临时文件，隔离真实日志不被测试污染；`/api/agent-monitor`真实起停HTTP服务冒烟）。全量29/29通过，无回归。

**已知的测试副作用**：Test 7/13走`subprocess`调用`agent.py`（黑盒CLI验证），子进程里的`log_skill_call`没有被monkeypatch覆盖，每次跑测试都会往真实`skill_usage_log.json`里追加几条`project_root`指向`/var/folders/.../pta_test_*`临时目录的记录——这是可识别的测试噪音（不是真实使用数据），本次已手动清理，后续如果要看"真实skill使用频率"，需要过滤掉这类tmp路径的记录，或者接受这点小噪音（数量很少，一次测试跑5条）。

## 任务看板"A类"模块收口：剩余三个后端 + 全部前端（v2.15.0）

延续上一条的"A类"批准范围，把剩下三个后端模块（今日动态/Pipeline漂移详情/执行记录，OB检索框严格说是四个）全部补齐，并第一次把它们接进真正的前端界面——此前只有Agent监控器有后端没有界面。

**新增后端函数**（复用优先，胶水层不重造轮子）：
- `skills/daily_sensing.py`的`latest_report_summary(workspace, project_name)`——只读该项目工作区里最新一份`daily-scan-*.json`（按run_id文件名排序取最后一份），不重新扫描，从没跑过daily-scan的项目优雅返回None。
- `skills/pipeline_health.py`的`drift_detail_from_latest_report(report_dir)`+`_parse_table_rows()`——把周检测报告"本周与矩阵声明不一致"那张markdown表格完整解析成`{stage, dimension, claim, actual, note}`结构化行，不只是`summarize_latest_report`的那个drift计数。
- `views.py`新增`activity_feed()`（跨项目取各自最新报告）、`pipeline_drift_detail()`、`execution_history()`（合并`state.json`的`task_history`，按时间戳降序取最近N条）、`ob_search()`（直接调用`tools/ob_bridge.get_background()`，实时查询，不缓存不落盘——检索本来就不是高频路径）。
- `server.py`新增4个路由：`GET /api/activity-feed`、`GET /api/pipeline-drift-detail`、`GET /api/execution-history`、`GET /api/ob-search`。

**新增前端**（全部基于已验证的接口，无额外后端改动）：
- `AgentMonitor.tsx`——五个Agent状态卡片（`AgentStatusBadge`四态配色：自动=success/人工=info/未搭建=muted/死的=danger）+ launchd job明细 + skill调用频率表。这个页面同时承担了此前规划里"C类VNW/AIT/方法论静态状态卡片"的角色——两者数据同源（都来自`agent_registry.json`+真实launchctl状态），拆成两个视图只会展示同一份事实两次，所以合并成一个，没有另建静态卡片。
- `ActivityFeed.tsx`——按`domain`分组展示各项目最新一次巡检的变化，附OB背景检索输入框（Enter或点击按钮触发，展示原样返回的检索结果文本）。
- `PtaStatus.tsx`扩展——drift数量旁新增可展开的完整表格（点击展开，懒加载`/api/pipeline-drift-detail`），新增"执行记录"板块展示跨项目最近20条`task_history`；移除了被Agent监控器取代的"其他Agent暂不接入"占位文案。
- `App.tsx`导航从2个tab扩到4个：任务看板/今日动态/Agent监控/运行状态。

**真实验证**：4个新接口全部对真实数据curl通过——activity-feed返回3个项目各自最新报告（含Jasper工作文档项目6条真实变化）；pipeline-drift-detail完整解析出4条真实drift行（含阶段/维度/声明/实测/说明全部字段）；execution-history对3个watched项目返回空列表（如实反映——这3个项目都还没有真正执行过RPT任务，不是bug）；ob-search真实调用OB子进程，查"价值节点"命中5条带来源标注的结果。`npm run build`零TypeScript错误。单进程生产模式（重新起停`server.py`）验证4个新路由+静态首页全部200。

**测试**：`test_integration.py`新增Test 30（`latest_report_summary`纯函数单测，含"选最新"和"从没跑过daily-scan返回None"两种场景）、Test 31（`drift_detail_from_latest_report`完整字段解析单测）、Test 32（4个新接口的真实起停HTTP冒烟，含一次真实OB子进程调用）。全量32/32通过，无回归。

## B类收口：evidence持久化修复 + 巡检项目管理界面（v2.16.0）

A类全部落地后，按Jasper此前的排序（"B类在A类完成后进行"），继续把此前明确推迟的两项B类工作做完。

**1. 真正的bug修复：evidence此前从未持久化进fingerprint**

复盘v2.12.0"文件回执自动识别"的实现，发现一个真实缺口：`scan()`里判断"今天的变化满足了某条搁置任务"时，`evidence`（识别依据）只塞进了当次简报的`ResolvedTask`临时对象，从没写回`fingerprint`本身。结果是——`list_tasks_from_state()`（任务看板读的是持久化状态，不是当次简报对象）永远拿不到这条evidence，过了生成简报的当天，用户在看板里点开"最近完成"只能看到任务被关闭了，却看不到是凭什么关闭的。

**修复**：`daily_sensing.py`里两处把fingerprint状态改成done/dismissed的地方（"首次展示"路径+"文件回执自动识别"路径）都同步把evidence写进fingerprint字典；`list_tasks_from_state()`的`resolved_recent`分桶新增`evidence`字段透传；前端`TaskCard.tsx`新增展示行（✅ 文件回执自动识别 — {evidence}）。

**测试**：新增Test 33，用真实两轮`scan()`调用（stub掉LLM调用，不产生真实费用）完整复现"第1天生成需要KPI表格的任务→第2天真的补了文件→模型识别到回执→evidence真的存进了fingerprint→仪表盘视角（`list_tasks_from_state`）也能读到"全流程——不只是测`list_tasks_from_state`这个纯函数本身，而是端到端验证了这个bug真的被修复了。

**2. 巡检项目管理界面**

此前加/删一个每日巡检项目，只能手改`daily_scan_projects.json`再手动跑`--seed-baseline`命令——不是一个真正的前端交互闭环。新增`views.py`的`add_watched_project()`/`remove_watched_project()`/`list_watched_projects_config()`，`server.py`新增`GET/POST /api/watched-projects`+`DELETE /api/watched-projects/{name}`：新增时校验目录真实存在、名称不重复，写入配置后立刻调用`DailySensor.seed_baseline()`建种子基线（不调LLM），一次表单提交完成此前需要两步的操作。前端新增`WatchedProjectManager.tsx`组件，嵌入`PtaStatus.tsx`运行状态页——展示已配置项目列表（含exclude_dirs）、新增表单、每项一个删除按钮。

**真实验证**：用一个真实tmp目录新增项目（确认建立了1个文件的种子基线）→重复新增同名被拒绝（400，不静默覆盖）→新增不存在的目录被拒绝（400，不污染配置文件）→真实删除后从配置文件消失（不留痕迹）→删除不存在的项目返回404（不是静默成功）——全部通过真实HTTP请求验证，测试的`finally`块里有双重保险确保测试项目不会残留在真实的`daily_scan_projects.json`里。

**测试**：新增Test 34，全量34/34通过，无回归；`npm run build`零TypeScript错误。

至此A类（今日动态/Pipeline漂移详情/执行记录/OB检索/Agent执行监控器）+ B类（evidence持久化/巡检项目管理）两批规划的模块全部收口，C类（VNW/AIT/方法论静态状态卡片）此前已确认并入Agent监控器视图，不再单独建面板。

**诚实记录的限制（跟v2.13.0同样的情况）**：本次会话仍然没有可用的浏览器渲染工具（尝试过ToolSearch查找Chrome/Playwright类工具，确认不可用），新增的4个前端页面同样只验证了TypeScript编译通过+API集成响应正确，**没有做真实DOM渲染/点击交互的可视化验证**。建议实际使用前先在浏览器里手动过一遍这几个新tab，尤其是OB检索框的交互和Pipeline漂移详情的展开/收起。

## 任务驾驶舱第一阶段（v2.17.0）

将原“任务看板”升级为 PTA 人工决策入口：首页改为今日指挥中心，直接展示待决策、P0、需 Mark 裁定、已接受/转交四类核心指标和待决策/执行准备/搁置三个工作队列；新增任务详情抽屉，支持接受、转交、合并、忽略，并可补齐负责人、截止日期、验收标准和决策备注。

数据层采用双状态设计：保留 daily_sensing 原有 `pending/done/dismissed` 生命周期，另增 `decision_status` 管理 `pending_review/accepted/transferred/merged` 人工判断，避免前端扩展破坏每日巡检、任务重开和文件回执自动识别逻辑。接受任务只进入“执行准备”，不会触发 Shell、Git 或外部通知。

新增 Test 35 验证双状态兼容、负责人/验收标准读取和合并关闭行为；全量集成测试 35/35 通过，前端生产构建通过。

## 执行控制台第二阶段（v2.18.0）

已接受任务新增执行准备面板，直接复用 `ExecutionScheduler` 生成真实步骤计划，不另造一套编排逻辑。后端新增 prepare/dry-run/approve 三个动作：计划生成后按命令与工具做确定性风险分级；dry-run 使用现有执行器的无副作用模式，保存逐步骤状态和输出；全部通过后才允许批准。批准只写入 `approved_at/approval_note/requires_explicit_execute`，不会调用真实执行入口。

任务标题、优先级或验收标准变化时自动作废旧计划；任务转交、合并、忽略或被巡检重新打开时也会清除旧执行状态，避免过期批准继续生效。新增 Test 36 覆盖计划生成、高风险标注、dry-run 无真实命令执行和批准后二次授权，全量 36/36 通过。

## 个人三项目指挥中心纠偏（v2.19.0）

根据 Jasper 明确反馈，纠正此前把首页做成“任务审批控制台”的产品定位：个人驾驶舱的SSOT是EA、Jasper工作文档、Rw三个项目从上一次成功巡检到本次巡检之间的全部文件变化。首页改为EA核心主视图、Jasper AI试验田/Rw真实案例辅视图；完整列出新增/修改/删除文件，关键变化默认展开内容级前后对比，任务与执行建议降为文件事实的下游页面。

数据层新增文件级 `change_type/before_excerpt/after_excerpt/diff_text`，模型漏项时由本地diff补齐，不允许SSOT因LLM摘要取舍而丢文件；删除文件保存最后已知内容。新增 `cross_project_sensing`，三项目巡检全部结束后只消费落盘报告做一次跨项目深度分析，持久化后供页面读取，不因刷新重复调用LLM。Test 37验证模型漏项下的完整文件集合、三类变化、修改前后/删除前事实、三项目角色排序和跨项目分析落盘；全量37/37通过。

## daily_sensing按项目差异化分析（v2.20.0）

Jasper反馈任务驾驶舱"鸡肋"、感受不到实际工作场景里的意义。诊断：不是模块做得不够多，是daily_sensing这个分析入口本身，从建成以来一直对EA/Rw/Jasper工作文档三个项目用同一份通用系统提示词——LLM产出的永远是笼统的"这个文件变了什么"，下游不管是任务看板的个人相关性判断还是跨项目分析，都只能在这份笼统输出上做事后加工，天然有天花板。这次的范围严格限定在PTA核心的daily_sensing分析层，不碰`12_任务看板_Task_Dashboard/`下任何文件（那是Codex负责的界面设计范围），也不改`cross_project_sensing.py`/`personal_work()`——upstream产出更精确后，这两处会自然获得更可靠的判断信号，不需要跟着改代码。

**做法**：新增两份项目专属提示词。`daily_sensing_system_ea.md`（EA流程架构项目）——把"这条变化跟四方有没有关系"这种笼统判断，换成EA真实生效的四人协同裁定权限模型（业务架构类→Mark/流程类→Terresa/执行规则SOP类→Jasper/HR岗位类→Ivan袁林/跨域→Mark兜底，来源CLAUDE.md§6.1），并新增02→03层晋升信号识别（有依据时才提示"这份产出可能已具备晋升条件"）。`daily_sensing_system_jasper_meta.md`（Jasper工作文档）——判断标准换成"反哺EA成熟度"三档：已验证可复用/探索阶段/纯内部工具，只有第一档才允许在rationale里出现"反哺EA"/"应用于EA"类措辞，从严要求（错误使用的代价比漏报大，会让还在探索期的方法被误判成可以直接推给EA用）。Rw权益项目暂时保持通用提示词，不强行设计一套还没想清楚的专属标准。

**技术实现**：`daily_scan_projects.json`每个项目新增可选`system_prompt`字段（相对`08_设计提示词_Design_Prompts/prompts/`的文件名）；`agent.py`新增`_resolve_daily_scan_system_prompt()`按`project_root`查表解析对应文件，找不到配置/项目未登记时返回None，`cmd_daily_scan()`据此回退`DailySensor`的默认通用提示词——schema完全不变（`changes`/`suggested_tasks`/`resolved_pending_tasks`结构一致），只是不同项目喂给LLM的判断标准不一样，对下游消费者（任务看板、跨项目分析）完全透明。

**真实验证**：resolver对EA/Jasper工作文档/Rw/未登记项目四种真实场景返回正确结果（前两者精确指向新建的提示词文件且文件真实存在，后两者正确回退None）。新增Test 38，与Codex同期新增的Test 35-37并行共存（发现命名撞车后按顺延处理，未覆盖对方的测试），全量38/38通过，无回归。

**诚实记录**：这次只解决了"upstream分析该不该按项目差异化"这一层，没有验证差异化后的prompt在真实DeepSeek调用下的实际输出质量（要等下一次真实daily-scan cron触发，或人工用真实API key跑一次才能看到实际效果）——建议下次真实巡检后过一遍EA和Jasper工作文档两个项目的简报，确认rationale措辞是否真的命中了预期的判断框架，而不是空有提示词、模型没有照办。

## 两个调度bug修复 + 对齐北京时间（v2.21.0）

Jasper发现这两天没收到企业微信推送，怀疑是电脑没开/网络问题导致自动化没跑，要求排查。

**根因1：daily-scan周一的触发被系统睡眠吞掉了**。`pmset -g log`显示本机2026-07-26(周日)09:01进入合盖睡眠，连续38小时没有真正唤醒（中间只有几次系统级DarkWake维护，不算真醒），直到07-27(周一)23:03才真醒——daily-scan周一11:00的触发窗口正好落在这段睡眠正中间。这不是代码bug，是launchd的固有限制（睡眠/关机时不触发，且不会补跑，之前已经跟Jasper讨论过这条限制）。已用`launchctl kickstart -k gui/$(id -u)/com.jasper.pta-multi-daily-scan`手动补跑一次，3/3项目成功，EA项目产出1条真实建议任务，企业微信通知补发成功——顺带验证了v2.20.0新增的EA专属提示词真的按预期工作：这条任务的rationale里出现了"这属于执行规则/SOP类工作"和"可能已达到晋升03层交付标准"这类精确措辞，不是泛泛而谈。

**根因2：pipeline-check的Weekday写反了**。检查过程中发现`pta-pipeline-check.plist`里的注释写着"Weekday: 6 = 周五"，但launchd真实约定是`0/7=周日、1=周一...6=周六`，这个注释本身就是错的（这是我自己在v2.9.0/v2.9.1埋下的bug，不是Codex的）。真实证据：`_pipeline_health/`报告目录只有07-21(周二)和07-25(周六)两份，原定的07-24(周五)完全没有产出，证实这条job从装上开始就一直在周六触发，不是周五。

**新需求：对齐北京时间**。排查过程中Jasper提出，这两个定时任务应该按**北京时间**11:00触发，不是本机系统时区(America/Chicago)的11:00——launchd的`StartCalendarInterval`只认本地挂钟时间，没有跨时区调度能力。用当前CDT(UTC-5)与北京(UTC+8)的13小时时差反推：北京周一~周五11:00对应芝加哥本地**周日~周四22:00**（提前一整个日期）；pipeline-check的北京周五11:00对应芝加哥**周四22:00**——这次改动正好把根因2的off-by-one也一并吸收掉了（周四=Weekday 4，不再是错误的6）。

给Jasper摆了两个方案：直接改Hour/Weekday（简单但每年11月/3月美国夏令时切换时会漂移1小时，需要人工微调）vs 做一个每小时检查真实北京时间的包装脚本（永远不用手动调，但多一层机制）。**Jasper选了前者**，原话"无非到时候是北京时间到12点就好了"——接受这个可预期的季节性偏差，不为此增加额外的调度层。

**改动范围**：仓库模板（`10_部署与运行_Deploy_and_Run/`下两份plist的注释和StartCalendarInterval）+ `~/Library/LaunchAgents/`安装副本（用`PlistBuddy`精确改`StartCalendarInterval`的`Hour`/`Weekday`字段，不碰其他内容，尤其不碰daily-scan安装副本里的真实`DEEPSEEK_API_KEY`），改前各自备份为`.bak_before_tz_fix`。`launchctl unload/load`两个job重新加载，`launchctl list`确认调度生效。这次只是plist配置改动，没有改任何Python代码，`test_integration.py`的38/38通过状态不受影响。

## Agent Registry 追上真实世界的变化（v2.22.0）

跑完上面的调度修复后照例跑一遍`test_integration.py`确认没有回归，结果Test 28（Agent状态检测）真的报了一个错——不是这次改动引入的，是`agent_registry.json`里VNW的`code_paths`指向的目录已经不存在了。

排查后跟Jasper确认：**"规则前端设计已经与VNW合并，并删掉了之前的部分文件"**——VNW项目已经从旧的`规则前端设计/Agent_价值节点-规则分析前端/`搬到了`05_Agent库/草稿/VNW/`，而且不只是搬家，是按PTA/OB同款的01-11方法论结构完整重建了一遍（有自己的agents/skills/tools/memory/prompts/tests）。

顺手核查AIT和方法论转正Agent这两个此前判定"未搭建"的目录，发现更大的进展：
- **方法论转正Agent**：已经有真实代码`github_discovery.py`（GitHub方法论线索扫描），而且已经真实装机了launchd定时任务`com.jasper.methodology-github-discovery`，最近一次运行exit 0——从"未搭建"直接升级到"自动"。
- **AIT**：目前只有`config.json`+`需求定义.md`，还是规划性质，没有可执行代码，维持"未搭建"。
- 顺带确认：上次发现的OB `ob-sync-agent`（openpyxl解释器路径bug）已经被真实修复，最近一次运行exit 0，OB整体从"死的"变回"自动"。

**改动**：`agent_registry.json`同步更新——VNW的`code_paths`指向新位置（`04_定义Agent_Define_Agent/agents`+`10_部署与运行_Deploy_and_Run/frontend/package.json`）；方法论转正Agent新增`code_paths`+`launchd_labels`；PTA的`launchd_labels`补上此前发现但漏记的`com.jasper.pta-dashboard`常驻服务job。`test_integration.py`的Test 28断言同步更新以反映新现实（AIT维持未搭建的断言不变，方法论转正Agent的断言从「未搭建」改成「自动」）——这不是断言追不上代码，是代码需要追上真实世界这几天的变化。全量38/38通过。

## agent_status区分「真崩溃」和「非0退出码但无崩溃痕迹」（v2.23.0）

跟进OB `ob-sync-agent`那次排查——之前诊断的`vault_sync_health.py`第301行`NameError`确认已经被修复（重新读源码+真实跑了一遍`--sync-check`，6项检查全部走完没有任何Traceback），但退出码还是1。查了vault真实git状态，是2个文件（`_VNW引用原件/README.md`和`_manifest.json`）处于未提交状态——脚本设计成"6项健康检查有1项不达标就`sys.exit(1)`"，"工作区不干净、主动跳过pull避免冲突"属于这一类，是完全正常、非破坏性的预期行为，不是崩溃。

这暴露了PTA自己`agent_status.py`判定逻辑的一个真实粗糙点：只看launchctl的退出码，没法区分"真的跑崩了"和"跑完了、如实报告了一个真实待办事项"，这类脚本会被系统性地误判成"死的"。

**修复**：`skills/agent_status.py`新增`_stderr_path_for_label()`（从`~/Library/LaunchAgents/*.plist`读`StandardErrorPath`，不在`agent_registry.json`里另存一份，plist本身才是真源，多存一份就是又给pipeline-check的Weekday off-by-one式bug开一条后路）+`_recent_stderr_has_crash()`（读stderr文件末尾20KB，查有没有真实Python `Traceback`字符串）。退出码非0时不再直接判"死的"：查到Traceback才算真崩溃；查无痕迹则维持健康，但在该job的`launchd_jobs`明细里附一条note说明原因；找不到stderr路径/文件时保守按"证据不足=不健康"处理（宁可多提醒，不可把真崩溃悄悄放过）。

真实验证：OB现在正确显示为"自动"，`ob-sync-agent`那一项标healthy=true，附note"退出码非0，但stderr近期无崩溃痕迹...不代表任务真的崩溃"。测试新增Test 39，用fixture plist+err文件覆盖"真崩溃/无崩溃痕迹/找不到plist"三个方向，加一条真实OB端到端断言，全量39/39通过。前端`AgentMonitor.tsx`的`JobRow`新增note展示行。

**别忘了重启常驻服务**：`com.jasper.pta-dashboard`是`RunAtLoad+KeepAlive`的常驻进程，编辑`agent_status.py`后已经在跑的那个进程不会自动重新加载代码——`launchctl kickstart -k`重启后才生效，重启后真实curl验证`/api/agent-monitor`确实反映了新逻辑。顺带用`/api/projects`确认了上一版调对齐北京时间的调度真的生效了：三个项目最新一次daily-scan时间戳都在2026-07-29 22:00左右（对应北京时间07-30 11:00）。

## pta_tasks.json迁移进项目专属工作区（v2.24.0）

Jasper在追问"这个pta_tasks.json文件是谁创建的、是我吗"之后，顺着往下问出了一个真实的架构问题：PTA自己的所有状态（`daily_sensing_state.json`、报告、执行记录）都严格隔离在项目专属工作区里，唯独`pta_tasks.json`是例外——它一直直接写进被扫描项目自己的根目录（EA/Rw/Jasper工作文档三个项目的文件夹里各有一份），跟"PTA的东西和目标项目物理隔离"这条从一开始就定的铁律不一致。

**根因**：`pta_tasks.json`最初的设计意图是"项目自己可以维护的任务执行知识库"（供项目负责人手写真实执行步骤），所以刻意放在项目根目录、想让它"看起来是项目自己的文件"。但实际使用中它几乎全是daily_sensing自动写入的RPT任务（步骤都是占位的`manual_review`），"项目自定义"这个原始用途从未被真正用到，反而让它成了唯一一处隔离原则的例外。

**修复**：`tools/task_knowledge.py`新增`from memory import workspace as ws`依赖，`load_task_map()`和`merge_suggested_tasks()`的读写路径都从`project_root/pta_tasks.json`改成`ws.get_project_workspace(project_root)/pta_tasks.json`——能力不变，只是物理位置搬进工作区。真实迁移：EA/Rw/Jasper工作文档三个项目现存的`pta_tasks.json`（含`.bak`备份）全部从项目根目录移动到各自工作区，确认项目根目录下不再残留。

顺带修了一个真实bug：`agent.py`里daily-scan写完建议任务后打印的提示信息，之前一直显示的是旧的（现已错误的）项目根目录路径，会让用户去错误位置找文件，随迁移一并修正为打印真实工作区路径。

**测试同步更新**：Test 8/11（`task_knowledge`自身单测）和Test 36（Codex的驾驶舱执行准备测试）的fixture都改成写在`get_project_workspace()`真实解析的路径上——Test 36原本monkeypatch了一个跟`get_project_workspace()`不一致的workspace变量，这次顺带核实清楚"读fingerprint"和"读pta_tasks.json"其实是两条独立的工作区解析路径，不能共用同一个monkeypatch。

**真实验证**：迁移后`load_task_map`对EA项目正确读到27条任务（Rw 5条，Jasper工作文档22条）；真实跑CLI`agent.py "执行 RPT-20260715-01" --project-root .../流程架构项目_jasper`（dry-run，非破坏性）确认intent_parsing→task_knowledge查找→execution_planning整条链路在新路径下完全正常，跟迁移前行为一致。全量39/39通过，无回归。

## pta_focus.md同样迁移进项目专属工作区（v2.25.0）

Jasper接着指出"还有一个pta_focus.md在根目录下"。核查后发现这个文件跟pta_tasks.json性质不同：全代码库只有`daily_sensing.py`的`_read_pta_focus()`一处读它，没有任何写入路径，是纯人工维护的输入文档（"四方关注领域"，各方各自维护一段）。一开始按"这是多人协作维护、其他人可能需要方便地找到并编辑它"的假设，建议保留在项目根目录不动。

Jasper澄清了这个判断：**这份文件是Jasper自己任务驾驶舱的配置，跟其他人无关——Terresa/HR/Carrie未来如果需要，会有他们自己独立的PTA实例，不会共用这一份**。既然不是多方协作文档，就应该跟pta_tasks.json一样物理隔离进PTA自己的工作区，不该散落进目标项目的文件夹——这是这次会话反复出现的同一条原则："PTA/Agent相关的文件不要乱放"。

**改动**：`skills/daily_sensing.py`新增`from memory import workspace as ws`依赖，`_read_pta_focus()`的读取路径从`project_root/pta_focus.md`改成`ws.get_project_workspace(project_root)/pta_focus.md`。真实迁移：EA项目唯一现存的一份`pta_focus.md`（Rw和Jasper工作文档两个项目目前都还没有各自建这份文件）从项目根目录移动到其专属工作区，确认项目根目录不再残留。

**真实验证**：迁移后`_read_pta_focus()`对EA项目正确读到1099字符的真实内容。这个函数此前没有专门的单元测试覆盖，迁移不需要改测试，全量39/39仍然通过。
