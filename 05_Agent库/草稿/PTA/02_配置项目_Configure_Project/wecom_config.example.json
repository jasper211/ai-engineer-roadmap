{
  "_说明": "复制这份文件为 wecom_config.json（同目录），填入真实值。wecom_config.json 已加进 .gitignore，不会被提交。webhook_url 和手机号都是敏感信息，绝不要把真实 wecom_config.json 放进任何 --daily-scan 会扫描的目标项目目录——只能留在这里（PTA 自己的配置目录）。",
  "webhook_url": "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=REPLACE_WITH_YOUR_WEBHOOK_KEY",
  "mobiles": {
    "Jasper": "REPLACE_WITH_MOBILE",
    "Terresa": "REPLACE_WITH_MOBILE",
    "HR": "REPLACE_WITH_MOBILE",
    "Carrie": "REPLACE_WITH_MOBILE"
  }
}
