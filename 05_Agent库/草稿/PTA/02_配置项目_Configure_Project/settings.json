{
  "agent_id": "PTA",
  "name": "项目任务协同 Agent",
  "version": "2.13.0",
  "status": "草稿",
  "description": "通用型项目执行引擎：理解自然语言指令，拆解执行，追踪进度，归档复盘；每天主动感知项目变化、提炼建议任务、判断该通知Terresa/HR/Jasper/Carrie中的哪几方、推送企业微信（组内约定只@Jasper本人，转达由他自己决定）；适用于任何项目，不局限于本项目自己的任务",
  "entrypoint": "04_定义Agent_Define_Agent/agents/agent.py",

  "migration_note": "v2.0.0：从扁平的 PTA-S01~S05 + PTA-RUN 脚本结构，迁移为 agents/skills/tools/memory/prompts/tests 标准结构。原来 5 个技能靠 subprocess 串联、用临时 JSON 文件中转，现在是同进程内的 Python 对象调用；config.json 拆成 settings.json（本文件，运行期配置）+ agents/agent.yaml（Agent 身份/能力声明）。旧扁平脚本移入 _retired_flat_structure/ 保留，不再是入口。v2.1.0：按《Agent 项目搭建全流程》11 步方法论，把 agents/skills/tools/memory/prompts/tests 六个包目录各自套进一个编号+中英文命名的顶层文件夹（01-11），作为以后任何 Agent 搭建都要遵循的标准结构；包名本身不变，只是外面多了一层编号目录。v2.2.0：逐文件夹复核，修复重排后失效的相对链接、pta_tasks_default.json 里指向已退役脚本的过时任务条目；修复 agent.py 一个真实 bug——指令被判定为模糊需要澄清时，此前只更新 current_task、漏了 追加进 task_history，导致'澄清触发率'这类指标永远统计不到数据；新增 11_监控与优化/PTA-MONITOR_自我监控.py，对应方法论第11步'监控 Agent 自己'的本意（区别于该文件夹里其余'帮你分析别的项目'的工具）。v2.3.0：新增 --daily-scan 每日主动巡检——本地文件 diff（新增 tools/file_diff.py，从 PTA-SCAN 抽取通用原语）+ 合并的 LLM 关系分析/相关性判断（新增 tools/llm_client.py，从 PTA-DISCOVER 抽取，PTA-DISCOVER 本身也改用它）→ 建议任务铸造成 RPT-YYYYMMDD-NN 格式 ID，安全 merge 进目标项目 pta_tasks.json（新增 tools/task_knowledge.merge_suggested_tasks，只增改 RPT- 前缀 key）。执行前必须人工确认（`执行 RPT-xxx --execute`），走的是完全不变的现有执行管线，不做全自动执行。新增 10_部署与运行/ 下的 launchd plist + 手动安装文档，用于每天定时触发。v2.4.0：6个扩展脚本迁移进skills/tools批1完成——PTA-DASH→skills/project_dashboard.py（新增--dashboard），PTA-EXT→tools/dir_scan.py（新增--dir-scan，顺带修复原脚本目录排除不完整的隐藏bug，复用file_diff.py的DEFAULT_EXCLUDE_DIRS）；两个原脚本移入_retired_flat_structure/。批2（DISCOVER/SCAN去重+包装成skill，含真正切换SCAN到sha256、修复SCAN定时模式写入项目目录的隔离bug）和批3（INTEL+INTEL-RW合并）已确认要做，待后续执行。v2.5.0：daily_sensing 从二元'跟Jasper有没有关系'升级为四方关联——SuggestedTask.relevant_to_jasper(bool) 改为 signal_to(List[str])+needs_mark_alignment(bool)，pta_focus.md 改版为 Terresa/HR/Jasper/Carrie 四方各自一段（Carrie只接收信号不参与裁定路由；需Mark裁定的事项不直接路由给他，标记needs_mark_alignment提示三方先内部对齐再线下带方案找Mark）；新增 tools/wecom_notify.py 企业微信群机器人通知（--notify，默认关闭），配置固定放 02_配置项目_Configure_Project/wecom_config.json（不进git、且绝不能放进任何--daily-scan会扫描的目标项目目录，避免手机号被当'变更文件'发给DeepSeek）。v2.5.1：修复真实联调时发现的SSL证书验证失败（wecom_notify.py复用llm_client.py已解决过的build_ssl_context，同一类macOS/Homebrew证书路径问题）；新增文件上传+file消息类型（upload_file/send_file，手工实现multipart/form-data编码），解决文本消息里塞本地文件路径、收消息的人在其他设备上点不开的问题——现在完整简报会作为附件直接发进企业微信群。v2.5.2：修复真实联调时发现的自我递归 bug——pta_tasks.json 是 daily_sensing 自己往目标项目写的输出产物，之前没有从扫描范围里排除，下次扫描会把刚写进去的建议任务当成「新变化」重新分析，产出一堆「关于任务的任务」；file_diff.snapshot_dir 新增 exclude_files 参数（按文件名精确匹配排除，不影响 dir_scan.py 等其他调用方），daily_sensing.py 新增 PTA_OWN_ARTIFACTS 常量（pta_tasks.json/pta_tasks.json.bak）传入排除。v2.5.3：与组内成员沟通后确定，企业微信通知只 @ Jasper 本人，不 @ Terresa/HR/Carrie——build_notification_text 不再按每条任务的 signal_to 汇总 @ 名单，固定只取 mobiles_map 里的 Jasper；signal_to 判断结果仍然显示在消息文字里，转达给谁、怎么转达由 Jasper 自己决定，机器人不越权直接 @ 到其他人。v2.5.4：daily_sensing_system.md 提示词重写——Jasper 反馈'推送的核心是谁新增/改/删了什么、背景是什么、哪些需要全员沟通找Mark决策、以及和我需要跟进的规则提取或SOP梳理相关的任务'，据此把提示词任务从三层扩到四层：变化摘要新增\"谁做的\"（仅在文件名含姓名等真实依据时推断，无依据填\"未知\"，不许瞎猜）和\"背景是什么\"（这个变化属于哪条工作线，帮助建立大局上下文）；建议任务里 signal_to 包含 Jasper 的判断标准从泛泛的\"有点重要\"收紧到\"和规则提取/SOP梳理直接相关\"；needs_mark_alignment 逻辑不变（已覆盖\"需要全员沟通找Mark决策\"这一点）。同时确认企业微信通知的推送渠道从原来的共享群改为 Jasper 自建的私人机器人群——这是纯配置变更（只需替换 wecom_config.json 里的 webhook_url），不涉及代码改动，新群的 webhook URL 需 Jasper 本人创建群+添加机器人后自己获取填入，不能由我代填。v2.5.5：已安装的 launchd 定时任务（~/Library/LaunchAgents/com.jasper.pta-daily-scan.plist）此前只有 --daily-scan --project-root，没有 --notify，导致每天10点自动跑的是本地扫描+生成简报，不会真的推送企业微信——发现后经 Jasper 确认，用 plistlib 精确插入 --notify 参数（不整体覆写文件，避免不必要地重新显示已写入的真实 DEEPSEEK_API_KEY），unload/load 重新加载生效；同步更新 10_部署与运行_Deploy_and_Run/ 下的 git 追踪模板 plist（补 --notify 说明注释、触发时间从模板默认的 8:00 同步成实际使用的 10:00），保持模板和已安装副本一致，供以后其他项目复用这份 plist 时不再遗漏 --notify。v2.6.0：daily_sensing 新增 .docx/.xlsx 扫描支持——之前 DEFAULT_SCAN_EXTENSIONS 只覆盖能直接当文本读的格式，Jasper 反馈「目前他人的更新会有docx和xlsx」，这两种格式本质是zip+XML，直接decode是乱码没法做逐行diff。新增 tools/office_text.py（extract_docx_text 用 python-docx 抽段落+表格文字，extract_xlsx_text 用 openpyxl 抽单元格值拼成文本网格，抽取失败或库未安装时优雅降级返回提示文字而不崩溃）；daily_sensing.py 的 _read_truncated 按后缀分流到抽取函数，DEFAULT_SCAN_EXTENSIONS 新增这两个后缀；hash_file 本身不受影响（sha256对原始字节，任何格式通用）。新增 requirements.txt 记录这是本项目唯一的两个非stdlib依赖（本机环境已装好：python-docx 1.2.0/openpyxl 3.1.5）。测试新增 Test 16，真实生成 docx/xlsx fixture 验证抽取内容正确、被正确纳入扫描范围、且真的进了喂给LLM的diff片段（不是二进制原文）。v2.6.1：修复真实巡检中发现的指纹去重失效 bug——2026-07-15 真实EA项目的 pta_tasks.json 里出现5组内容完全相同的重复RPT任务（同一件事被判定成新任务重复铸造ID）。根因：_task_fingerprint 用 name+related_files 一起哈希，而 related_files 是 LLM 每次分析时自己生成的列表，同一件事在两次独立LLM调用间输出会有细微差异（顺序/取舍不同），导致哈希不同、误判为新任务；两次调用发生是因为项目在同一天内又有了新变化，第二次不是空diff分支，真的触发了完整的LLM分析。修复：_task_fingerprint 改为只用 name 做哈希，不再掺 related_files。新增 Test 17 复现并锁定这个场景（两次非空diff扫描，同一任务名但related_files不同，断言只产生1个任务ID）。同时清理了已产生的真实脏数据：EA项目 pta_tasks.json 删除5条重复的RPT-20260715-06~10（保留01~05/11共6条，改动前已备份为.bak_before_dedup_cleanup）；对应工作区 daily_sensing_state.json 的11条指纹按新哈希方案合并去重为6条（改动前已备份为.bak_before_fingerprint_migration），与pta_tasks.json的6个RPT id校验一致。v2.7.0：扩展脚本迁移批2完成——PTA-SCAN→skills/rule_based_task_scan.py（新增--rule-scan，零LLM调用的规则抽取+风险预警，跟daily_sensing互补不重叠），PTA-DISCOVER→skills/document_task_discovery.py（新增--discover，需DEEPSEEK_API_KEY，产出仍只进task_registry.json供人工审阅，不进pta_tasks.json可执行流程的安全边界不变）；两个原脚本移入_retired_flat_structure/。迁移时的修复：①SCAN真正切到sha256（改用tools/file_diff.py的snapshot_dir/diff_snapshots，此前自己维护一套md5哈希逻辑）；②删除SCAN的--schedule内部忙等循环——那个模式会把报告直接写进目标项目自己的目录，违反隔离原则，且跟daily_sensing已确立的「launchd外部调度驱动单次调用」架构不一致；③删除FILE_PRIORITY/DocumentInfo.priority死代码（计算了但从未被使用）；④新发现并修复一个bug——DISCOVER的内容去重（同一内容的重复文件只处理一份）此前没有把被去重跳过的文件哈希记进增量状态，导致它们每次运行都被重新当「新文件」排队，是Test 19真实测出来的（不是从旧脚本继承的问题，是这次重写增量状态存取方式时新引入又被测试挡住的）。新增tools/file_diff.py的read_content_truncated共享助手，消化daily_sensing.py和document_task_discovery.py之前各自维护的重复编码兜底逻辑（也顺带给DISCOVER加上了.docx/.xlsx支持）。memory/workspace.py新增load_discover_state/save_discover_state/load_rule_scan_state/save_rule_scan_state，各自独立状态文件（跟daily_sensing_state.json的隔离原则一致，避免不同功能的「已处理」判断互相污染）。测试新增Test 18/19，全量19/19通过。批3（INTEL+INTEL-RW合并）仍待做。v2.8.0：扩展脚本迁移批3完成，三批全部迁移结束——PTA-INTEL+PTA-INTEL-RW合并为skills/project_intelligence.py（新增--intel --intel-mode {analyze,query,cross} --query）。深入对比后发现这两个原脚本表面同构（都是analyze/query/cross三模式+CrossDocumentAnalyzer），数据模型其实完全不同：通用版猜Markdown/CSV结构（TaskItem），Rw专用版精确读固定台账CSV固定列名（TrackItem，字段语义如source_work_id/today_action/escalation跟通用版完全对不上），强行统一数据模型是没必要冒的风险。这次合并的是入口层不是数据模型：两套解析器/分析器/CrossDocumentAnalyzer原样保留（改名Generic前缀/Rw前缀避免类名冲突），新增ProjectIntelligence统一入口，自动探测项目目录下有没有RwProjectParser.TRACKING_FILES列的固定台账CSV文件名来选后端，调用方不需要自己判断。明确删除的部分：原PTA-INTEL（不是INTEL-RW，两边本就不对称）内嵌的agent_status跨Agent状态上报，会写进PTA工作区和目标项目之外的第三方路径（Jasper全局的Agent健康报告.md/Agent运行仪表盘.md），违反已确立的workspace隔离原则，迁移时不保留。顺带修的真实bug：迁移到共享read_content_truncated时发现，原PTA-SCAN/PTA-INTEL-RW都显式处理过utf-8-sig（Excel导出CSV常见BOM问题），但批2迁移SCAN时改用的共享函数没覆盖这点，是当时没测出来的回归；这次在read_content_truncated里统一做BOM剥离修复（解码后lstrip掉U+FEFF），批2的rule_based_task_scan.py也一并受益。原两个脚本移入_retired_flat_structure/，11_监控与优化/现在只剩PTA-MONITOR一个独立脚本。测试新增Test 20（自动探测+两套后端+BOM回归）/Test 21（read_content_truncated的BOM剥离单测），全量21/21通过。v2.8.1：批3三批扩展脚本迁移完成后，首次拿真实Rw项目数据（40条真实跟踪项）跑--rule-scan/--intel/--dashboard/--dir-scan做验证，发现并修复2个真实bug（--discover涉及把真实内容发给DeepSeek，留给Jasper自己跑，未验证）。①RwProjectAnalyzer.analyze()进度统计出现负数（真实数据跑出「进行中: -9」）——根因是is_completed/is_blocked/is_ready三个判断互相独立，同一条跟踪项可能同时命中多个，而`总数-完成-阻塞-未开始`的减法假设四类互不重叠，现实数据里假设不成立。修复：改成按优先级（完成>阻塞>就绪>有今日动作视为进行中>未开始）单遍分类，每条只落一个桶，总数恒等，不会再出现负数。②`blocker != 无`精确字符串比较太天真——真实台账「无阻塞」常见写法是「无(Roy财务口径已确认...)」这种「无+补充说明」格式，精确比较会把解释性文字误判成「有阻塞」。这个bug同时存在于project_dashboard.py（批1迁移）和project_intelligence.py（批3迁移），因为两边独立继承了原脚本同一段逻辑。修复：新增_blocker_is_active()判断函数（去空白后以「无」开头就算没有阻塞），两个文件各自应用（按项目现有设计原则两个Rw解析器不共享代码，各自维护一份helper，不新增跨文件依赖）；project_dashboard.py顺带修了一个关联的展示bug——已完成的跟踪项如果blocker字段还留着说明文字，之前会同时出现在「已完成」和「阻塞中」两个板块，现在阻塞判断统一排除已完成项。用真实数据重新跑一遍验证：Rw项目「进行中」从-9变成0（40条总数吻合：15完成+0进行中+2就绪+23阻塞+0未开始=40），阻塞项从25降到23（两条被无中生有算成阻塞的假阳性被排除），dashboard的「关键风险」里对应的假阳性条目也消失。--rule-scan在真实叙述性文档上噪音很大（290条任务/552条风险，很多是从工作日志类文档误抽取的历史记录，不是真任务）——这不是纯bug，是关键词规则的信噪比问题，需要Jasper定调收紧到什么程度，本次未处理，留待后续。新增Test 22（RwProjectAnalyzer五分类总数恒等、不再出现负数）/Test 23（project_dashboard/project_intelligence两边blocker判断一致、「无(说明)」格式正确识别为无阻塞），全量23/23通过。v2.8.2：Jasper指出真实Rw项目的验证范围应该是整个项目目录（/Rw权益项目/），不只是台账CSV所在的07_项目立项启动/子目录——重新指向真正根目录后发现--intel/--dashboard两边都失效：is_rw_project()/RwParser都只检查project_root的直属文件，不递归查子目录，而真实台账CSV实际嵌套在07_项目立项启动/里，指向根目录时--intel悄悄退回通用解析器（明明是Rw项目），--dashboard直接报错找不到台账。这是跟v2.8.1同一类问题（project_dashboard.py和project_intelligence.py独立维护同一段Rw解析逻辑，一个位置修了另一个没修），这次不再各自打补丁，抽成共享模块tools/rw_conventions.py（TRACKING_FILES/CHARTER_FILE常量+find_tracking_csv/find_rw_data_dir/is_rw_project/blocker_is_active函数），两个skill都改成从这里导入，以后这类Rw数据格式规则只用改一处。修复过程中用真实完整项目数据重新验证时，又意外发现一个bug：真实项目里同名台账文件存在不止一份拷贝（07_项目立项启动/的正式版本之外，另一个OB巡检Agent自己的工作区08_RW_OB_工作区/01_启动基线/里还留着一份更早冻结的同名快照），find_tracking_csv第一版实现直接取rglob结果的第一项，在有重名文件时会不确定地选中哪一份（取决于文件系统遍历顺序），真实验证时就选中了那份旧快照，统计数字对不上直接指向正确子目录时的结果。修复：改成同名文件有多份时选最近修改的那一份（推定持续维护的台账文件修改时间必然比冻结的基线快照新）。新增Test 24复现「指向真正项目根目录+存在同名旧快照文件」这个真实场景，锁定「递归找到台账」和「重名优先选最新」两点。全量24/24通过。v2.9.0：按《Pipeline差距矩阵_检测机制设计_v1.0.md》规格新增`skills/pipeline_health.py`+`--pipeline-check`——对`[RPT]_AI协同建造者_Agent架构目标差距与实现路径_Jasper_v5.0.html`矩阵里能查证的部分（文件存在性/测试exit code/字段读取/mtime），做确定性核对，不做主观判断，跟`daily_sensing`刻意保持独立（不需要LLM关系分析，混进去反而降低确定性、且平白产生DeepSeek调用费用）。检测项定义外置为`05_Agent库/草稿/_pipeline_health/checks.json`（结构类比`pta_tasks.json`），加载优先级复用`task_knowledge.py`同款的显式路径>project_root>内置默认三级模式（内置默认`pipeline_checks_default.json`是空列表兜底）。首次真实运行（非--dry-run）就验证了机制本身的价值——不只是理论设计：①捕获到README.md「测试覆盖13项」与`test_integration.py`实际24个测试函数的真实文档漂移（本次顺带修复，见下）；②捕获到`/tmp/pta-daily-scan.log`缺失（daily_sensing的launchd定时任务在本机没有留下预期的运行痕迹，需人工核实是环境问题还是任务未安装/未运行）；③意外发现`test_integration.py`在本机实际运行exit_code=1（不是声明的24/24全过）——根因待查，`memory/workspace.py`的`WORKSPACE_ROOT`兜底默认值仍硬编码指向上一台机器的用户目录（`/Users/zhaoqitrenda.cn/...`），本机未设置`PTA_WORKSPACE_ROOT`环境变量时测试清理阶段（`finally`块里的`shutil.rmtree`前置`mkdir`）因该路径不可写而抛`PermissionError`导致整个测试进程非正常退出，这掩盖了测试内容本身的真实通过率，需要另外确认是否还有独立于此的真实回归（Test 7「状态记忆未生成」/Test 13「未按预期报错」两处check()失败是否也是同一根因的连锁反应，还是独立问题）——这条留给下一次任务处理，本次不顺手改，避免在验证新功能的同时又动了别的代码引入变量。README.md的13→24已在本次同步修正（见README变更）。新增独立的每周五09:00 launchd任务（`com.jasper.pta-pipeline-check.plist`，跟daily-scan的每日plist完全独立，不共用触发节奏，不需要DEEPSEEK_API_KEY）。v2.9.1：诊断出daily_sensing「没在跑」的根因——这台机器从未安装过`com.jasper.pta-daily-scan.plist`（launchctl list/~/Library/LaunchAgents均无记录），EA项目工作区最后一份报告停在07-15，纯粹是换机器后没有重新走安装步骤，不是代码坏了。顺带修复一个被--pipeline-check间接暴露、这次真正踩实的真实bug：`memory/workspace.py`的`WORKSPACE_ROOT`兜底默认值硬编码指向上一台机器的用户目录，本机没设`PTA_WORKSPACE_ROOT`时`get_project_workspace()`的mkdir直接抛`PermissionError`——改用`Path.home()`动态推导，不再硬编码用户名。同时把daily_sensing从「一项目一次巡检」扩展为多项目：`skills/daily_sensing.py`的`DailySensor`新增`extra_exclude_dirs`（与内置默认排除取并集，不是替换），`agent.py`新增`--exclude-dirs`透传；新增`10_部署与运行_Deploy_and_Run/multi_project_daily_scan.py`调度器，读`02_配置项目_Configure_Project/daily_scan_projects.json`项目清单依次调用，新增独立的`com.jasper.pta-multi-daily-scan.plist`（一个job服务任意多个项目，加项目只改JSON不用再装plist）。首批清单：EA流程架构项目、Rw权益项目、Jasper工作文档（不含EA项目）（含exclude_dirs排除OB知识库_vault等无关/高频变动子目录）。两个plist已装进~/Library/LaunchAgents/并launchctl load成功，唯一剩余人工步骤是填真实DEEPSEEK_API_KEY（按惯例不由AI代填）。WORKSPACE_ROOT修复后重新验证：test_integration.py真实24/24通过、exit_code=0，此前的Test 7/13两处失败确认是同一个PermissionError根因的连锁反应，不是独立回归。v2.10.0：PTA↔OB接口设计的第一个真实试点落地——新增tools/ob_bridge.py（subprocess调用OB自己的agent.py --retrieve，不直接import OB的Python包，因为PTA/OB各自都有agents/skills/tools同名包，同进程import会静默污染sys.modules缓存）；skills/document_task_discovery.py新增use_ob_context开关（默认关闭），开启后逐文件分析前用文件名查询OB取得项目背景注入提示词，试图缓解该技能在叙述性文档上噪音大的问题（历史记录被误判成新任务）；agent.py新增--use-ob-context透传给--discover。选document_task_discovery作为试点而非daily_sensing/rule_based_task_scan，是因为它单文件孤立分析、缺背景的代价最直接。真实验证：直接调用OB的--retrieve确认检索链路真实可用（真实查询「价值节点」命中3条带信任标注的结果，vault里非0命中）；用假API key跑通完整--discover --use-ob-context CLI链路，确认OB背景检索步骤正确执行且不崩溃，失败准确停在预期的DeepSeek 401认证错误——即OB接入这一步本身是通的，真实DEEPSEEK_API_KEY可用后即可验证背景内容是否真的改善了任务抽取质量（这条留待后续用真实key专门验证，本次只验证了接口链路本身）。v2.10.1：真实DEEPSEEK_API_KEY已写入`~/Library/LaunchAgents/com.jasper.pta-multi-daily-scan.plist`的安装副本（不进git），并reload生效。同时把`tools/llm_client.py`的`DEFAULT_MODEL`从即将下线的`deepseek-chat`换成`deepseek-v4-pro`——查证`api-docs.deepseek.com`官方公告确认`deepseek-chat`/`deepseek-reasoner`已宣布2026-07-24 15:59 UTC起完全下线（届时路由到`deepseek-v4-flash`而非继续可用），换模型既满足Jasper要更强模型（pro非flash）的要求，也顺带避开三天后的硬下线时间点。真实API调用验证：用真实key直接调用`deepseek-v4-pro`成功返回，确认模型名被API正确接受、不是仅凭文档猜测。v2.10.2：三项目（EA/Rw/Jasper工作文档）小范围真实测试，暴露并修复两个真实问题：①`skills/daily_sensing.py`新增`seed_baseline()`+`agent.py`新增`--seed-baseline`——Rw(1719候选文件)/Jasper工作文档(440候选文件)从未建过基线，直接跑`--daily-scan`会把全部现存文件当「今天的变化」打包成一次巨大LLM调用，语义不对也真实费token；新增只写快照不调LLM的「种子基线」能力，先建立起点，从下一次真实巡检起才是名副其实的增量对比。②`daily_scan_projects.json`给Jasper工作文档新增排除`项目工作区`——真实复现了自我递归噪音：daily-scan自己写的报告/状态文件被当成「项目变化」反过来分析，`PTA_OWN_ARTIFACTS`只排除了`pta_tasks.json`一个文件名，没覆盖整个工作区目录。③顺带发现`deepseek-v4-pro`（带思考模式的推理模型）响应明显比`deepseek-chat`慢，真实跑EA项目6天累积的416处变更时60秒HTTP超时被触发——`tools/llm_client.py`超时从60秒调到180秒。三项目最终测试结果：Rw/Jasper工作文档零变化零成本；EA真实产出9条建议任务（含P0级治理审计红线事项），企业微信通知+完整简报文件真实发送成功，全链路端到端验证通过。v2.11.0：补齐daily_sensing「执行→回写」闭环这一真实缺口（Jasper发现：任务没被标记完成时，检测机制该怎么体现价值）。此前`status`字段只在建议任务首次生成时写成pending，之后再没有代码路径改过它，导致任务不管有没有被真正执行都天天原样重复出现。新增：①`memory/workspace.py`的`mark_suggested_task_status()`；②`run_instruction()`执行RPT-xxx任务成功后自动回写done状态；③新增`agent.py --dismiss TASK_ID`人工关闭（不走执行的任务）；④`SuggestedTask`新增`first_suggested`字段支持算搁置天数；⑤同一指纹之前被标完成/关闭但又被重新建议时，自动重开为pending并重置搁置计时（视为「新」而非「搁置中」，避免「已搁置0天」的自相矛盾文案）；⑥新增`resolved_tasks`——已完成/关闭的任务在下一次简报里展示一次提示后不再重复出现，不是静默消失。同时按报告优化要求重构输出：`daily_sensing_system.md`提示词新增`domain`短标签字段（替换此前从没被实际使用过的`background`叙述字段），`format_text()`按域分组展示变化，建议任务按新增/搁置中（按天数降序）分桶；新增`format_text_plain()`通俗版，企业微信正文改发通俗版、详细版仍作为文件附件。`tools/wecom_notify.py`重构出`truncate_utf8_safe()`+`build_notification_text_from_content()`，旧接口保留给测试套件。真实验证：mock掉LLM调用（避免真实费用），完整跑通新建议→人工dismiss→展示一次已关闭→不再重复→同名任务重开为pending并重置计时的全流程；`test_integration.py`全量24/24仍然通过，无回归。v2.12.0：补齐「文件回执自动识别」——Jasper的前端设想里，任务分两种关闭方式：需要文件回执的应该自动识别，不需要的才走人工勾选/CLI。此前只有人工触发的done/dismissed，没有「新文件自动满足某条搁置任务」的判断。新增：`daily_sensing_system.md`提示词加第5层职责，把「当前搁置中的建议任务」（task_id+名称+关注文件）喂给模型，模型逐条核对today的变化是否满足要求，命中的在`resolved_pending_tasks`里报告并附证据，从严判断（宁可漏报不错报，因为漏报只是继续搁置、错报会让一条没做完的任务被误判静默关闭）；`scan()`解析这个字段，只信任真实存在于当前搁置列表里的task_id（防止模型编造），命中的立刻标done，当次简报就展示「已完成（文件回执自动识别）」+识别依据，不用等下一轮。顺带修复一个数据丢失：fingerprint此前从未持久化`related_files`，导致「沿用上次巡检」路径重建的建议任务永远丢失关联文件信息——现在存进fingerprint，也是这次回执匹配能拿到「这条任务关心哪些文件」的前提。真实验证（mock LLM，不产生真实费用）：模拟「第1天生成需要KPI表格的任务→第2天真的补了对应文件→模型正确识别→自动标done并展示识别依据→第3天不再重复」全流程，行为符合预期；`test_integration.py`全量24/24仍然通过，无回归。v2.13.0：新增`12_任务看板_Task_Dashboard/`——PTA第一个真正的前端，落地「没有文件回执的任务，在界面上勾选关闭/继续跟踪」这条此前只能靠CLI（`--dismiss`）操作的路径。后端`api/server.py`用纯标准库`http.server`（不引入FastAPI/Flask，延续本项目一贯的依赖习惯），`api/views.py`是胶水层，业务逻辑全部复用已有函数：新增`skills/daily_sensing.py`的`list_tasks_from_state()`（直接读持久化状态分桶，不跑LLM不做diff）和`skills/pipeline_health.py`的`summarize_latest_report()`（只读最新报告数drift，不重新跑检测）。前端是独立的Vite+React+TS+Tailwind项目（跟VNW自己的app_v2是兄弟关系，只借用其主题token和StatusBadge/PriorityBadge组件写法，不合并路由/不共用app_v2的`react-router`/`recharts`/`framer-motion`）。范围明确排除VNW/AIT/方法论/OB四块（各自现状未变，硬做是假面板）。真实验证：4个API接口对真实3个watched项目全部curl验证通过，包含一次真实写回（dismiss了`RPT-20260715-03`真实存在的搁置任务，验证状态真的从aging搬到resolved_recent）；单进程生产模式（build+静态兜底）验证通过；`npm run build`零TypeScript错误。`test_integration.py`新增Test 25-27（`list_tasks_from_state`分桶+时间窗单测、`summarize_latest_report`单测、真实起停HTTP服务器的API冒烟测试），全量27/27通过，无回归。**已知限制（诚实记录，未做）**：前端未做浏览器可视化验证（Chrome MCP工具本次会话不可用），只验证了TypeScript编译通过+Vite模块图正确解析+API集成，没有验证真实DOM渲染/点击交互；建议使用前先手动在浏览器里过一遍任务看板的勾选流程。",

  "loop": "think-act-observe",
  "skills": ["intent_parsing", "execution_planning", "progress_tracking", "archive_review", "doc_sync",
             "project_dashboard", "daily_sensing", "rule_based_task_scan", "document_task_discovery",
             "project_intelligence", "pipeline_health"],
  "tools": ["shell_exec", "git_ops", "task_knowledge", "file_diff", "llm_client", "dir_scan", "wecom_notify",
            "office_text", "rw_conventions", "ob_bridge"],
  "memory": ["workspace"],

  "daily_sensing": {
    "note": "独立于上面 think-act-observe 主循环的另一个能力：--daily-scan 检测文件变化、分析关系、判断该通知 Terresa/HR/Jasper/Carrie 四方里的哪几位（signal_to），需 Mark 裁定的事项标记 needs_mark_alignment（不直接路由给 Mark），产出简报并把建议任务写进 pta_tasks.json，但绝不自动执行，需人工确认后走正常执行路径；--notify 时额外推送企业微信群通知（@ 相关方），默认关闭",
    "skill": "daily_sensing",
    "requires": "DEEPSEEK_API_KEY 环境变量（--notify 额外需要 02_配置项目_Configure_Project/wecom_config.json）",
    "scheduling": "10_部署与运行_Deploy_and_Run/com.jasper.pta-daily-scan.plist（需手动安装，见同目录 INSTALL_DAILY_SCAN.md）"
  },

  "pipeline_check": {
    "note": "独立于 daily_sensing 的另一条能力：--pipeline-check 依据 checks.json 定义的确定性检查项（文件存在性/测试exit code/字段读取/mtime），核对 Pipeline差距矩阵的声明与实际状态是否一致，不调用 LLM、不做主观判断，判断交给人",
    "skill": "pipeline_health",
    "checks_definition": "05_Agent库/草稿/_pipeline_health/checks.json（加载优先级同 task_knowledge：显式路径 > project_root下 > 内置默认兜底 pipeline_checks_default.json）",
    "report_output": "{project_root}/05_Agent库/草稿/_pipeline_health/检测记录_YYYY-MM-DD.md，基线存于同目录 .baseline.json",
    "scheduling": "10_部署与运行_Deploy_and_Run/com.jasper.pta-pipeline-check.plist（需手动安装，独立于 daily-scan 的每日plist，每周五09:00触发，不需要 DEEPSEEK_API_KEY）"
  },

  "task_knowledge_base": {
    "note": "任务执行知识库（任务名+执行步骤）优先级：--task-map 显式路径 > --project-root 下的 pta_tasks.json（目标项目自定义）> pta_tasks_default.json（本项目内置，兜底）",
    "default_path": "05_集成工具_Integrate_Tools/pta_tasks_default.json"
  },

  "workspace": {
    "note": "PTA 自己的状态/运行产物不落在这个共享仓库里，落在按目标项目区分的专属工作区（见 07_接入记忆_Integrate_Memory/memory/workspace.py）",
    "root_env_var": "PTA_WORKSPACE_ROOT"
  },

  "trigger": {
    "auto": [],
    "manual": ["用户指令: 按顺序完成...", "用户指令: 回顾下进度", "用户指令: 执行"]
  },

  "archive_gate": {
    "mark_confirmed": false,
    "deliverables": ["settings.json", "agents/agent.yaml", "README.md", "执行记录.md"]
  }
}
