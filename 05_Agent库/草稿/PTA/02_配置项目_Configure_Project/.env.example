# PTA 运行期环境变量示例。复制为 .env 后按需修改（.env 本身不入库，见 .gitignore）。

# PTA 自己的状态/运行产物（跨会话记忆、每次运行的 task/plan/report 快照）存放根目录。
# 不设置时使用 memory/workspace.py 里的默认值：
#   /Users/zhaoqitrenda.cn/Desktop/Jasper工作文档（不含EA项目）/项目工作区
PTA_WORKSPACE_ROOT=
