{
  "_说明": "复制为同目录 github_config.json，填入真实值，真实文件不进git。GitHub token只需要public repo读权限（fine-grained PAT可以不勾任何scope，公开数据默认可读）。用途：github_discovery.py 调用 GitHub REST API 时提高限流上限（未认证60次/小时，认证后5000次/小时）。",
  "GITHUB_TOKEN": "REPLACE_WITH_YOUR_GITHUB_TOKEN"
}
